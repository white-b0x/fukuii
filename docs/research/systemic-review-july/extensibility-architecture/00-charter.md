# Extensibility Architecture — Charter (placeholder)

Populated when `SR-EXT-01` (`.claude/sprints/QUEUE.md`, "Systemic Review" section) is
dispatched. Builds directly on `docs/architecture/pluggable-consensus-vision.md`'s existing
3-layer model and Orbita naming — operationalizes it into concrete findings, does not
re-derive it.

Planned doc set (per `.agents/protocols/process/systemic-review-protocol.md`):
`00-charter.md` (this file, authored by the main session once `SR-EXT-01` starts) →
`01-current-state-pluggability-audit.md` (`SR-EXT-01`'s output) →
`02-consensus-plugin-pattern-proposal.md` → `03-network-layer-generalization-proposal.md` →
`04-domain-config-fork-table-generalization.md` → `05-worked-example-poa-network.md` →
`06-adr-recommendations.md` (`02`–`06` are `SR-EXT-02`'s output, gated on `01`). Ready now
(`SR-EXT-01` has no Batch 2/3/4 file overlap); `SR-EXT-02` is `BLOCKED-ON-SR-EXT-01`.
