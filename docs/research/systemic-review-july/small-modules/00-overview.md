# small-modules — Overview (placeholder)

Populated when `SR-11` (`.claude/sprints/QUEUE.md`, "Systemic Review" section) is dispatched.
Small-subsystem basket: `faucet`, `forkid`, `cli`, `security`, `testmode`, `nodebuilder`,
`metrics`, `healthcheck`, `keystore`, `ommers`, `common`/`console`/`logger`/`runtime`/`utils`,
and `transactions`. Tier B, one `##` section per package. Ready now except the
`transactions` sub-section (`BLOCKED-ON-BATCH-4`).
