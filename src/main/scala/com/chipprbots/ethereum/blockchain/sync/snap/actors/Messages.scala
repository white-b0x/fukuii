package com.chipprbots.ethereum.blockchain.sync.snap.actors

/** Message protocols for SNAP sync actors.
  *
  * All coordinator-specific messages now live in each coordinator's companion object (sealed Command ADTs). This file
  * is retained as a package anchor for backward compatibility with any remaining wildcard import sites.
  */
object Messages {}
