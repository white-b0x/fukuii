# RX — L10 `node` (composition root) + `cli` per-item reference-client verification

_The depth pass for the composition root. Each item the L10 plan (`plan/L10.md` §1–§10 + `optimizations.md`
L10 rows + `requirements.md` R1/R2/R5/R7/R8/R9/R11 L10 cells + `feature-ledger.md` F8/F12) commits to
implement, verified against the **actual vendored reference-client source** under
`.claude/repo-references/clients/{besu,core-geth,erigon,go-ethereum,nethermind,reth}/`. Method: `rx/README.md`._

**L10 is composition, not consensus** — but the family-dispatch wiring (RX-L10-13/14) *gates which consensus
engine activates*, so those two items are forge/beacon co-review (chain-split-class), and every entry is Tier
A/B/C per the crux: the R2 isolation decisions are the blast-radius core (Q3b). **Every entry answers the four
questions before its verdict.** Verdict: **CONFIRMS** / **CORRECTS** (Q1/Q2/Q3 no → Q4 fixes) / **SHARPENS**
(right decision, a load-bearing mechanism/blast-radius detail to fold in).

**Authority note (README §"absence is evidence"):** node-lifecycle is **not consensus** — core-geth is NOT a
byte-authority here (it inherits geth's container verbatim; its only L10 relevance is the `--classic`/`--mordor`
preset *values*). The structural authorities are **go-ethereum** `node/{node.go,lifecycle.go}` (ordered-start/
reverse-stop + `ShutdownTracker`), **besu** (the JVM implementation guide — `ServiceManager`, `BesuPlugin`
FSM, `Runner` phase-ordering, `BesuControllerBuilder` per-mechanism wiring, `CascadingDefaultProvider`),
**erigon** (`WaitIdle` drain + `node/direct/` embedded-or-remote shim — sole authority), **nethermind**
(`INethermindPlugin` self-declaration + `EthereumStepsManager` concurrent step-init — the OBSOLETE example),
**reth** (`ForkCondition` prior art). The **7th client is fukuii's own AS-IS**, cited from the SR snapshot
`clients/fukuii/node-lifecycle.md` + `consensus-engines.md` (the reference tree is on `july-fourth`).

**Path-existence pre-check (every cited path verified present in the vendored tree):** go-ethereum
`node/{node.go,lifecycle.go}`, `internal/shutdowncheck/shutdown_tracker.go`, `core/rawdb/accessors_metadata.go` ✓;
besu `plugin-api/…/plugin/{ServiceManager,BesuPlugin}.java`, `app/…/Runner.java`, `app/…/controller/{BesuController,BesuControllerBuilder,MainnetBesuControllerBuilder,CliqueBesuControllerBuilder,QbftBesuControllerBuilder,MergeBesuControllerBuilder,TransitionBesuControllerBuilder,ConsensusScheduleBesuControllerBuilder}.java`, `app/…/services/BesuPluginContextImpl.java`, `app/…/cli/util/{CascadingDefaultProvider,EnvironmentVariableDefaultProvider}.java` ✓;
core-geth `cmd/utils/flags.go` (`ClassicFlag`/`MordorFlag`) ✓;
nethermind `Nethermind.Api/Extensions/{INethermindPlugin,IConsensusPlugin}.cs`, `Nethermind.Api/Steps/IStep.cs`, `Nethermind.Init/Steps/EthereumStepsManager.cs`, `Nethermind.Runner/Ethereum/EthereumRunner.cs` ✓;
erigon `node/direct/{txpool_client,eth_backend_client,sentry_client}.go`, `execution/execmodule/exec_module.go` (`WaitIdle`), `cmd/utils/flags.go` (`*.api.addr`) ✓;
reth `crates/ethereum/hardforks/src/hardforks/mod.rs` (`ForkCondition`) ✓.

---

## Group 1 — Guardian tree / Typed root / spawn-time DI (retire the cake)

### RX-L10-01 · Typed guardian tree — `Behavior[GuardianCommand]`, one child per subsystem · Tier A (structural spine, R5) · owner-layer L10 `node`
- **Plan claim / disposition:** §5 idiom target + §4 besu mirror — a top-level `Behavior[GuardianCommand]` (sealed ADT) spawns one child per subsystem, each constructed with its `ActorRef[Command]` dependencies **injected at spawn** (besu `ServiceManager` shape); no `actorSelection`/global lookup, no `Behavior[Any]`. Maps the geth ordered-start container DEFAULT onto a Pekko-Typed supervisor.
- **fukuii AS-IS:** one **Classic** `ActorSystem` (`ActorSystemBuilder.classicSystem`, `NodeBuilder.scala:111`) wrapped `.toTyped`; every actor `classicSystem.spawn`ed into the Classic guardian tree while callers hold a Typed `ActorRef[Command]` (`clients/fukuii/node-lifecycle.md` §ActorSystem topology).
- **Reference source (byte-cited):**
  - go-ethereum `node/node.go:58` — `lifecycles []Lifecycle` flat registration list; `:190` `for _, lifecycle := range lifecycles { lifecycle.Start() }` — registration order *is* the DAG. The container contract fukuii's guardian replaces mechanism-for-mechanism.
  - besu `plugin-api/…/plugin/ServiceManager.java:33,56` — `<T> void addService(Class<T>, T)` / `Optional<T> getService(Class<T>)`: a typed `Map<Class,BesuService>` handed to components, "the only time it will be provided … store it in a field" (`BesuPlugin.java:44-49`) — spawn-time injection, not late global lookup. The JVM analog of the Typed target.
  - No reference client uses actors (`cross-cutting-themes.md` Theme 1) — the *mechanism* is fukuii's; the clients give the target *shape* (typed service handed in once, field-stored).
- **Q1 appropriate decision?** Yes — a Typed guardian with spawn-time injection is the correct R5 target; it is the actor-idiomatic form of geth's container + besu's `ServiceManager`, and dissolves the AS-IS Classic-root `.toTyped` surface.
- **Q2 what we should implement?** Yes — the sealed `GuardianCommand` ADT + one-child-per-subsystem, not `Behavior[Any]`. The all-client read shows no better alternative (reth's typestate needs one-binary-per-family; fails R2).
- **Q3 understanding + blast radius?** Mechanism correct. Blast radius: every L6/L7/L8/L9 long-lived actor's construction site changes (each now receives its `ActorRef` deps at spawn) — the plan states this at R5. The guardian is the convergence point for RX-L10-05 (shutdown phases), RX-L10-09 (multi-instance = N guardian subtrees), RX-L10-07 (secondary ActorSystems).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L10-02 · Spawn-time `ActorRef[Command]` DI (besu `ServiceManager`) replaces the ~90-builder cake · Tier A (structural spine, R5) · owner-layer L10 `node`
- **Plan claim / disposition:** `optimizations.md` L10 row 3 DEFAULT(R5) + §4 — dependencies handed to an actor **at spawn** through a typed context (besu "`register(ctx)` once, field-store it, no global singleton lookup"), the direct counter to nethermind's `INethermindApi` god-context and fukuii's cake god-trait.
- **fukuii AS-IS:** cake / self-type DI — ~60 `*Builder` traits (`self: A & B & C =>` + `lazy val`), god-trait `Node` mixes ~90 (`NodeBuilder.scala:1092-1170`), one 1181-line file; `MiningBuilder.scala:9-13` imports back into `nodebuilder` = a genuine `consensus↔nodebuilder` self-type cycle (`clients/fukuii/node-lifecycle.md` §Gotchas).
- **Reference source (byte-cited):**
  - besu `BesuPlugin.java:44-51` — "The `context` parameter should be stored in a field … the only time it will be provided" — inject-once-then-field-store. `ServiceManager.SimpleServiceManager` (`ServiceManager.java:59-74`) = the typed `Map<Class,BesuService>` container both core and plugins read/write.
  - besu `app/…/controller/BesuControllerBuilder.java:133` `abstract class BesuControllerBuilder` with `createConsensusContext`/`createMiningCoordinator` (`:1277,:1247`) abstract, per-mechanism subclasses (`MainnetBesuControllerBuilder`/`CliqueBesuControllerBuilder`/`QbftBesuControllerBuilder`/`MergeBesuControllerBuilder`) — the composition root selects one at build time from genesis; the "per-mechanism wiring" reference.
  - nethermind `INethermindApi` god-context = the named anti-pattern the DI migration retires (`observations/node-lifecycle.md` best-practice #4).
- **Q1 appropriate decision?** Yes — DEFAULT; spawn-time injection is the R5-mandated replacement for both the cake god-trait and the nethermind god-context.
- **Q2 what we should implement?** Yes — constructor-injected `ActorRef[Command]`, never a global/`actorSelection` lookup. Correct; no YAGNI concern (this IS the SCC fix).
- **Q3 understanding + blast radius?** Mechanism correct. Blast radius: retiring the cake ripples to *every* module's construction (the ~90 builders collapse into the guardian's child-spawn sites); ties directly to RX-L10-03 (the DAG down-only fix).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L10-03 · `node` depends down-only, nothing depends on it — the 13-package SCC fix · Tier A (structural spine) · owner-layer L10 `node`
- **Plan claim / disposition:** §1 + §6 + §Layer boundaries — `node` is the sole composition root, "depends down on all L0–L9 and nothing depends on it"; that single rule dissolves the `nodebuilder↔consensus` cycle; an upward edge is a compile error.
- **fukuii AS-IS:** the cake `Node` transitively imports `ledger`/`consensus`/`sync`/`jsonrpc`/`network`/`db`/`domain` (`NodeBuilder.scala:27-71`) with a **bidirectional** `consensus/mining/MiningBuilder.scala:9-13 → nodebuilder` self-type cycle; "nodebuilder sits at the coupling apex" (`clients/fukuii/node-lifecycle.md` §Gotchas). **The SR itself flagged the "13-package SCC" phrase as unverified** — `dependency-graph.md` names concrete cycles, not that SCC verbatim.
- **Reference source (byte-cited):**
  - go-ethereum `node/node.go` — `makeFullNode` wires services into the container; nothing imports `node` back (leaf composition root). besu `BesuController`/`RunnerBuilder` sit at the same apex with no upward edge.
  - The SR-11 verdict (`clients/fukuii/node-lifecycle.md` §Gotchas VERIFY note): cite the concrete cycles (`db↔mpt`, `network→blockchain`, `jsonrpc→almost-everything`), **not** the unconfirmed "13-package SCC centrality" figure.
- **Q1 appropriate decision?** Yes — a down-only composition root is the correct structural target; every client's composition root is a leaf.
- **Q2 what we should implement?** Yes — the compile-enforced DAG edge (`node → {L0–L9}` only). Correct.
- **Q3 understanding + blast radius?** Mechanism correct. Blast radius: the `consensus↔nodebuilder` cycle specifically dissolves because family selection moves to L5 (`EngineId`) and only *wiring* stays at L10 (RX-L10-14). The plan should carry the SR's own caveat.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.
- **Plan edit (SHARPENS):** §6 — when the plan asserts the "old 13-package strongly-connected component," annotate it with the SR's VERIFY caveat (`clients/fukuii/node-lifecycle.md` §Gotchas: the SCC size/centrality is *unconfirmed* in `dependency-graph.md`); cite the concrete named cycles instead, so the "improvement over AS-IS" claim rests on verified evidence.

---

## Group 2 — Lifecycle FSM / CoordinatedShutdown / custody-grade shutdown

### RX-L10-04 · Lifecycle-as-Behavior FSM + registration-after-start guard (single-shot, besu `resetState` caveat) · Tier B · owner-layer L10 `node`
- **Plan claim / disposition:** §2 (DEFAULT guard, with caveat) + §4/§5 — `Initializing/Starting/Running/Stopping/Stopped` as distinct Pekko-Typed `Behaviors`; an illegal transition is a wrong-state message, not a silent no-op (besu `checkState`-guarded FSM). Registration-after-start = panic (single-shot); caveat: a strict one-shot FSM forbids dynamic service addition (besu bolted on `resetState()`).
- **fukuii AS-IS:** imperative `BaseNode.start()` (`StdNode.scala:50-89`) — a hand-ordered six-phase method forcing lazy-vals; no FSM, no state guard (`clients/fukuii/node-lifecycle.md` §Design decisions).
- **Reference source (byte-cited):**
  - besu `BesuPlugin.java` FSM callbacks `register`→`beforeExternalServices`→`start`→`afterExternalServicePostMainLoop`→`stop` (`:51,57,64,67,89`); the `Lifecycle` enum (`UNINITIALIZED→…→STOPPED`, `checkState`-guarded) is the behavior-as-state-machine reference.
  - besu `app/…/Runner.java:289-295` `ephemeryRestartPrepare` → `besuCommand.getBesuPluginContext().resetState()` + `.initialize(...)` + `.registerPlugins()` — **proof of the caveat**: besu had to add `resetState()` to re-run the FSM in-process for Ephemery.
  - go-ethereum `node/node.go:168-175` — `switch n.state { case runningState: return ErrNodeRunning; case closedState: return ErrNodeStopped }` + `:227` `panic("node is in unknown state")` — the single-shot guard DEFAULT.
- **Q1 appropriate decision?** Yes — behavior-as-FSM + hard single-shot guard is the correct DEFAULT.
- **Q2 what we should implement?** Yes — but the caveat is load-bearing for fukuii specifically: `TestNode` per-test fork reconfiguration (`clients/fukuii/node-lifecycle.md`: `AtomicReference[BlockchainConfig]`) is exactly the "dynamic re-run" case besu needed `resetState()` for. A strict one-shot FSM must expose a reset path for testmode, or tests break.
- **Q3 understanding + blast radius?** Mechanism correct. Blast radius: the FSM owns the shutdown sequencing (RX-L10-05/06/07); the testmode reset path touches `eye`'s test harness (per-test reconfig).
- **Q4:** n/a (SHARPENS).
- **Verdict:** SHARPENS.
- **Plan edit (SHARPENS):** §2/§5 — name the testmode consequence: the single-shot FSM must ship a **guarded reset transition** (the besu-`resetState()` analog) for `TestNode`/ephemery-style in-process reconfiguration, or the AS-IS per-test fork-reconfig (`AtomicReference[BlockchainConfig]`) has no home. State it as a DoD vector.

### RX-L10-05 · Ordered-start / reverse-stop via `CoordinatedShutdown` phases (geth container + besu phase-ordering distributed across the tree) · Tier A (structural spine) · owner-layer L10 `node`
- **Plan claim / disposition:** §2 DEFAULT(container) + §4 — geth ordered-start/reverse-stop; keep besu's *phases* (external-services before main-loop before plugins) but "distribute them across the supervisor tree rather than a 3000-line god-command," driven by `CoordinatedShutdown` phases.
- **fukuii AS-IS:** manual hand-ordered `shutdown` (`StdNode.scala:235-275`), each `Await`-bounded; `dataSource.close()` last; **no `CoordinatedShutdown`** (`clients/fukuii/node-lifecycle.md` §Gotchas).
- **Reference source (byte-cited):**
  - go-ethereum `node/node.go:284-296` `stopServices` — `for i := len(running) - 1; i >= 0; i--` — a **mechanical reverse** of the registration list; errors collected into `StopError`, never fatal; `:198` rollback on partial start (`n.stopServices(started)`). This is the reverse-order authority.
  - besu `app/…/Runner.java:153-166` `startExternalServices` (metrics→jsonRpc→engineJsonRpc→graphQL→ws→ipc) **before** `:173-197` `startEthereumMainLoop` (nat→networkRunner→mining→sync) — the phase ordering to preserve. **But `:298-323` `stopServices` is HAND-ORDERED** (jsonRpc, engine, graphQL, ws, ipc, txpool, metrics, mining, sync, network, nat, `besuController.close()` last), each bounded by `:352-366` `waitForServiceToStop(...30s)` — **not** a mechanical reverse.
- **Q1 appropriate decision?** Yes — DEFAULT; `CoordinatedShutdown` phases are the right Pekko home for registration-order-respecting teardown.
- **Q2 what we should implement?** Yes — phase-ordering distributed across the tree (not a god-command). Correct.
- **Q3 understanding + blast radius?** Mechanism mostly correct, **but one citation is imprecise**: besu's teardown is **hand-ordered + per-service-bounded (30s)**, *not* a mechanical reverse — the mechanical reverse-order is **geth's** (`node.go:289`). The lesson to carry from besu is the **bounded** (per-service timeout) teardown; the reverse-*order* discipline is geth's. Blast radius: the phase boundaries must map to the guardian subtree structure (RX-L10-01) and sequence the secondary systems (RX-L10-07).
- **Q4:** besu is the *bounded-teardown* authority; geth is the *reverse-order* authority — keep them distinct so the plan doesn't build a "reverse-order registration list" expectation from besu (besu has none).
- **Verdict:** SHARPENS.
- **Plan edit (SHARPENS):** §2/§3 — split the citation: geth `node.go:289` (`for i:=len-1`) = the mechanical reverse-order authority; besu `Runner.stopServices` = the **hand-ordered, per-service-bounded (30s `waitForServiceToStop`)** authority. `CoordinatedShutdown` phases give the ordering; each phase task carries a bound (the besu lesson).

### RX-L10-06 · Custody-grade shutdown: unclean-shutdown DB marker (geth `ShutdownTracker`) + drain-before-DB-close (erigon `WaitIdle`) · Tier A (custody; ordering load-bearing) · owner-layer L10 `node`
- **Plan claim / disposition:** §2 DEFAULT(shutdown) + §6 — graceful teardown + boot-time unclean-shutdown DB marker + "explicitly wait for every fire-and-forget task to release its DB transaction *before* closing the DB." AS-IS has neither.
- **fukuii AS-IS:** "No unclean-shutdown DB marker" (unlike geth `ShutdownTracker`); `dataSource.close()` is merely last in the list, "not gated on every fire-and-forget task having released its DB handle" (`clients/fukuii/node-lifecycle.md` §Gotchas).
- **Reference source (byte-cited):**
  - go-ethereum `internal/shutdowncheck/shutdown_tracker.go:27-30` — "It needs to be **started after a successful start-up and stopped after a successful shutdown, just before the db is closed**." `MarkStartup` (`:46`) `PushUncleanShutdownMarker` + reports previous; `Start` (`:63`) refreshes every 5 min; `Stop` (`:80`) `PopUncleanShutdownMarker` (clears). Note: geth keeps a **bounded list** of recent unclean timestamps (`:52-59` reports each), not a single boolean.
  - erigon `execution/execmodule/exec_module.go:309-311` — `// WaitIdle blocks until any in-flight updateForkChoice goroutine finishes` — the drain barrier (scoped to the in-flight forkchoice writer, the DB-touching task).
- **Q1 appropriate decision?** Yes — DEFAULT; both are custody preconditions the AS-IS lacks.
- **Q2 what we should implement?** Yes — marker + drain. A single boolean marker suffices for detect→repair (geth's list is a reporting nicety, not load-bearing for L10's goal).
- **Q3 understanding + blast radius?** Mechanism correct, **but the ordering is load-bearing and under-stated**: (a) `MarkStartup` runs **after** a successful boot (not at process entry); (b) the marker-clear (`Pop`) must be the **last op before `dataSource.close()`, sequenced AFTER the drain** — so that a hang during drain leaves the marker set (correctly signaling unclean on next boot); (c) `WaitIdle` needs a **registry of in-flight DB-touching fire-and-forget tasks** to drain — erigon drains a specific goroutine; fukuii must enumerate *which* tasks hold DB handles (the drain barrier is only as complete as that list). Blast radius: the drain registry touches every subsystem that writes async (L2 storage commit, L7 sync, L4 execution persist).
- **Q4:** n/a (SHARPENS).
- **Verdict:** SHARPENS.
- **Plan edit (SHARPENS):** §2/§8 — fold the shutdown ordering: `[MarkStartup after successful boot; refresh loop]` … `[graceful stop phases]` → `[drain: WaitIdle over the in-flight-DB-task registry]` → `[Pop marker]` → `[dataSource.close()]`. Name that the drain barrier requires an explicit registry of async DB-writers (not "reverse-order alone"). DoD vector: kill-during-write → marker survives → boot detects unclean; drain-before-close prevents the close hang.

### RX-L10-07 · Secondary ActorSystems (Engine-API 8551 + periodic-DB-check) → guardian shutdown sequencing · Tier A (blast-radius crux) · owner-layer L10 `node` (co-review L9)
- **Plan claim / disposition:** §5 — the two AS-IS secondary ActorSystems (Engine-API server owns 8551 + its own `IORuntime`; the periodic-DB-consistency check), today "sibling roots *outside* the main tree torn down by a hand-ordered `Await` list," **become child subtrees of the guardian** so `CoordinatedShutdown` sequences them.
- **fukuii AS-IS:** two secondary ActorSystems created outside the main tree, torn down explicitly (`StdNode.scala:201-218`; `clients/fukuii/node-lifecycle.md` §ActorSystem topology).
- **Reference source (byte-cited):**
  - besu `app/…/Runner.java:157-158,300-301` — `engineJsonRpc` is a *separate service* (own start/stop, own port), started/stopped in the phase pipeline — sequenced, but **not** collapsed into a shared container object.
  - **L9 plan (the lower-layer isolation claim):** `L9.md:141-142` — "fukuii already goes one level deeper (**a whole second `ActorSystem` + `IORuntime`**), which is **retained as a design improvement, not regressed**"; `L9.md:48` — "**L10 wires the isolated Engine-API system**." `optimizations.md` L9: "Engine-API dedicated `ActorSystem`+`IORuntime` isolation — **DEFAULT (fukuii-superior; keep)** — prevents peer/sync flood starving `fcU`."
- **Q1 appropriate decision?** The *goal* (guardian-sequenced shutdown, not a hand-ordered `Await` list) is right. The *mechanism as stated* is wrong.
- **Q2 what we should implement?** Sequence the secondary systems' teardown via `CoordinatedShutdown` — yes. But NOT by making the Engine-API a "child subtree of the guardian."
- **Q3 understanding + blast radius?** **Mechanism is wrong and it is a cross-layer isolation mismatch.** A second `ActorSystem` cannot be a literal "child subtree of the guardian" — the guardian is the user-guardian of *one* ActorSystem. Folding the Engine-API into the guardian tree **dissolves the separate `ActorSystem`+`IORuntime` that L9 explicitly retains** (`L9.md:141-142`, and L9 expects L10 to "wire the *isolated* Engine-API system"). L10's §5 mechanism claim directly contradicts L9's DEFAULT-keep isolation claim. (The periodic-DB-check system has *no* isolation requirement — it can safely fold into the guardian, or run as a guardian child actor.)
- **Q4 the correct answer + why:** **Keep** the Engine-API on its dedicated `ActorSystem`+`IORuntime` (L9's flood-isolation DEFAULT), and **sequence its shutdown by registering its `terminate()` as a `CoordinatedShutdown` phase task on the main system** — you sequence teardown without nesting a second ActorSystem under the guardian. Source: `L9.md:141-142` retains the second system as a design improvement; besu keeps its Engine-API service physically separate yet phase-sequenced (`Runner.java`). Only the periodic-check system folds into the guardian tree.
- **Verdict:** **CORRECTS** (cross-layer isolation-mechanism mismatch: L10 §5 vs L9 §5/§9).
- **Plan edit (CORRECTS):** §5 — replace "become **child subtrees of the guardian**" with: "the **periodic-DB-check** system folds into the guardian tree; the **Engine-API** system *retains its dedicated `ActorSystem`+`IORuntime`* (L9 DEFAULT, `L9.md:141-142`) and is shutdown-sequenced by **registering its `terminate()` as a `CoordinatedShutdown` phase task**, not by nesting it under the guardian." Re-enters a Wave-3 coherence mini-check (crosses L9↔L10).

### RX-L10-08 · Retire `StorageConsistencyChecker` auto-shutdown-on-gap (G-NL2) → marker + `fixDatabase` · Tier B · owner-layer L10 `node`
- **Plan claim / disposition:** §6 + `optimizations.md` L10 row 5 DEFAULT(fix) — `StorageConsistencyChecker` walks block-number→hash→header and calls `shutdown()` on any gap; three skip-guards (post-SNAP, mid-SNAP "Bug 28", Engine-API optimistic import) had to be bolted on to stop it turning a recoverable restart into a wipe-and-resync. Rebuild removes the auto-shutdown: defence = unclean-shutdown marker (detect) + boot-time `fixDatabase` (repair).
- **fukuii AS-IS:** `StorageConsistencyChecker.scala:24-27` calls the passed `shutdown()` on any header-chain gap; `PeriodicConsistencyCheck.scala:15` re-runs every 10 min with the same skip-guards (`clients/fukuii/node-lifecycle.md` §Gotchas + Key files).
- **Reference source (byte-cited):**
  - go-ethereum `ShutdownTracker` (RX-L10-06) — the corruption story is **detect-on-boot** (unclean marker), not **stop-on-gap-at-runtime**. No reference client shuts the node down on a walked-chain gap; recovery is boot-time repair. Absence-as-evidence: the auto-shutdown-on-gap is a fukuii-only anti-pattern.
- **Q1 appropriate decision?** Yes — DEFAULT(fix); a self-inflicted stop on a recoverable gap is strictly worse than marker+repair.
- **Q2 what we should implement?** Yes — remove the runtime auto-shutdown; keep `fixDatabase` (`StdNode.scala:277`) as the repair path. Correct.
- **Q3 understanding + blast radius?** Mechanism correct. Blast radius: removing it also removes the three skip-guards' reason-to-exist (post-SNAP/mid-SNAP/Engine-API) — the plan should note those guards are deleted *with* the checker, not migrated. Ties to RX-L10-06 (marker is the replacement detect path).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.
- **Plan edit (SHARPENS):** §6 — note the three skip-guards (post-SNAP, mid-SNAP "Bug 28", Engine-API optimistic-import) are **deleted with** the auto-shutdown, not ported; they exist only to defang the anti-pattern being removed.

---

## Group 3 — Multi-`ChainInstance` runtime & R2 isolation (the differentiator + blast-radius core)

### RX-L10-09 · Concurrent multi-`ChainInstance` runtime — hard isolation, the differentiator · Tier A (structural spine, R1+R2 converge) · owner-layer L10 `node`
- **Plan claim / disposition:** §7 primary deliverable (scope FLOOR) + §Layer boundaries — many networks in one JVM, hard-isolated: each `ChainInstance` owns its observability registry (L8), config, supervisor subtree, ports, DB handle. The enterprise single-binary multi-network runtime.
- **fukuii AS-IS:** `FukuiiRuntime` (`runtime/FukuiiRuntime.scala:24`) parses `fukuii-runtime.instances` HOCON into one `ChainInstance` per entry (`runtime/ChainInstance.scala:24`, a `BaseNode with StdMiningBuilder`), each with its own `InstanceConfig`/ActorSystem/RocksDB dir/ports — "runs multiple *whole* nodes/JVM, multi-instance, **not** one-interface-two-impls" (`clients/fukuii/node-lifecycle.md`), but leaks process-globals (RX-L10-10).
- **Reference source (byte-cited):**
  - erigon `node/direct/` — the *sole* embedded-or-remote authority (RX-L10-18); its multi-instance is one-interface-two-impls, **not** N-whole-nodes. Absence-as-evidence: no reference client runs N *isolated* whole nodes in one process (`observations/node-lifecycle.md` embedded-or-remote row — geth/besu/nethermind/reth all "monolith only"). fukuii's multi-`ChainInstance` is a genuine differentiator, not a client port.
- **Q1 appropriate decision?** Yes — this is the product thesis (`sr-phase3-fukuii-7th-client`, `fukuii-mission-strategic-context`); the seam exists AS-IS, the plan finishes the isolation.
- **Q2 what we should implement?** Yes — hard isolation is the FLOOR, not optional. The all-client read confirms there's no off-the-shelf pattern; it's fukuii's to build (correctly, this is scope-floor per `planned-work-is-scope-floor`).
- **Q3 understanding + blast radius?** Mechanism correct. Blast radius is **the entire R2 leak-set** (RX-L10-10) plus per-instance mode (RX-L10-11) and per-instance auth (RX-L10-12) — the largest blast radius in the plan; the plan states it (§7/§9).
- **Q4:** n/a.
- **Verdict:** CONFIRMS. **DoD (§8):** two concurrent `ChainInstance`s (PoW-ETC + PoS-ETH) with distinct registries/configs/ports/DB-dirs/subtrees, no cross-talk — the besu acceptance-cluster-DSL vehicle (`eye`, `warden`).

### RX-L10-10 · Full G-NL1 leak-set retirement + R2 isolation-regression grep DoD · Tier A (structural spine, R2 — blast radius) · owner-layer L10 `node` (edges into L2/L6/L8/L9)
- **Plan claim / disposition:** §2 OBSOLETE + §5 + §8 + §9 + `optimizations.md` L10 row 4 STRUCTURAL(R2) — retire the whole AS-IS leak-set, standing DoD grep (`object … { var … }` + direct `Config.` reads in per-instance code): (1) shared static metrics registry (Bug 29); (2) `object ShutdownHookBuilder` (`NodeBuilder.scala:1071`); (3) the `ioRuntime` diamond (`NodeBuilder.scala:1172`); (4) direct `object Config` reads — `loadGenesisData`/`Config.testmode`, `startPeriodicDBConsistencyCheck`/`Config.Db`, `GraphQLServiceBuilder`/`Config.config`; (5) `Metrics.configure` static; (6) `System.setProperty` network-selection mutation.
- **fukuii AS-IS:** each cited leak confirmed in `clients/fukuii/node-lifecycle.md` §Gotchas ("Process-global singletons leak into per-instance code"; "`ioRuntime` diamond needs a manual override"; "Global mutable state in network selection").
- **Reference source (byte-cited):**
  - `observations/node-lifecycle.md` — "Global mutable singletons for log/metrics — **OBSOLETE (for multi-node/embedded)** … fukuii should scope observability to the node instance, not the process"; nethermind per-module static `Metrics` + reth per-instance `RethTracer` are the cleaner references.
  - besu `SignatureAlgorithmFactory` (the *global-static curve* anti-pattern, `optimizations.md` L0 row 3) — the same class of R2 break one layer down; confirms the invariant is cross-layer.
- **Q1 appropriate decision?** Yes — OBSOLETE/STRUCTURAL(R2); each leak breaks the RX-L10-09 isolation premise. The grep-as-standing-gate is the right enforcement (memory `research-into-cohesive-plan-before-building`: mechanical ratchet, not a one-time cleanup).
- **Q2 what we should implement?** Yes — full leak-set retirement + the standing grep. Correct; no item is a false leak.
- **Q3 understanding + blast radius?** Mechanism correct, **but the retirement homes are under-specified across layers** — the crux Q3b:
  - **`ioRuntime` diamond → L6.** The two declarers are `PeerDiscoveryManagerBuilder` + `PortForwardingBuilder` (`clients/fukuii/node-lifecycle.md`: both declare the same `lazy val`) — **L6 `network` components**. The per-instance `IORuntime` must be *injected into L6* at wiring, not resolved by a hand override at L10. L6 (`requirements.md` R2 has no L6 cell) does not currently declare it receives a per-instance `IORuntime`.
  - **`GraphQLServiceBuilder`/`Config.config` → L9.** The GraphQL service is L9 (`rpc`); its config read must become an injected `InstanceConfig` at the **L9 service boundary**, not an L10 fix. L9 owns the retirement site.
  - **`System.setProperty`/`invalidateCaches()` network-swap → L8 logging.** AS-IS sets `config.file`/`config.resource` sysprops **before logging** so logback's property definers see the right file (`App.scala:85-89`). Two instances cannot each pick a logback config via a process-global sysprop — so the profile-layer replacement (RX-L10-16) must feed **per-instance logback config** (an L8 observability edge), not just per-instance HOCON.
  - **`Metrics.configure` static → L8 Bug-29** (already homed: `optimizations.md` L8 "Per-instance metric registry (Bug-29) STRUCTURAL(R2)"; `requirements.md` R2 L8 "per-inst registry") ✓ matches.
- **Q4:** the three under-homed edges above get explicit retirement sites: L6 receives a per-instance `IORuntime`; L9 owns the GraphQL `InstanceConfig` injection; L8 owns per-instance logback config. Each must be named so a lower-layer plan carries it (else it re-surfaces at integration).
- **Verdict:** SHARPENS (blast-radius edges into L6/L8/L9 are under-stated; no leak is mis-classified).
- **Plan edit (SHARPENS):** §2/§9 — annotate each G-NL1 item with its **retirement home + owning layer**: `ioRuntime` diamond → **L6** (inject per-instance `IORuntime` into `PeerDiscoveryManager`/`PortForwarding`); `GraphQLServiceBuilder` `Config.config` → **L9** (inject `InstanceConfig` at the GraphQL service boundary); `System.setProperty` logback-file sysprop → **L8** (per-instance logback config, not a process-global sysprop); `Metrics.configure` → **L8 Bug-29** (already homed). Keep the grep DoD, but add these as cross-layer coherence assertions.

### RX-L10-11 · Per-instance storage-mode selection (R8) — wire each instance's `StorageProfile` from `InstanceConfig` · Tier B (cross-layer, L2 owns selector) · owner-layer L10 `node`
- **Plan claim / disposition:** §7 + §Layer boundaries — with R2, different `ChainInstance`s in one binary may run different `StorageProfile`s; L10 wires each instance's mode from its `InstanceConfig`; **L2 owns the selector**. AS-IS: no `StorageProfile` (single hardcoded store).
- **fukuii AS-IS:** each `ChainInstance` already gets its own RocksDB dir (`FukuiiRuntime`), but one hardcoded keying/pruning scheme (`clients/fukuii/storage-persistence.md` — hash-keyed inherited from core-geth).
- **Reference source (byte-cited):**
  - **L2 plan (the owning layer):** `L2.md:66` — `storage` delivers "the **`StorageProfile` role×network selector**"; `L2.md:89` R8 cell + `L2.md:77` "`StorageProfile` are **constructor-injected per instance** — no process-global DB state, no `object … { var … }`." **L10's "wire per instance from `InstanceConfig`" matches L2's "constructor-injected per instance"** ✓ — no mismatch.
  - besu `DataStorageFormat` (`L2.md:231` §4b) — the single-enum-selects-a-coexisting-approach shape; per-instance selection is the composition-root's job (L10), the selector definition is L2's.
- **Q1 appropriate decision?** Yes — per-instance mode is the R2×R8 convergence; the L10-wires / L2-owns split is correct.
- **Q2 what we should implement?** Yes — L10 reads `InstanceConfig.storageProfile` and hands it to L2's selector. Correct; no duplication of the selector.
- **Q3 understanding + blast radius?** Mechanism correct; L10↔L2 ownership boundary is clean (L2 constructor-injects per instance, L10 supplies the value). No isolation mismatch.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (L10↔L2 isolation claims agree).

### RX-L10-12 · Per-instance auth/identity (R11) — scope L9's `Principal`/`Capability` gate per `ChainInstance` · Tier B (cross-layer, L9 owns mechanism) · owner-layer L10 `node`
- **Plan claim / disposition:** §7 + §Layer boundaries + `requirements.md` R11 L10 "per-inst auth" — the L9 `Principal`/`Capability` auth gate is scoped per `ChainInstance`; no process-global credential state; each instance's MCP-OAuth2.1 / Engine-API JWT / external-signer boundary is isolated. **L9 owns the gate mechanism; L10 scopes it.**
- **fukuii AS-IS:** no auth/capability gate at all (foundation dossier A8: zero); N/A for isolation today.
- **Reference source (byte-cited):**
  - **L9 plan (owning layer):** `L9.md:22,111` — the `Principal`/`Capability` gate "designed **now**, not per-transport"; MCP-2026 OAuth2.1, Engine-API JWT, external-signer "**converge on one per-request `Principal`**"; `requirements.md` R2 L9 "per-inst routing" + R11 L9 "auth/capability gate". **L10's "scope per instance" matches L9's per-instance gate** ✓.
- **Q1 appropriate decision?** Yes — per-instance credential isolation is mandatory under R2; the L9-owns / L10-scopes split is correct.
- **Q2 what we should implement?** Yes — L10 gives each `ChainInstance` its own auth-gate instance; no process-global credential store. Correct.
- **Q3 understanding + blast radius?** Mechanism correct; L10↔L9 boundary clean (L9 defines the gate, L10 instantiates one per subtree). DoD: a grant on one instance is invisible to another (§7). No mismatch.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (L10↔L9 isolation claims agree).

---

## Group 4 — `NetworkFamily` registry wiring (family-dispatch — forge/beacon co-review)

### RX-L10-13 · `given NetworkFamily` typeclass registry + static `EmbeddedFamilies` list, wired live · Tier A (family-dispatch, R1) · owner-layer L10 `node` (forge/beacon co-review)
- **Plan claim / disposition:** §2 B7.0.5 + §5 + `optimizations.md` L10 row 2 STRUCTURAL(R1) — a Scala 3 `given`-based `NetworkFamily` registry: reth compile-time type-safety + nethermind single-binary runtime selection (static `EmbeddedFamilies` list), without reth's one-binary-per-family or nethermind's reflection/Autofac. L10 wires the container-side; L5 `consensus-api` ships the family-selection half. Invariant: inject a family's knobs **through** the typeclass, never into shared readers.
- **fukuii AS-IS:** the `EngineId.fromMarkers` seam is BUILT but DORMANT; prod falls back to a 2-case `networkType` binary defaulting ETC (`ConsensusEngine.scala:48-62,145-148`; `observations/consensus-engines.md:25`).
- **Reference source (byte-cited):**
  - nethermind `Nethermind.Api/Extensions/INethermindPlugin.cs:11-26` — self-declaring: `Name`/`Description`/`Author` metadata, `Enabled` flag, hooks `InitTxTypesAndRlpDecoders`/`Init`/`InitNetworkProtocol`/`InitRpcModules`, `MustInitialize`, optional `IModule`; `IConsensusPlugin.cs` extends it (`ApiType`). The **runtime-openness reference** — "add a family by existing and implementing an interface, no central manifest edited." Port the *self-declaration idea*, not reflection.
  - reth `crates/ethereum/hardforks/src/hardforks/mod.rs:87-90` `ForkCondition::{Block,Timestamp,TTD,Never}` — the compile-time type-safe fork-key (the L3-owned `ForkActivation` prior art, cited here as the unified dispatch).
  - erigon `polygon/` packaging (family-ships-own-chainspecs) — the data-side reference (`observations/consensus-engines.md`).
  - **`consensus-engines.md:64-73`** — the SR B7.0.5 target verbatim: "a two-tier registry (auto-derived `given` instances + one explicit `EmbeddedFamilies` list)"; **sized to XDC-stress depth** (`L5.md:279-284`: custom domain block-header type + custom block tree + cluster-swap + new-tx-type axes; F11 ZK needs them to *exist* in the seam).
- **Q1 appropriate decision?** Yes — STRUCTURAL(R1); the `given`+`EmbeddedFamilies` synthesis is the SR-designed B7.0.5 target (memory `consult-sr-research-before-design`), already grounded, not a new gap.
- **Q2 what we should implement?** Yes — the two-tier registry, wired live. NOT reflection/Autofac (nethermind's mechanism) and NOT one-binary-per-family (reth's constraint). Correct.
- **Q3 understanding + blast radius?** Mechanism correct. **Blast radius: forge/beacon co-review (§9)** — the container is non-consensus but a wrong family selection is chain-split-class. The registry *depth* is set at L5 (sized to XDC/cluster-swap), not at L10 — L10 only *wires* the L5-defined typeclass. The plan should keep the depth-vs-wiring split explicit (L10 must not narrow the seam L5 sized).
- **Q4:** n/a.
- **Verdict:** CONFIRMS. forge/beacon co-sign the wiring.
- **Plan edit (SHARPENS):** §2/§5 — cross-reference that the typeclass *depth* is L5's (`L5.md:279-284`, sized to XDC-stress / cluster-swap / new-tx-type for F11); L10 wires it without narrowing — the wiring must not collapse the seam to PoW/PoS.

### RX-L10-14 · `EngineId.fromMarkers` positive-keyed, shipped live — no `else-means-ETC` · Tier A (chain-split-class hazard, R1) · owner-layer L10 `node` (forge/beacon co-sign)
- **Plan claim / disposition:** §5 + §6 + §9 — wire the dormant `EngineId.fromMarkers` (unknown throws, >1 throws) as the live selection path; **no `else`-means-ETC/`else`-means-ethash fallthrough** (the named trap). The "SHIP it" the L5 `consensus-api` plan carries, activated in the L10 runtime. Built-but-unshipped is the DOMINANT node-layer risk.
- **fukuii AS-IS:** `EngineId.fromMarkers` positive-keying exists but is guarded/dormant; prod defaults to the 2-case `networkType` binary (`ConsensusEngine.scala:48-62,145-148`; `observations/consensus-engines.md:25,30`).
- **Reference source (byte-cited):**
  - core-geth `GetConsensusEngineType` (positive keying Ethash/Clique/Lyra2/Unknown) — **but** `CreateConsensusEngine` layer still has the geth fallthrough (`observations/consensus-engines.md:25`) — the negative example fukuii's positive keying must beat.
  - besu **positive genesis markers** `isClique()/isQbft()/getPowAlgorithm()`, "no fallthrough; closed hand-maintained if/else dispatch" (`consensus-engines.md:25`); `BesuControllerBuilder` subclass selected from genesis (RX-L10-02).
  - reth **positive type-switch on parsed config, `panic` on unknown** (`consensus-engines.md:25`). Four clients model positive keying; geth is the negative example (absence-as-evidence: never reproduce `else-means-ETC`).
- **Q1 appropriate decision?** Yes — positive keying is the DEFAULT four clients share; the `else-means-ETC` binary is OBSOLETE (`optimizations.md` L5 OBSOLETE row).
- **Q2 what we should implement?** Yes — activate `fromMarkers` in prod (unknown throws, >1 throws). The critical Q2 point: this must be a **live cutover, not another scaffold** (memory `planned-work-is-scope-floor` + §9 "built-but-unshipped is the DOMINANT risk") — the AS-IS `EngineId` seam IS the built-but-guarded-dormant anti-pattern; L10's job is to finish it.
- **Q3 understanding + blast radius?** Mechanism correct. Blast radius: **chain-split-class** — a wrong family selection activates the wrong consensus engine (forge/beacon co-sign, §9). The dispatch feeds L5 engine selection → L4 reward/finalize → state root. The plan flags this correctly (§9 risk register).
- **Q4:** n/a.
- **Verdict:** CONFIRMS. forge/beacon co-sign; DoD bar is **GREEN and wired** (positive-keyed in prod, §8).

### RX-L10-15 · Plugin/extension SPI = container-side of B7.0.5 (`given` typeclass, not reflection/Autofac) · Tier C (OPTIONAL enterprise) · owner-layer L10 `node`
- **Plan claim / disposition:** §2 OPTIONAL(enterprise extension) + §7 — port the self-declaration idea as a Scala 3 `given`-based typeclass registry (compile-checked instances + static `ServiceLoader`-style list), not reflection/Autofac. A family registers via a `given` with zero edits to shared readers (the anti-`isEtc()` invariant). Land the container-side here; the family-*selection* half ships at L5.
- **fukuii AS-IS:** **no plugin SPI** — the only config-driven selection is the mining-protocol switch (`MiningBuilder.scala:61`) + a degenerate `VmSetup` switch (`observations/node-lifecycle.md` fukuii column; `clients/fukuii/node-lifecycle.md`).
- **Reference source (byte-cited):**
  - besu `plugin-api/…/plugin/BesuPlugin.java:23` — "Plugins are discovered … using `java.util.ServiceLoader` from jar files"; `register(ServiceManager)` (`:51`) — the JVM `ServiceLoader` SPI.
  - nethermind `INethermindPlugin.cs` (RX-L10-13) — self-declaring registry; "port the self-declaration idea, not reflection/Autofac" (`observations/node-lifecycle.md` best-practice #4). Two clients (besu ServiceLoader / nethermind self-registering) prove the seam; the four others have none (absence: not a universal need → OPTIONAL is right).
- **Q1 appropriate decision?** Yes — OPTIONAL(enterprise) is correct; only besu/nethermind ship an SPI, and fukuii's need is the enterprise extend-without-forking case, not the base node.
- **Q2 what we should implement?** The container-side `given` registry (same object as RX-L10-13's family registry). Q2 caveat (§9): **do not scaffold** the SPI without a live default — build the `given`+`EmbeddedFamilies` container with a tested default family, or it becomes another built-but-unshipped seam.
- **Q3 understanding + blast radius?** Mechanism correct — the plugin-SPI container-side *is* the B7.0.5 registry container (RX-L10-13), not a separate mechanism; the plan correctly unifies them. Blast radius: zero shared-reader edits (the anti-`isEtc()` DoD).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Group 5 — Config / `InstanceConfig`

### RX-L10-16 · Config layering CLI > env > HOCON > profile (besu `CascadingDefaultProvider`) + profile replaces sysprop network-swap · Tier B · owner-layer L10 `node`
- **Plan claim / disposition:** §2 DEFAULT(config) + §5 + `optimizations.md` L10 row 6 DEFAULT(R2) — HOCON + a shipped-profile layer beneath + an env layer above; the AS-IS network swap (mutating `config.file`/`config.resource` sysprops + `ConfigFactory.invalidateCaches()`, `App.setNetworkConfig`) is itself an R2 break (two instances can't each pick a network via a process-global sysprop) → the profile layer replaces it. core-geth's preset mapping defines what each profile resolves to.
- **fukuii AS-IS:** HOCON defaults→file→`-D` sysprop; network = whole conf file swapped by sysprop + `invalidateCaches()`; `public`/`enterprise` via more `System.setProperty` (`App.scala:69,100`; `clients/fukuii/node-lifecycle.md` §Design decisions).
- **Reference source (byte-cited):**
  - besu `app/…/cli/util/CascadingDefaultProvider.java:39-47` — iterates `List<IDefaultValueProvider>`, **first non-null wins**; precedence is the *list order*. `EnvironmentVariableDefaultProvider.java` is one provider. **Nuance:** picocli gives an explicit CLI arg precedence over *any* default provider — so "CLI wins" is picocli's inherent behavior; `CascadingDefaultProvider` only orders the **defaults** (env → TOML → profile). besu ships named `--profile` presets as the lowest layer.
  - core-geth `cmd/utils/flags.go:170,185` `ClassicFlag`/`MordorFlag`; `:1214-1218,:1695-1698` resolve datadir (`classic`/`mordor` subdir); `:1873-1874` gas-limit 8M for classic/mordor; `:1804,:1828` ethash dataset/cache dirs — the preset-flag → {chainID/genesis/datadir/gas-limit/ethash} mapping each profile must reproduce.
- **Q1 appropriate decision?** Yes — DEFAULT; the profile-layer cascade is the correct R2 fix (a process-global sysprop cannot serve N instances).
- **Q2 what we should implement?** Yes — profile beneath HOCON, env above, explicit precedence. Correct.
- **Q3 understanding + blast radius?** Mechanism mostly correct, **one nuance under-stated**: `CascadingDefaultProvider` orders **defaults only** — "CLI wins" is the *CLI framework's* job, which HOCON does **not** have. fukuii must implement CLI-wins explicitly (a `-D`/CLI-arg override layer above HOCON), it is not free. Blast radius: the sysprop-swap removal ripples to **L8 logging** (logback property-definers read `config.file` sysprop before logging — per-instance logback config needed; see RX-L10-10 Q3).
- **Q4:** n/a (SHARPENS).
- **Verdict:** SHARPENS.
- **Plan edit (SHARPENS):** §2/§5 — note that `CascadingDefaultProvider` orders **defaults only** (env > TOML > profile); the "CLI >" tier is the CLI-framework's inherent precedence, which HOCON lacks — fukuii must build the CLI/`-D`-override layer explicitly. DoD precedence test must exercise the CLI-override tier as a *fukuii-built* layer, not an assumed one.

---

## Group 6 — `cli`

### RX-L10-17 · `cli` subcommands — `enum` + `given`-dispatch; keys opaque, never logged · Tier C · owner-layer L10 `cli`
- **Plan claim / disposition:** §1 + §5 + §7 — `fukuii-cli` (`.dependsOn keystore/domain/crypto`, deliberately *not* downstream of `node`): node-key generation, key encryption, address derivation, genesis-alloc snippets; subcommands as an `enum` with a `given`-dispatched handler; keys opaque, never logged. Tests: key gen/encryption round-trip; address derivation vs go-ethereum vectors; genesis-alloc shape.
- **fukuii AS-IS:** `keytool`/`cli`/`signature-validator` are separate `main`s dispatched from `App` (`clients/fukuii/node-lifecycle.md` boot path); no `node` dependency.
- **Reference source (byte-cited):**
  - go-ethereum `cmd/geth` + address derivation = `keccak256(pubkey)[12:]` (shared L0 crypto authority; the cli only surfaces it). The address-derivation test vectors are go-ethereum's.
  - besu key-management subcommands (structural analog); the `given`-dispatch + opaque-key discipline is fukuii's idiom, not a client port.
- **Q1 appropriate decision?** Yes — cli-not-downstream-of-node is the correct DAG (a thin tool over keystore/crypto leaves); `enum`+`given` is the right Scala 3 idiom.
- **Q2 what we should implement?** Yes — the four subcommand families. Correct; no scope creep.
- **Q3 understanding + blast radius?** Mechanism correct; blast radius minimal (cli is a leaf tool, nothing depends on it). The opaque-key/never-logged discipline ties to R11 (keystore hardening, L8) but the cli only *uses* L0/L1 crypto.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Group 7 — Deferrals / product-family seams / feature-ledger boundaries

### RX-L10-18 · Embedded-or-remote component wiring (erigon `direct.*` shim vs gRPC, `*.api.addr`) · Tier C (OPTIONAL product-family, R7/R9; seam-now) · owner-layer L10 `node`
- **Plan claim / disposition:** §2 OPTIONAL(product-family) + §7 + §Layer boundaries + `optimizations.md` L10 row 8 — the one-interface-two-impls seam (erigon `direct.*` in-proc shim vs remote gRPC, `*.api.addr` topology flag) is *decided* here per component, over the interfaces L9 defined; lets pool-software/validator-software/GUI/dRPC-gateway (DRPC-GATEWAY-01)/MCP-server attach in-proc or as separate processes. F8 serve-side platform.
- **fukuii AS-IS:** **monolith only**; `FukuiiRuntime` runs whole nodes, "not erigon's one-component-two-impls" (`clients/fukuii/node-lifecycle.md` §vs observations).
- **Reference source (byte-cited):**
  - erigon `node/direct/txpool_client.go:29-32` — `var _ txpoolproto.TxpoolClient = (*TxPoolClient)(nil)`; `type TxPoolClient struct { server txpoolproto.TxpoolServer }` — the **in-proc shim implementing the gRPC-generated client interface over a direct server reference** (also `eth_backend_client.go`, `sentry_client.go`, `state_diff_client.go`). The sole authority for one-interface-two-impls.
  - erigon `cmd/utils/flags.go:463,561,569` — `txpool.api.addr`/`sentry.api.addr`/`downloader.api.addr` ("default: use value of `--private.api.addr`") — the per-component embedded↔remote flag.
- **Q1 appropriate decision?** Yes — OPTIONAL(product-family) with the seam designed now; erigon is the sole authority and the shape maps directly onto R7's dRPC seam.
- **Q2 what we should implement?** The seam (one interface per heavy subsystem), occupancy deferred per-component. Q2 caveat (§9): **do not scaffold without a live default** — the in-proc shim must be the tested default, the gRPC impl feature-flagged.
- **Q3 understanding + blast radius?** Mechanism correct (shim implements the gRPC interface over a direct server ref). Blast radius: the *interfaces* are L9's (`grpc-seam`); L10 only decides wiring per component per `ChainInstance` — the plan states the L9-defines/L10-decides split (§Layer boundaries "the dRPC/product-family lever lives here").
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L10-19 · OBSOLETE (consciously): nethermind concurrent topo-sorted step-init — the "hides undeclared-dep bugs under races" claim · Tier C · owner-layer L10 `node`
- **Plan claim / disposition:** §2 OBSOLETE + `optimizations.md` L10 row 7 — nethermind's concurrent topo-sorted step-init (`IStep` + `EthereumStepsManager`, Kahn-sorted, independent steps run in parallel) "declare-deps init that **hides undeclared-dependency bugs under races**"; use supervised phase-ordering instead. **The plan marks this OBSOLETE-consciously; RX verifies the claim against source.**
- **fukuii AS-IS:** imperative six-phase `BaseNode.start()` (no step-DAG) — already closer to the geth registration-order DEFAULT than to nethermind's concurrent init.
- **Reference source (byte-cited) — verifying the OBSOLETE claim:**
  - nethermind `Nethermind.Init/Steps/EthereumStepsManager.cs:43-52` — `InitializeAll`: `CreateAndExecuteSteps` then `await Task.WhenAny(allRequiredSteps)` loop — **all step tasks created and run concurrently**.
  - `:107-116` `CreateAndExecuteSteps` creates a `Task` per step; `:274-281` `StartExecute`: `await Task.WhenAll(dependentSteps.Select(s => s.StepTask))` **then** `await Step.Execute(...)` — each step blocks only on its **declared** dependencies (`StepWrapper.Dependencies`, `:270`), gated by a `TaskCompletionSource` (`:272`). **Claim CONFIRMED:** a step that depends on another *without declaring it* runs concurrently — it passes when the undeclared dep happens to finish first, fails under a different schedule. The DAG is only as correct as the declarations, and concurrency exposes the gap as a race.
  - go-ethereum `node.go:190` sequential `for _, lifecycle := range lifecycles` — the registration-order DEFAULT (no concurrency, no hidden-dep race). fukuii's Typed guardian naturally sequences via spawn-time injection (a child cannot be injected an `ActorRef` before its dependency is spawned).
- **Q1 appropriate decision?** Yes — OBSOLETE is correct. Cold-start parallelism is low value for a once-per-run node boot; the hidden-undeclared-dep race is a real hazard the source confirms.
- **Q2 what we should implement?** Supervised phase-ordering (geth registration-order made explicit by `CoordinatedShutdown` phases + spawn-time injection), **never** concurrent step-init. Correct.
- **Q3 understanding + blast radius?** Mechanism understanding is **accurate** (source-verified above). Blast radius: none — avoiding a pattern; the Typed guardian's spawn-injection sequencing (RX-L10-01) is the structural reason fukuii doesn't need it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (the OBSOLETE-conscious verdict is source-accurate).

### RX-L10-20 · F12 auto-maintenance / F8 network-infra boundary — L10 exposes surfaces, does not own the gate · Tier C (boundary confirm) · owner-layer L10 `node`
- **Plan claim / disposition:** §Layer boundaries + `feature-ledger.md` F12/F8 — the CI auto-maintenance hooks (F12/R10 — reth `update-book-cli` + `git diff --exit-code` regenerate-and-verify gate for the generated CLI/RPC-method reference) are **setup-owned and sentinel-governed**; the CLI subcommand surface + RPC-method registry the gate documents live here (L10) and at L9, so L10 *exposes the runtime surfaces the gate documents, it does not own the gate*. F8 network-infra (serve-side) attaches via RX-L10-18's embedded-or-remote wiring.
- **fukuii AS-IS:** ships **zero** auto-maintenance (foundation dossier A8); the cli surface exists (`keytool`/`cli`) but no auto-doc gate.
- **Reference source (byte-cited):**
  - reth `update-book-cli` + go-ethereum `check_generate` (`optimizations.md` cross-cutting row: "Auto-doc `update-book-cli` + `git diff --exit-code` — DEFAULT(R10/F4)") — the regenerate-and-verify pattern; a **setup/CI** concern, not L10 runtime. (Deep verification is setup-layer's, out of L10 scope; L10 confirms only the boundary.)
  - The gate *documents* L10's cli surface (RX-L10-17) + L9's RPC-method registry — L10 owns the surface, setup owns the gate.
- **Q1 appropriate decision?** Yes — the setup-owns-gate / L10-exposes-surface split is correct; F12 is `requirements.md` R11+R6+R10+setup, sentinel-governed, not an L10 deliverable.
- **Q2 what we should implement?** At L10: only the runtime surfaces (cli subcommands, per RX-L10-17). The gate itself is setup's. Correct — no scope creep into L10.
- **Q3 understanding + blast radius?** Boundary correct; L10's obligation is that its cli surface is *machine-enumerable* so setup's `update-book-cli` analog can regenerate-and-diff it. That obligation ties L10↔setup (the gate consumes L10's surface).
- **Q4:** n/a.
- **Verdict:** CONFIRMS (boundary correctly drawn; F12 gate not owned at L10). **Not-forgotten check:** F8 addressed via RX-L10-18 (serve-side product-family wiring); F12 addressed as a boundary (setup-owned).

---

## Summary

**20 items** (RX-L10-01 … RX-L10-20), full L10 registry coverage: `optimizations.md` L10 rows 1–8, `L10.md` §2 SR verdicts + §4 besu mirror + §5 idioms + §6 AS-IS gaps + §7 deferrals + §8 DoD + §9 risks, `requirements.md` R1/R2/R5/R7/R8/R9/R11 L10 cells, `feature-ledger.md` F8/F12.

- **CORRECTS (1):** RX-L10-07 — cross-layer isolation-mechanism mismatch (L10 §5 folds the Engine-API secondary `ActorSystem` into the guardian as a "child subtree"; L9 §5/§9 explicitly *retains* the dedicated `ActorSystem`+`IORuntime`, `L9.md:141-142`). Fix: keep it isolated, sequence its shutdown via a registered `CoordinatedShutdown` phase task, not by nesting.
- **SHARPENS (6):** RX-L10-03 (SCC-figure VERIFY caveat), RX-L10-04 (single-shot FSM needs a guarded reset for testmode/ephemery), RX-L10-05 (besu teardown is hand-ordered+bounded, not mechanical-reverse — reverse is geth's), RX-L10-06 (shutdown ordering: MarkStartup-after-boot, drain→Pop-marker→close, drain needs an in-flight-DB-task registry), RX-L10-10 (G-NL1 blast-radius homes: ioRuntime→L6, GraphQL Config→L9, logback sysprop→L8), RX-L10-16 (`CascadingDefaultProvider` orders defaults only — CLI-wins is not free in HOCON). Plus minor SHARPENS folded into RX-L10-08 (skip-guards deleted with the checker) and RX-L10-13 (registry depth is L5's).
- **CONFIRMS (13):** RX-L10-01/02/08/09/11/12/13/14/15/17/18/19/20.

No disposition should flip (every DEFAULT/STRUCTURAL/OPTIONAL/OBSOLETE is source-appropriate). The one hard finding is a **mechanism/isolation mismatch across L9↔L10**, which re-enters a Wave-3 coherence mini-check.
