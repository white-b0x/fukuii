name: CI

on:
  push:
    branches:
      - main
      - master
      - develop
      - staging
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - '.github/workflows/docs-*.yml'
      - '.github/workflows/gh-pages.yml'
  pull_request:
    branches:
      - main
      - master
      - develop
      - staging
  workflow_dispatch:

permissions:
  contents: read

# Cancel in-progress runs for the same PR/branch
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    name: Test and Build (JDK 25, Scala 3.3.8)
    # Skip pure version-bump pushes (auto-version) — the merge that caused the bump already
    # ran full CI. pull_request events are unaffected (head_commit is empty there).
    if: >-
      github.event_name != 'push' ||
      !startsWith(github.event.head_commit.message, 'chore: bump version to')
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          submodules: recursive
          fetch-depth: 0

      - name: BSL guard — zero Akka imports (report-only, burn-in)
        # Ported from .agents/skills/fukuii-dependency-audit/SKILL.md step 4. Any
        # com.typesafe.akka or com.lightbend:akka* dependency is a CRITICAL BSL 1.1
        # violation — incompatible with Fukuii's Apache 2.0 license. Grep is clean across
        # all of src/ as of this writing; this step stays report-only
        # (`continue-on-error: true`) for its first burn-in period, same discipline as
        # REPO-01's new scanners, so a future false positive in the grep itself doesn't
        # block merges before it's been proven reliable. Promote to a hard-failing step
        # (drop continue-on-error) once burn-in confirms no false positives.
        run: |
          echo "=== Akka import scan (src/) ==="
          grep -rn "import akka\." src/ --include="*.scala" && echo "::warning::Akka import found — BSL 1.1 risk, see fukuii-dependency-audit skill" || echo "✓ Zero Akka imports — BSL-clean"
          echo "=== Akka dependency scan (project/Dependencies.scala) ==="
          grep '"com.typesafe.akka"\|"com.lightbend".*akka' project/Dependencies.scala && echo "::warning::Akka dependency found in Dependencies.scala — BSL 1.1 risk" || echo "✓ No Akka deps in Dependencies.scala"
        continue-on-error: true

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: 'sbt'

      - name: Cache Coursier
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/coursier
            ~/.ivy2/cache
            ~/.sbt
          key: ${{ runner.os }}-sbt-25-${{ hashFiles('**/build.sbt', '**/build.properties', '**/Dependencies.scala', '**/plugins.sbt') }}
          restore-keys: |
            ${{ runner.os }}-sbt-25-
            ${{ runner.os }}-sbt-
      
      - name: Set up SBT
        run: |
          curl -sL "https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x2EE0EA64E40A89B84B2DF73499E82A75642AC823" | sudo gpg --dearmor -o /usr/share/keyrings/scalasbt-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/scalasbt-archive-keyring.gpg] https://repo.scala-sbt.org/scalasbt/debian all main" | sudo tee /etc/apt/sources.list.d/sbt.list
          sudo apt-get update
          sudo apt-get install sbt
      
      - name: Check formatting
        run: sbt scalafmtCheckAll
        env:
          FUKUII_DEV: true

      - name: Compile all modules
        run: sbt compile-all
        env:
          FUKUII_DEV: true

      # scalafix lint gate (semantic — needs the semanticdb from the compile step above).
      # Enforces .scalafix.conf: DisableSyntax (noNulls/noAsInstanceOf/noIsInstanceOf/noReturns/
      # noFinalize), OrganizeImports, ExplicitResultTypes, RedundantSyntax, RemoveUnused,
      # NoValInForComprehension. scalafmtCheckAll (above) covers formatting; this covers lint.
      - name: Check scalafix lint
        run: sbt "scalafixAll --check"
        env:
          FUKUII_DEV: true

      - name: Run Essential Tests (Tier 1)
        run: |
          echo "=========================================="
          echo "Running Essential Tests (< 15 minutes)"
          echo "=========================================="
          set -o pipefail
          sbt -v testEssential 2>&1 | tee /tmp/sbt-testEssential.log || {
            echo "sbt testEssential failed — dumping 'last test'"
            sbt 'last test' 2>&1 | tee /tmp/sbt-last-test-essential.log || true
            exit 1
          }
        env:
          FUKUII_DEV: true
        timeout-minutes: 15
      
      - name: Run Standard Tests with Coverage (Tier 2)
        # Tier 2 runs `clean; coverage; testEssential; testStandard; coverageReport`
        # — a full coverage-instrumented recompile plus a re-run of BOTH test tiers in
        # one sbt session. It takes ~45 min and has been timing out (jobs hit 1h5m and
        # failed at the 45-min step cap), making it a permanently-red gate that blocks
        # every merge. Skip it on the staging fast-iteration branch (pushes to staging
        # and PRs targeting staging) and rely there on the fast gates that already run:
        # Tier 1 Essential, KPI baselines, and the deterministic consensus Gating
        # Integration suite (EVM compliance / ethereum-tests / ECIP-1017). Full Tier 2
        # with coverage still runs on develop and on the release gate (PRs into main),
        # so the heavy suite + coverage are enforced at the main boundary.
        if: github.base_ref != 'staging' && github.ref != 'refs/heads/staging'
        run: |
          echo "=========================================="
          echo "Running Standard Tests with Coverage (coverage-instrumented double-run; ~45-60 min)"
          echo "=========================================="
          set -o pipefail
          # Run tests with coverage in a single sbt session
          # Use 'clean' to force recompilation with coverage instrumentation
          # since Tier 1 compiled classes without instrumentation
          # Include both testEssential and testStandard for comprehensive coverage
          sbt -v "clean; coverage; testEssential; testStandard; coverageReport; coverageAggregate" 2>&1 | tee /tmp/sbt-testStandard.log || {
            echo "sbt testStandard failed — dumping 'last test'"
            sbt 'last test' 2>&1 | tee /tmp/sbt-last-test-standard.log || true
            exit 1
          }
        env:
          FUKUII_DEV: true
        # Coverage-instrumented double-run (testEssential + testStandard). At 0.7.0 (#1326)
        # this took 42.7 min — already at the edge of the old 45-min cap — and the suite has
        # since grown past it, so PRs into main failed at the cap with all tests PASSING (pure
        # wall-clock, not a correctness failure). Raised to 90 min (~2x the historical run) to
        # absorb suite growth + coverage/runner variance with headroom. Follow-up: split the
        # coverage run or shard Tier 2 to bring it back down.
        timeout-minutes: 90
      
      - name: Validate KPI Baselines
        run: |
          echo "=========================================="
          echo "Validating KPI Baselines"
          echo "=========================================="
          echo ""
          set -o pipefail
          sbt -v "testOnly *KPIBaselinesSpec" 2>&1 | tee /tmp/sbt-kpi.log || {
            echo "KPI baseline validation failed"
            sbt 'last test' 2>&1 | tee /tmp/sbt-last-test-kpi.log || true
            exit 1
          }
          echo ""
          echo "✅ KPI baselines validated successfully"
        env:
          FUKUII_DEV: true
      
      - name: Run Gating Integration Tests (deterministic subset)
        run: |
          echo "Running the deterministic, gate-able slice of the it/ suite..."
          # IMPORTANT: enumerate specs explicitly. Only fast, fully-deterministic IT specs belong in
          # the PR gate. The real-socket networking specs (E2EHandshakeSpec, E2ESyncSpec,
          # RegularSyncItSpec, FastSyncItSpec, …) bind TCP ports and depend on RLPx/handshake timing
          # that flakes on shared CI runners — they stay in the nightly only (nightly.yml
          # `IntegrationTest / testOnly`), never in this gate.
          #
          #   ethtest:    SimpleEthereumTest, BlockchainTestsSpec, VMTestsSpec  (EVM compliance)
          #   txExecTest: ForksTest, ContractTest, ECIP1017Test                 (block exec vs fixtures)
          #
          # SlowTest is NOT excluded here: ForksTest/VMTestsSpec are tagged SlowTest but each runs in
          # well under a minute, and excluding it would skip exactly the specs this gate exists to guard.
          sbt "IntegrationTest / testOnly \
            com.chipprbots.ethereum.ethtest.SimpleEthereumTest \
            com.chipprbots.ethereum.ethtest.BlockchainTestsSpec \
            com.chipprbots.ethereum.ethtest.VMTestsSpec \
            com.chipprbots.ethereum.txExecTest.ForksTest \
            com.chipprbots.ethereum.txExecTest.ContractTest \
            com.chipprbots.ethereum.txExecTest.ECIP1017Test"

          echo ""
          echo "=== Gating Integration Results ==="
          echo "EVM compliance (ethereum/tests) + block-execution fixtures (txExecTest) validated."
          echo "Real-socket networking IT specs run in the nightly only — see nightly.yml."
          echo "See test artifacts for detailed results"
        env:
          FUKUII_DEV: true
        timeout-minutes: 15
      
      - name: Upload coverage reports to Codecov
        if: always()
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          # Specify the coverage file locations for scoverage
          files: "**/target/scala-*/scoverage-report/scoverage.xml"
          directory: .
          flags: unittests
          name: codecov-fukuii
          fail_ci_if_error: false
          verbose: true
      
      - name: Upload coverage artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-reports-jdk25-scala-3.3.8
          path: |
            **/target/scala-*/scoverage-report/**
            **/target/scala-*/scoverage-data/**
          retention-days: 30
          if-no-files-found: warn
      
      - name: Build assembly
        run: sbt assembly
        env:
          FUKUII_DEV: true
      
      - name: Build distribution
        run: sbt dist
        env:
          FUKUII_DEV: true
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: test-results-jdk25-scala-3.3.8
          path: |
            **/target/test-reports/**
            **/target/scala-*/test-classes/**/*.log
            /tmp/sbt-*.log
          retention-days: 7
          if-no-files-found: warn
      
      - name: Upload ethereum/tests integration results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: ethereum-tests-results-jdk25-scala-3.3.8
          path: |
            **/target/scala-*/it-classes/**/*.log
            /tmp/fukuii-it-test/**/*.log
          retention-days: 7
          if-no-files-found: warn
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v6
        with:
          name: fukuii-distribution-jdk25-scala-3.3.8
          path: |
            target/universal/*.zip
            **/target/scala-*/*-assembly-*.jar
          retention-days: 7
          if-no-files-found: warn
