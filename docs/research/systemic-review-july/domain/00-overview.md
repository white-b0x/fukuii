# domain — Overview (placeholder)

Populated when `SR-07` (`.claude/sprints/QUEUE.md`, "Systemic Review" section) is dispatched.
Tier B (flagged as the most likely Tier B→A promotion candidate — high blast-radius core
types, opaque-type layer). Ready now.
