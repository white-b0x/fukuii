# RX — L2 `storage` + `trie` per-item reference-client verification

_The depth pass for the storage layer. Each item the L2 plan (`plan/L2.md` §1–§9 + `optimizations.md`
L2 rows + `requirements.md` R1/R2/R4/R5/R7/R8 cells + `feature-ledger.md` F8/F9/F11) commits to
implement, verified against the **actual vendored reference-client source** under
`.claude/repo-references/clients/{besu,core-geth,erigon,go-ethereum,nethermind,reth}/`. Method: `rx/README.md`._

**Every entry answers the operator's four questions before its verdict:**
1. **Q1 — appropriate decision?** is the plan's disposition (DEFAULT/STRUCTURAL/OPTIONAL/OBSOLETE) the right call given what the source actually shows?
2. **Q2 — what we should implement?** the right thing to build, vs a better alternative the multi-client read reveals, vs NOTHING (YAGNI / seam suffices)?
3. **Q3 — mechanism + blast radius correct?** (a) how it actually works (source is the check); (b) blast radius — every layer/item/consensus-path it ripples into.
4. **Q4 — if any "no": the correct answer + why** (source-grounded).

Verdict: **CONFIRMS** (all four yes) / **CORRECTS** (Q1/Q2/Q3 no → Q4 fixes) / **SHARPENS** (right decision, Q3 reveals a load-bearing mechanism detail or an under-stated blast-radius edge to fold in).

**Storage-authority caveat (README §"absence is evidence"):** storage tuning (RocksDB CF/cache/compaction,
freezer, scheme-indirection) is a **go-ethereum/besu/erigon/nethermind engineering concern ETC and ETH
share** — core-geth is NOT storage-authoritative; it is authoritative here **only** for (a) confirming ETC
introduces zero state-structure divergence and (b) *what an ETC cold store retains* (total difficulty). The
**state-root/MPT hashing** the storage layer serves IS byte-critical (node RLP, `<32` inline, HP, keccak
key) — those items are flagged **"forge co-sign at build"**; RX confirms only that the plan's *understanding*
is source-accurate now, the byte-exact build gate is forge's (state-root litmus, `consensus-change-protocol.md`).

**Path-existence pre-check (every cited path verified present in the vendored tree unless noted):** besu
`plugin-api/…/storage/{DataStorageFormat,SegmentIdentifier,WorldStateKeyValueStorage}.java`,
`ethereum/core/…/keyvalue/KeyValueSegmentIdentifier.java`, `ethereum/trie/…/{MerkleTrie,NodeLoader,NodeUpdater,CompactEncoding,patricia/StoredMerklePatriciaTrie}.java`,
`…/pathbased/bonsai/storage/flat/{BonsaiFlatDbStrategy,BonsaiFullFlatDbStrategy}.java`,
`plugin-api/…/trielogs/TrieLog.java` ✓; nethermind `Nethermind.Trie/{INodeStorage,NodeStorage}.cs`,
`Nethermind.Db/{ITunableDb,FullPruning/FullPruningDb}.cs`, `Nethermind.Trie/Pruning/{IPruningStrategy,IPersistenceStrategy}.cs` ✓;
go-ethereum `trie/{node,encoding,stacktrie,proof,secure_trie,hasher}.go`, `core/rawdb/{schema,freezer}.go`,
`triedb/generate.go`, `internal/era/` ✓; core-geth `core/rawdb/{ancient_scheme,chain_freezer}.go` ✓;
erigon `db/kv/mdbx/`, `db/state/domain.go` ✓; reth `crates/static-file/`, `crates/trie/sparse/`, `crates/trie/trie/src/witness.rs` ✓.
**One citation correction surfaced (RX-L2-06):** the plan attributes `triedb/generate.go` / `GenerateTrie`
(16-partition parallel rebuild) to **reth** in §5/§6/§7 + `optimizations.md` L2 row 3 + `feature-ledger.md` F9
— it is **go-ethereum's** (`.go` file, Rust reth has no `GenerateTrie`; grep-confirmed absent). reth
contributes the `HashBuilder`/streaming-cursor substrate, not `GenerateTrie`.

---

## Group 1 — Node schema / MPT / state-root (consensus, forge-gated)

### RX-L2-01 · `enum MptNode` (5 node types) + node RLP encoding — state-root byte-exact · Tier A (consensus) · owner-layer L2 `trie`
- **Plan claim / disposition:** §5 idiom target + §6 row 14 + §8 DoD — replace the `sealed abstract class MptNode` + subclass hierarchy with `enum MptNode { Leaf, Extension, Branch, Hash, Null }`; a total `match` retires the `july-fourth` `@unchecked` suppressions. The node RLP encoding (17-item list = branch; 2-item = leaf/ext by HP flag; `<32` value = inline) is DEFAULT-**not-negotiable** consensus, geth/core-geth byte-identical.
- **fukuii `july-fourth`:** `Node.scala:51-117` — `sealed abstract class MptNode` with 5 subtypes (`LeafNode`/`ExtensionNode`/`BranchNode(children:Array[MptNode](16), terminator)`/`HashNode`/`NullNode`); `@unchecked` matches in `MerklePatriciaTrie.scala:336-397`; `MptTraversals.decodeNode` reconstructs from RLP.
- **Reference source (byte-cited):**
  - go-ethereum `trie/node.go:39-96` — `fullNode struct` (16 children + value), `shortNode struct` (key+val), `hashNode []byte`, `valueNode` — the identical 4-shape (+ null) node model; `decodeNode` at `:193` reconstructs `&shortNode{key, valueNode(val), flag}` from a 2-item list. The exact camp fukuii sits in.
  - besu `ethereum/trie/…/patricia/` — `BranchNode`/`ExtensionNode`/`LeafNode`/`NullNode` via `StoredNodeFactory` RLP decode; the JVM structural mirror (fukuii's Visitor layer cites it).
  - core-geth — geth-identical node encoding (ETC introduces zero state-structure divergence; `clients/fukuii/state-trie.md` Authority note).
- **Q1 (appropriate?):** Yes — `enum` over the closed 5-shape node set is the correct Scala-3 idiom; the shape is fixed by consensus (geth `node.go` has exactly these), so a closed enum is faithful, not a lossy simplification.
- **Q2 (what to implement?):** The `enum` + total `match`. Correct — the `@unchecked` retirement is a real compiler-safety win with zero consensus-semantic change (the node set cannot grow without a hard fork).
- **Q3 (mechanism + blast radius?):** Mechanism correct (5 shapes, RLP list-arity dispatch = geth). Blast radius: the node RLP bytes ARE the state-root preimage → **forge-gated**; ripples to L4 world-state (reads node bytes), L7 SNAP (serialized node transport), and the cross-backend parity vector (§8). The enum's `encode`/`hash` lazy-vals must reproduce the `july-fourth` byte-for-byte — an encoding change here is a chain split.
- **Q4:** n/a (CONFIRMS).
- **Verdict:** CONFIRMS. forge co-sign at build (node encoding is the state-root preimage).

### RX-L2-02 · `<32` inline threshold (`NodeCapper`) + HP nibble compaction + keccak "secure trie" key · Tier A (consensus) · owner-layer L2 `trie`
- **Plan claim / disposition:** §2 DEFAULT-not-negotiable + §9 consensus flags — the `<32`-byte inlining threshold, HP (hex-prefix) nibble compaction, and keccak-key hashing (folded into the key serializer, not a `SecureTrie` wrapper class) are consensus-load-bearing and must stay geth/core-geth byte-identical.
- **fukuii `july-fourth`:** `MptNode.MaxEncodedNodeLength = 32` (`Node.scala:45`); `HexPrefix.scala:14` HP compaction; keccak key via `Address.hashedAddressEncoder` + `HashByteArraySerializable` (`domain/package.scala:28`) — a serializer-composition "secure trie", not a wrapper.
- **Reference source (byte-cited):**
  - go-ethereum `trie/encoding.go:37-46` — `hexToCompact`: `terminator := byte(0)`; `buf[0] = terminator << 5` (leaf-flag bit 5); `buf[0] |= 1 << 4` (odd-length flag bit 4) — the exact HP flag-byte packing fukuii's `HexPrefix` must reproduce.
  - go-ethereum `trie/secure_trie.go` (a.k.a. `StateTrie`) — geth wraps keccak-key hashing in a distinct type; fukuii folds it into the serializer (behaviorally identical, byte-for-byte).
  - besu `ethereum/trie/…/CompactEncoding.java` — the JVM HP reference; corroborates the flag-byte layout.
- **Q1 (appropriate?):** Yes — treating these as DEFAULT-not-negotiable (locked, finalized spec) is exactly right; the state root is a one-way door.
- **Q2 (what to implement?):** Verbatim byte-parity, no redesign. The serializer-folded secure-trie (vs geth's wrapper class) is an acceptable structural choice **provided** the hashed-key bytes are identical — which they are. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct (flag-byte bits 4/5; 32-byte inline cutoff). Blast radius: forge-gated; the `<32` cutoff and HP packing feed every node hash → the state root; touches L4 (storage-root splice), L6 (not directly — but a wrong root partitions peers). The serializer-fold means the keccak-key logic lives at the L1 `domain` opaque-type boundary (`hashedAddressEncoder`), not in `trie` — a blast-radius edge the plan should keep visible so the "secure trie" isn't searched for as a class.
- **Q4:** n/a (Q1/Q2 yes; Q3 mechanism yes with one placement detail to fold).
- **Verdict:** SHARPENS. forge co-sign.
- **Plan edit (SHARPENS):** §5/§8 — note the keccak-key "secure trie" lives as **serializer composition at the L1 `domain` key-encoder** (`hashedAddressEncoder`/`HashByteArraySerializable`), not a `trie`-layer wrapper class; the DoD "secure-trie known roots" vector exercises the L1 encoder, not a `trie` type.

### RX-L2-03 · trie↔persistence decoupling — `NodeLoader`/`NodeUpdater`/`MerkleTrie` seam (the DEFAULT structural move) · Tier A (structural spine) · owner-layer L2 (`trie` owns the typed contract, `storage` byte-pure)
- **Plan claim / disposition:** §2 DEFAULT (highest-value structural move) + §4a besu mirror + §5 — express besu's `NodeLoader`/`NodeUpdater` function-pair as the **L2 module edge**: `storage` provides bytes keyed by `(location, hash)`; `trie` provides the typed `MptStorage`/`NodeLoader`/`NodeUpdater` on top. Keep the immutable functional `MerklePatriciaTrie` (Mantis lineage → the geth-`NodeSet` decoupling: `(root, mutations) → (newRoot, dirtyNodes)`).
- **fukuii `july-fourth`:** `MptStorage.scala:11` + `collapseTrie` (`MptTraversals.scala:16`) already give the decoupling — but "*less fully factored than besu's `MerkleTrie`/`WorldUpdater` stack (the seam is split across two mutually-cyclic packages)*" (`clients/fukuii/state-trie.md` §Notable patterns).
- **Reference source (byte-cited):**
  - besu `ethereum/trie/…/NodeLoader.java:23` — `Optional<Bytes> getNode(Bytes location, Bytes32 hash)` (read half); `NodeUpdater.java:21` — `void store(Bytes location, Bytes32 hash, Bytes value)` (write half). **Both carry `(location, hash)`** — the exact abstraction that lets besu key by *either* hash (Forest) *or* path (Bonsai) behind one pair.
  - besu `ethereum/trie/…/MerkleTrie.java:43,68,93,108,115,59,133,143,147` — `get`/`put`/`remove`/`getRootHash`/`commit(NodeUpdater)`/`getValueWithProof`/`entriesFrom`/`visitAll`/`visitLeafs` — one interface, storage policy is a constructor arg (`StoredMerklePatriciaTrie` vs `SimpleMerklePatriciaTrie`). Exactly as §4a states.
  - go-ethereum pure-`Trie` → `NodeSet` + `triedb` backend — the Go form of the same seam.
- **Q1 (appropriate?):** Yes — DEFAULT and it is the single precondition for every optional mode (StorageProfile, INodeStorage, flat, freezer all sit on this seam). besu proves it byte-for-byte.
- **Q2 (what to implement?):** The module-edge realization (`storage` byte-pure, `trie` owns node contract) rather than besu's in-package factoring — this is *stronger* than besu (a compile-enforced DAG edge, not a convention). Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct — and the `(location, hash)` shape is the load-bearing detail (RX-L2-08 depends on it). Blast radius: this edge IS the db↔mpt cycle resolution (RX-L2-04); it ripples to every consumer of node bytes (L4/L7). The plan states it (§1 DAG, §6 db↔mpt).
- **Q4:** n/a (CONFIRMS).
- **Verdict:** CONFIRMS.

### RX-L2-04 · db↔mpt 2-cycle resolution via byte-pure `storage` (target-arch OPEN #2) · Tier A (structural spine) · owner-layer L2
- **Plan claim / disposition:** §1 + §6 db↔mpt row + §6 closing + §9 structural-risk — `storage` stores only `(Location, NodeHash) → NodeEncoded` and knows nothing about `MptNode`; `trie` owns the node-shape contract (encode/decode, `collapseTrie`, child-hash extraction) and depends *down* on `storage`. Node contract has exactly one owner (`trie`), so no `storage-api` module needed (option b is the fallback). An upward edge is a compile error.
- **fukuii `july-fourth`:** `db ↔ mpt` 2-cycle — `mpt` imports `db.storage.MptStorage` (9 edges), `db` imports `mpt` node types (7 edges); `MptStorage` (resolver) defined in `db` while `NodesKeyValueStorage` (its KV half) defined in `mpt` (`state-trie.md` §Gotchas; `storage-persistence.md` §Gotchas).
- **Reference source (byte-cited):**
  - besu `NodeLoader`/`NodeUpdater` `(location, hash)` bytes seam (RX-L2-03) — proves `storage` needs no node-type awareness: it moves bytes, `trie` owns shape. besu's `StoredNodeFactory` (node RLP decode) lives in the trie package, not storage.
  - go-ethereum `triedb` backend stores `[]byte` blobs keyed by hash/path; the `trie` package owns `node.go` decode — the same directional split.
- **Q1 (appropriate?):** Yes — the byte-pure realization is strictly cleaner than either OPEN #2 option (node-types-in-domain, or a `storage-api` module); a compile-enforced single edge cannot re-form the cycle.
- **Q2 (what to implement?):** byte-pure `storage`, `storage-api` only as a documented fallback. Correct — node-aware refcount GC lives in `trie` handing `storage` explicit child-hash lists, so `storage` never needs a parsed node.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: the whole L2 module boundary rests on this; if the TrieLog `{prior,updated}` diff builder or refcount child-hash propagation leaks into `storage`, the cycle re-forms (§9 states the grep: `storage` imports no `…fukuii.trie.*`). The one build-time sub-decision (§6 closing) is correctly flagged.
- **Q4:** n/a (CONFIRMS).
- **Verdict:** CONFIRMS.

### RX-L2-05 · `getProof` EIP-1186 inclusion + non-inclusion · Tier B · owner-layer L2 `trie`
- **Plan claim / disposition:** §1 (`trie` delivers `getProof` EIP-1186) + §8 DoD (inclusion **and** non-inclusion; walked-path terminal proves absence, root always included) + §3 authority (go-ethereum `trie/proof.go`; besu `ProofVisitor`).
- **fukuii `july-fourth`:** `MerklePatriciaTrie.scala:134` `getProof` — inclusion + non-inclusion via `proofMode` on `pathTraverse` (`:170`); comment cites `Besu ProofVisitor.java:64` (`state-trie.md`).
- **Reference source (byte-cited):**
  - go-ethereum `trie/proof.go` — `Prove` (collects nodes on the path to a key) + `VerifyRangeProof` (SNAP range). The byte-authority.
  - besu `ethereum/trie/…/ProofVisitor.java` / `MerkleTrie.getValueWithProof:59` — the JVM `Proof<V>` shape fukuii's code cites directly.
  - reth `crates/trie/trie/src/witness.rs` + `crates/trie/sparse/` — `SparseStateTrie`/`TrieWitness` (stateless; the OPTIONAL(light/custody) extension, deferred — see §7).
- **Q1 (appropriate?):** Yes — EIP-1186 point proofs are a DEFAULT capability; fukuii already has it and it matches the authority.
- **Q2 (what to implement?):** Keep the `july-fourth` shape (inclusion+non-inclusion); the sparse-trie/witness extension is correctly deferred to OPTIONAL. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct (terminal-node absence proof). Blast radius: feeds L9 RPC (`eth_getProof`) and L7 SNAP range-proof verify (`StackTrie`/`ProofTrieInserter`); the proof bytes are consensus-adjacent (must verify against the state root) → light forge interest at build, but the algorithm is EIP-locked.
- **Q4:** n/a (CONFIRMS).
- **Verdict:** CONFIRMS.

### RX-L2-06 · Streaming `StackTrie` + `HashBuilder` substrate + `ShardEnumerator` 16-partition parallel rebuild · Tier A (STRUCTURAL, seam-now; OPTIONAL(validator/perf) occupancy) · owner-layer L2 `trie`
- **Plan claim / disposition:** §5 gap #5 + §6 row 5 + §7 + `optimizations.md` L2 row 3 — keep the mutable `StackTrie` (geth port), wire it to a **"reth-`GenerateTrie` 16-partition parallel rebuild"**; `ShardEnumerator` (16 shards, first-nibble) "already aligns with **reth** `triedb/generate.go` `GenerateTrie`". Enables parallel/flat state-root + the snap/2 completion path.
- **fukuii `july-fourth`:** `StackTrie.scala:38` (geth `trie/stacktrie.go` port, mutable, sorted-insert, write-once root, SNAP-only); `ShardEnumerator.scala:35` (≤16 first-nibble shards, unconnected to a parallel rebuild) (`state-trie.md`).
- **Reference source (byte-cited):**
  - go-ethereum `triedb/generate.go` — **`GenerateTrie` is go-ethereum's** (`.go`); `trie/stacktrie_partial_test.go:52,112` — `partitionRoot`/`PartialStackTrie` builds "partition n" subtrees, byte-identical to the `[n]`-subtree of the full trie — the 16-partition (first-nibble) parallel rebuild backbone.
  - reth `crates/trie/` — `TrieCursor`/`HashedCursor` + `HashBuilder` + prefix-set (parallel `state_root_task`); reth's streaming/flat commitment substrate. **reth has NO `GenerateTrie`** (grep-confirmed absent — it is not a Rust symbol).
- **Q1 (appropriate?):** Yes — STRUCTURAL seam-now / OPTIONAL(validator/perf) occupancy is correct: the streaming substrate is the enabler for parallel state-root and snap/2, retrofitting it later behind the resident-MPT-only path is a rewrite.
- **Q2 (what to implement?):** The `StackTrie`+`ShardEnumerator` connection to a `GenerateTrie`-equivalent + a reth-`HashBuilder`-shaped cursor. Correct — but the plan **mis-attributes `GenerateTrie` to reth**; the two ideas are distinct clients (geth's 16-partition `GenerateTrie` + reth's `HashBuilder`/prefix-set cursor).
- **Q3 (mechanism + blast radius?):** Mechanism correct (16 first-nibble partitions; `PartialStackTrie` subtree = full-trie subtree byte-identical). Blast radius: the streaming root must produce the **same state root** as the resident MPT (§8 cross-backend parity, forge-gated); feeds L7 (snap/2 completion, parallel pivot rebuild). Under-stated: the parallel rebuild's per-partition roots must recombine to the canonical root — a forge-gated vector the plan lists but attributes to the wrong client.
- **Q4 (correct answer):** The 16-partition parallel rebuild is **go-ethereum `triedb/generate.go` `GenerateTrie`** (Rust reth has no such symbol); **reth** contributes the `HashBuilder` + `TrieCursor`/`HashedCursor` streaming substrate. The plan conflates the two. Fix the attribution wherever "reth-`GenerateTrie`" / "reth `triedb/generate.go`" appears.
- **Verdict:** CORRECTS (citation/attribution). forge co-sign (streaming root == resident root is a state-root vector).
- **Plan edit (CORRECTS):** L2.md §5 gap #5, §6 row 5, §7 row "Streaming cursor + `HashBuilder`", `optimizations.md` L2 row 3, and `feature-ledger.md` F9 — replace "reth-`GenerateTrie`" / "reth `triedb/generate.go` `GenerateTrie`" with **go-ethereum `triedb/generate.go` `GenerateTrie` (16-partition)**; reth remains authority for the `HashBuilder`/streaming-cursor substrate only. (§3 authority table is already correct — it lists `triedb/generate.go` under go-ethereum and `HashBuilder` under reth; the prose sections drifted from it.)

---

## Group 2 — `StorageProfile` / node-keying (the structural spine, R8)

### RX-L2-07 · `StorageProfile` role×network selector (besu `DataStorageFormat` shape) · Tier A (STRUCTURAL R8) · owner-layer L2 `storage`
- **Plan claim / disposition:** the doctrine block + §5 gap #1 + §6 row 1 + §7 + `optimizations.md` L2 row 1 — a `StorageProfile` record composing **six** orthogonal axes `{keying × pruning × flat × freezer × expiry × engine}`, resolved once per `ChainInstance` at open, recorded in the schema marker, threaded downward. besu `DataStorageFormat` is the structural proof the selector belongs at the interface. STRUCTURAL(R8): multi-approach from line one, not one hardcoded store.
- **fukuii `july-fourth`:** one general node store, hash-keyed only; `PruningMode` is a 3-value enum over one `NodeNamespace` CF; no multi-approach seam (`storage-persistence.md` §Pruning mode).
- **Reference source (byte-cited):**
  - besu `plugin-api/…/storage/DataStorageFormat.java:18-24` — `enum { FOREST, BONSAI, X_BONSAI_ARCHIVE }` + `isBonsaiFormat()`. A **single 3-value enum** selecting an entire coexisting storage philosophy, with separate `WorldStateKeyValueStorage` hierarchies (`ForestWorldStateKeyValueStorage` vs `BonsaiWorldStateKeyValueStorage`, both present).
  - besu `ethereum/core/…/keyvalue/KeyValueSegmentIdentifier.java:29-41` — `WORLD_STATE(id=2, EnumSet.of(FOREST))`, `ACCOUNT_INFO_STATE(id=6, EnumSet.of(BONSAI, X_BONSAI_ARCHIVE))`, `TRIE_LOG_STORAGE(id=10, BONSAI…, static=true, GC=true)` — **the CF *set* is format-gated** (`includeInDatabaseFormat`), not fixed.
  - go-ethereum `core/rawdb/schema.go:46-47` — `persistentStateIDKey` ("path-based only") — geth records the scheme choice as a distinct marker; the scheme is a per-datadir decision, corroborating "resolve once, record it."
  - nethermind Hash/HalfPath + online-full-pruning; erigon flat-state/Domains/MDBX; reth pathdb/static-files — the full menu the plan catalogues; each ships ≥2 approaches.
- **Q1 (appropriate?):** Yes — STRUCTURAL(R8) is correct; besu is the byte-proof that ≥2 approaches coexist behind one interface, and retrofitting a second backend behind a hash-only design is a rewrite (`feature-ledger.md` R8, [[planned-work-is-scope-floor]]).
- **Q2 (what to implement?):** The selector-at-the-interface, yes. **But the six-axis shape needs one refinement:** no reference client has a 6-axis *product*; each has a small enum + separate flags (besu 3-value format + per-CF flags; geth scheme + snapshot/freezer bools). The `engine` axis has **exactly one live inhabitant** (RocksDB; MDBX = OBSOLETE, §7 "does not offer them") — so it is a *reserved seam-placeholder*, not a live selector axis. Building it as a live 6th axis implies a variability that cannot exist. Five live axes + one reserved is the honest shape.
- **Q3 (mechanism + blast radius?):** Mechanism correct (resolve-once, record, thread). **Blast radius is large and mostly stated** — but one edge is under-stated: because besu's `KeyValueSegmentIdentifier.includeInDatabaseFormat` gates the **CF set** on the format (Forest opens `WORLD_STATE`; Bonsai opens `ACCOUNT_INFO_STATE`/`TRIE_BRANCH`/`TRIE_LOG` instead), the resolved `StorageProfile` determines **which CFs the `enum Namespace` opens**, not just their tuning. The open path must open a profile-dependent CF subset, and the `SchemaMarker` (RX-L2-22) must reject a datadir whose CF set doesn't match the profile — not only a `(format,version)` mismatch. Ripples the plan *does* state: L4 world-state, L7 SNAP serve/receive, L9 prune-barrier registry (§"Layer boundaries").
- **Q4 (correct answer):** `engine` is a **reserved axis with one inhabitant** (RocksDB), documented as a future seam, not a live selector — otherwise it reads as offering a choice that doesn't exist. And the profile drives the **CF set** (format-gating), which the SchemaMarker must enforce. Source: besu `KeyValueSegmentIdentifier:29-41` format-gating + `DataStorageFormat` being a single enum (not a product).
- **Verdict:** SHARPENS. forge co-sign the keying axis (state-root-affecting).
- **Plan edit (SHARPENS):** (1) §5 gap #1 — annotate `engine: KvEngine` as a **reserved single-inhabitant seam** (RocksDB; MDBX OBSOLETE), i.e. **5 live axes + 1 reserved**, so it is not read as a live selector. (2) doctrine block / §5 / RX-L2-22 — add that the resolved profile determines the **opened CF set** (besu `includeInDatabaseFormat` format-gating), and the `SchemaMarker` gate covers CF-set-vs-profile, not only `(format,version,keying)`.

### RX-L2-08 · `INodeStorage` scheme-indirection seam (hash **and** path, mutable `Scheme` + dual-read) · Tier A (STRUCTURAL seam, R8) · owner-layer L2 (`MptStorage` boundary)
- **Plan claim / disposition:** doctrine block + §4a + §5 gap #4 + §6 row 4 + §7 + `optimizations.md` L2 row 2 — a mutable `Scheme (Hash | HalfPath)` at the `MptStorage` boundary with a **dual-read fallback**, realized as the `(location, hash)`-keyed store (besu shape): hash profile ignores `location`, path profile ignores `hash`. archival=hash / pruned=path as a role choice **and** an online Hash→HalfPath migration. Retires the one-way-locked `PathNodeStorage` SNAP island.
- **fukuii `july-fourth`:** `PathNodeStorage.scala:38` is a **PathScheme-only** SNAP island — two CFs, HP-nibble-path keys; Hash↔Path scheme switch on an existing DB is **forbidden**, startup-guarded in `SNAPSyncController` (`state-trie.md` §Gotchas). The general/live store is hash-keyed (`NodeStorage`).
- **Reference source (byte-cited):**
  - nethermind `Nethermind.Trie/INodeStorage.cs:14,21,37-44` — `KeyScheme Scheme { get; set; }` (**mutable**), `Get(Hash256? address, in TreePath path, in ValueHash256 keccak, …)`, `enum KeyScheme { Hash, HalfPath, Current }`. The seam shape.
  - nethermind `NodeStorage.cs:123-130` — the **directional dual-read**: `Scheme == HalfPath` → `Get(HalfPath-key) ?? Get(Hash-key)`; else → `Get(Hash-key) ?? Get(HalfPath-key)`. Also `RequirePath` (`:19,25`) — "when running completely from hash based db, some code path that calculate path can be ignored" (skip path computation under pure-hash).
  - besu `NodeLoader`/`NodeUpdater` `(location, hash)` pair (RX-L2-03) — the 2-tuple that lets one function-pair key by either scheme.
- **Q1 (appropriate?):** Yes — STRUCTURAL is correct; nethermind ships exactly this (a mutable scheme + dual-read that IS both a role choice and an online migration), and fukuii's SNAP-locked island is the gap. Making path-keying a first-class general backend matches the ref-client standard (geth pathdb-default, nethermind HalfPath-default, reth nibble-path).
- **Q2 (what to implement?):** The mutable-`Scheme` + dual-read at `MptStorage`, yes. Correct — this is the seam that makes the whole R8 doctrine tractable.
- **Q3 (mechanism + blast radius?):** **Mechanism partially under-specified.** The plan's key shape is besu's `(location, hash)` 2-tuple; nethermind's real HalfPath key is a **3-tuple `(address, path, keccak)`** — the `address` (account) distinguishes per-account storage-subtrie nodes (`NodeStorage.cs:32-35`, `GetHalfPathNodeStoragePathSpan(address, path, keccak)`; `address is null` = the account/state trie, non-null = a storage subtrie). fukuii shares the single-node-store-for-all-storage-tries design (`state-trie.md`: "the state trie and every per-contract storage trie sharing that single node store"), so a **path-keyed** profile MUST fold the account address into the key or storage-subtrie nodes at the same nibble path across different accounts collide. besu's `location` (a global nibble path from the account-trie root) sidesteps this differently; nethermind's `(address, path)` is the direct answer. The plan's `Location` opaque "over the nibble path" does not obviously carry the account dimension. Blast radius: the `Location` opaque type (L1/L2) and the `(location, hash)` seam must encode account-scope for storage subtries; forge-gated (keying axis is state-root-adjacent — same root under both schemes is §8).
- **Q4 (correct answer):** The path-keyed store key must be **account-scoped**. Decided as-built → nethermind's explicit `(owner, path)` key, not besu's global `location`: `storage/INodeStorage.scala` uses `NodeLocation(owner: Option[accountKey], path)` (`owner = None` → state trie, `Some` → that account's storage sub-trie) with the physical key `owner ++ path ++ nodeHash`. The dual-read is **directional and migration-gated**: a read tries the active scheme, and probes the other scheme **only when `migrationInProgress = true`** — steady-state (no migration in flight) is a single probe of the active scheme, geth-aligned (`ReadTrieNode`, one scheme, no fallback), explicitly **not** nethermind's unconditional `Get(x) ?? Get(y)` dual-read (a deliberate no-cost-when-not-migrating refinement); the *write* uses only the active scheme. Source: `storage/INodeStorage.scala`; nethermind `NodeStorage.cs:32-35,123-130`.
- **Verdict:** SHARPENS. forge co-sign (cross-scheme same-root is a state-root vector).
- **Plan edit (SHARPENS):** §4a/§5 gap #4 — (1) the path-keyed key is **account-scoped** via nethermind's explicit `(owner, path)` (`NodeLocation`, key `owner ++ path ++ nodeHash`, `storage/INodeStorage.scala`), not besu's global `location`, so per-account storage-subtrie nodes don't collide. (2) The dual-read is **directional and migration-gated** (`active ?? other` **only** under `migrationInProgress`, single-probe geth-aligned otherwise), and the **write** uses only the active scheme — restate "ignores" as "read probes the other scheme only during migration, writes use active." (3) note `RequirePath` (skip path computation under pure-hash) as the perf carve-out.

### RX-L2-09 · Flat-state accelerator promoted to the live account read path (adopt-on DEFAULT) · Tier B · owner-layer L2
- **Plan claim / disposition:** §2 DEFAULT(adopt-on) + §6 row 13 + §7 + `optimizations.md` L2 row "Flat-state accelerator on the live account path" — promote `FlatAccountStorage` to **flat-first, MPT-fallback** on the live account read, symmetric with `FlatSlotStorage`; trie stays authoritative so correctness never depends on flat completeness.
- **fukuii `july-fourth`:** `FlatSlotStorage` IS flat-first/MPT-fallback on the live slot path (`InMemoryWorldStateProxy.scala:147-171`); `FlatAccountStorage` is populated (SNAP/RPC serving) but **not** consulted on the live EVM account read — `getAccount` (`:198`) goes straight to `accountsStateTrie` (`state-trie.md` §Gotchas). Both cite besu Bonsai flat by name.
- **Reference source (byte-cited):**
  - besu `…/bonsai/storage/flat/BonsaiFullFlatDbStrategy.java:58` — `getFlatAccount(...)` (the method fukuii's `FlatAccountStorage:19-22` cites); `BonsaiFlatDbStrategy.java` — `getFlatStorageValueByStorageSlotKey`/`storageToPairStream` (the slot analogue). Flat values with trie fallback.
  - go-ethereum snapshot/flat layer — `hash(key)→value` O(1) mirror under a trie fallback; the DEFAULT-adopt-on pattern.
- **Q1 (appropriate?):** Yes — DEFAULT(adopt-on) is correct; it is additive, degrades gracefully (trie authoritative), and merely completes the asymmetry fukuii already half-built.
- **Q2 (what to implement?):** Wire `FlatAccountStorage` into `getAccount` (flat-first, MPT-fallback). Correct — the slot side already proves the pattern; this is symmetry, not new architecture.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: the account read path is at L4 (`WorldStateProxy.getAccount` moves to `execution`); L2 owns the flat CF + the seam, L4 consumes it. The plan places `WorldStateProxy` at L4 (§"Layer boundaries") — so the *wiring* of flat-first is an L4 consumer of an L2 primitive; the plan's §6 row 13 ("promote on the live account read") lands the read-path change at L4 while the CF is L2. Minor edge to keep explicit so the "promotion" isn't sought inside `storage`.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.
- **Plan edit (SHARPENS-adjacent):** §6 row 13 / §"Layer boundaries" — note the flat-first *read wiring* is an **L4** consumer (`WorldStateProxy.getAccount`) of the L2 `FlatAccountStorage` primitive; L2 delivers the CF + flat-first seam, L4 flips the account read to use it.

---

## Group 3 — DataSource / RocksDB engineering (not consensus)

### RX-L2-10 · `DataSource` contract returns `IO`/`fs2.Stream` (R5, TL1/TL2) + `EphemDataSource` parity · Tier B · owner-layer L2 `storage`
- **Plan claim / disposition:** §1 + §5 (cats-effect `IO` + `fs2.Stream` for `iterate`/`scanRange`; no `Future`/`unsafeRunSync`/blocking in the contract) + §8 DoD (`EphemDataSource` ≡ `RocksDbDataSource` behavioral parity; key-ordering + empty-read edge cases where they diverge). DEFAULT — keep the `july-fourth` return shape.
- **fukuii `july-fourth`:** `DataSource.scala:9-108` — `get`/`getOptimized`/`multiGetOptimized`/`scanRange`/`deleteRange`/`update`/`updateSync`/`iterate` returning `fs2.Stream[IO, Either[IterationError, (Array[Byte], Array[Byte])]]`; `EphemDataSource` is a `synchronized` `Map[ByteBuffer, Array[Byte]]`, O(n) filter+sort scans, not thread-safe, no CFs (`storage-persistence.md`).
- **Reference source (byte-cited):**
  - No reference client uses cats-effect (`REVIEW.md` R5: the effect discipline is Typelevel's, not the EVM clients' — they are imperative/async). besu `RocksDBColumnarKeyValueStorage` returns plain values / `Stream` (Java) with a `Stream.onClose` convention; go-ethereum returns iterators with a `Release()` mandate. The plan correctly ports **the pattern's intent** (bounded resource lifetime), not the API.
  - besu `plugin-api/…/storage/` KV contract — the structural mirror for "one backend-agnostic contract, ≥2 impls."
- **Q1 (appropriate?):** Yes — R5 mandates the effect-typed contract; keeping the `july-fourth` `IO`/`fs2` shape (already stronger than besu's convention) is correct.
- **Q2 (what to implement?):** Keep the contract + the `EphemDataSource`/`RocksDbDataSource` parity test. Correct — `EphemDataSource` is confined to throwaway stateless computes (`eth_simulate`, Engine-API dry runs), never restart-surviving data (§9).
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: the contract is the seam every layer above (L4/L7/L9) writes against; the `EphemDataSource` key-ordering/empty-read divergence (`storage-rocksdb.md`) is the one place a parity test is load-bearing (a scan that assumes RocksDB ordering breaks on the `Map`-backed impl). Plan states it (§8, §9).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L2-11 · Per-CF self-describing config (`SegmentIdentifier` → `enum Namespace`) + dedicated bookkeeping CFs · Tier B · owner-layer L2 `storage`
- **Plan claim / disposition:** §4c + §5 (`enum Namespace` cases carry self-describing config) + §6 rows 10 & 15 + §7 + `optimizations.md` L2 row "Per-CF self-describing config" — replace the one-shared-`ColumnFamilyOptions`-for-all-20-CFs with a `enum Namespace` whose cases carry besu-`SegmentIdentifier`-shaped flags (BlobDB/GC/cache-priority/format-gating); dedicated CFs for death-row/snapshots/trie-logs (not prefix-within-`NodeNamespace`). DEFAULT.
- **fukuii `july-fourth`:** `Namespaces.scala:4-29` — 19 single-byte constants, opened as real CFs, but **all 20 CFs share one `cfOpts`** built once (`RocksDbDataSource.scala:619-629`) — static (`Receipts`) and hot (`Node`) treated identically; `ReferenceCountNodeStorage` death-row/snapshot keys are **prefix-within** `NodeNamespace` (`storage-persistence.md` §Gotchas).
- **Reference source (byte-cited):**
  - besu `SegmentIdentifier.java:31,38,47,58,66,74,84` — `getName()`, `getId()`, `includeInDatabaseFormat(DataStorageFormat)` (format-gating), `containsStaticData()` (append-only ⇒ BlobDB-eligible), `isEligibleToHighSpecFlag()` (1 GiB cache opt-in), `isStaticDataGarbageCollectionEnabled()`, `isCacheIndexAndFilterBlocks()` — **exactly** the six flags §4c lists.
  - besu `KeyValueSegmentIdentifier.java:29-41` — the concrete enum: `BLOCKCHAIN(id=1, all-formats, static=true, …)`, `WORLD_STATE(id=2, FOREST)`, `ACCOUNT_INFO_STATE(id=6, BONSAI…)`, `TRIE_LOG_STORAGE(id=10, static=true, GC=true)` — static CFs carry BlobDB/GC flags a hot CF does not.
  - reth per-table tune-against-defaults (RX-L2-12) — the same carrier's OPTIONAL(perf) knob.
- **Q1 (appropriate?):** Yes — DEFAULT is correct; per-dataset tuning is the whole point of CFs and fukuii currently wastes it (one shared `cfOpts`).
- **Q2 (what to implement?):** `enum Namespace` cases carrying the six flags + dedicated bookkeeping CFs. Correct — CFs are cheap; the hot `Node` CF shouldn't hold GC bookkeeping keys.
- **Q3 (mechanism + blast radius?):** Mechanism correct (six flags map 1:1). Blast radius: `includeInDatabaseFormat` means the flag carrier also **gates which CFs exist per StorageProfile** (RX-L2-07 fold) — so `enum Namespace` + `StorageProfile` + `SchemaMarker` are one coupled triple, not three independent items. Namespace-ID immutability (`storage-rocksdb.md`) becomes enforced by the marker (RX-L2-22).
- **Q4:** n/a (CONFIRMS with the RX-L2-07 fold).
- **Verdict:** CONFIRMS.

### RX-L2-12 · Per-table tune-against-defaults (reth) · Tier C (OPTIONAL perf) · owner-layer L2 `storage`
- **Plan claim / disposition:** §4c tail + §7 + `optimizations.md` L2 row "Per-table tune-against-defaults" — disable compression + bloom for incompressible-always-hit hash tables (reth `tx_hash_numbers`), as an axis on the `Namespace` case; OPTIONAL(perf), "under-explored" per the SR.
- **fukuii `july-fourth`:** none — one shared `cfOpts` (LZ4 + ZSTD-bottommost) for every CF.
- **Reference source (byte-cited):**
  - reth `crates/static-file/` + per-table config — reth disables compression/bloom on tables where every key is a random hash (compression futile, bloom pointless since lookups always hit). The design idea (port the shape, not the crate).
- **Q1 (appropriate?):** Yes — OPTIONAL(perf) is correct; it is a measured micro-optimization on the `Namespace` carrier, not a structural requirement.
- **Q2 (what to implement?):** Just the flag on the `Namespace` case; no separate mechanism. Correct — it rides RX-L2-11's carrier.
- **Q3 (mechanism + blast radius?):** Correct; local to CF-open options. No cross-layer ripple.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (Tier C — pattern exists in reth, disposition safe).

### RX-L2-13 · Live-tunable compaction (`ITunableDb`) · Tier C (OPTIONAL perf) · owner-layer L2 `storage`
- **Plan claim / disposition:** §4c note + §6 row 9 + §7 + `optimizations.md` L2 row "Live-tunable compaction" — nethermind `ITunableDb` `HeavyWrite`/`DisableCompaction` via `SetOptions` for the SNAP write-heavy phase; the cited fix for fukuii's SNAP memtable-starvation incident, additive on top of the global memtable ceiling. OPTIONAL(perf).
- **fukuii `july-fourth`:** no live-tunable compaction — one shared `cfOpts` set once; SNAP's write-heavy phase starved the box (~1.9 GiB off-heap, ETC mainnet, cited `RocksDbDataSource.scala:597-606`).
- **Reference source (byte-cited):**
  - nethermind `Nethermind.Db/ITunableDb.cs:6-19` — `void Tune(TuneType)`, `enum TuneType { Default, WriteBias, HeavyWrite, AggressiveHeavyWrite, DisableCompaction, EnableBlobFiles, HashDb }` — **seven** tune modes, not just the two the plan cites.
- **Q1 (appropriate?):** Yes — OPTIONAL(perf) is correct; it directly addresses a cited incident.
- **Q2 (what to implement?):** The `Tune` seam. Correct. But the plan cites only `HeavyWrite`/`DisableCompaction`; the real enum adds `AggressiveHeavyWrite`, `EnableBlobFiles` (composes with the BlobDB-per-static-CF axis, RX-L2-11), and `HashDb` (retune when the keying scheme is Hash — ties to RX-L2-08's `Scheme`). A richer, still-additive set.
- **Q3 (mechanism + blast radius?):** Mechanism correct (`SetOptions` live retune). Blast radius: `EnableBlobFiles` overlaps RX-L2-11's BlobDB axis; `HashDb` overlaps RX-L2-08's scheme — worth noting so the three aren't designed in isolation. The memtable ceiling stays (additive, not replacement — §9).
- **Q4:** n/a (SHARPENS on the enum breadth).
- **Verdict:** SHARPENS.
- **Plan edit (SHARPENS):** §6 row 9 / §7 — note the nethermind `TuneType` set is **seven** (`Default/WriteBias/HeavyWrite/AggressiveHeavyWrite/DisableCompaction/EnableBlobFiles/HashDb`, `ITunableDb.cs:10-18`), and `EnableBlobFiles`/`HashDb` compose with the BlobDB (RX-L2-11) and keying-scheme (RX-L2-08) axes respectively — design the `Tune` seam over the full set, not just `HeavyWrite`/`DisableCompaction`.

### RX-L2-14 · Storage traps carried from `storage-rocksdb.md` (iterator-safety, deleteRange, memtable ceiling, `setStatsDumpPeriodSec(0)`, `liveIterGauge`) · Tier B · owner-layer L2 `storage`
- **Plan claim / disposition:** §4c ("keep the fukuii-authored hardening") + §6 row 16 + §8 DoD + §9 traps — carry forward the `#1355` batched-iterator fix (`newIterator` only inside `withResources`), `deleteRange` not point-delete-loop, the global `dbWriteBufferSize`/`maxTotalWalSize` memtable ceiling, `setStatsDumpPeriodSec(0)` JNI-SIGSEGV guard; add geth's `liveIterGauge` leak metric. DEFAULT (stronger than besu).
- **fukuii `july-fourth`:** all present and cited to specific ETC-mainnet incidents — `unboundedScan` batching (`RocksDbDataSource.scala:203-397`, `#1355` SIGSEGV race), `deleteRange` (`:127-139`, the ~140M-key/~30-min incident), memtable ceiling (`:597-606`) — but **no** `liveIterGauge` leak metric (`storage-persistence.md` DEFAULT note).
- **Reference source (byte-cited):**
  - besu `RocksDBColumnarKeyValueStorage` — the `Stream.onClose` convention (weaker than fukuii's batch-scoped iterators — the plan's explicit **do-not "align to besu"**); `setStatsDumpPeriodSec(0)` is besu's documented JNI-SIGSEGV workaround.
  - go-ethereum `core/rawdb` — the `liveIterGauge`-equivalent leak metric (the one thing to *add* on top of the already-strong scan).
- **Q1 (appropriate?):** Yes — carrying incident-driven hardening forward (DEFAULT) is exactly right; these are stronger than the byte-authority's shape and driven by real ETC incidents.
- **Q2 (what to implement?):** Keep all, add `liveIterGauge`. Correct — the metric makes leaks observable (they're already structurally prevented).
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: iterator-safety is a whole-`storage/`-sweep grep (DoD gate, §8/§9), not a one-file patch; the `liveIterGauge` sink is per-instance (R2) and its registry is at L8 (`observability`), so L2 emits, L8 registers (§"Layer boundaries"). Plan states it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Group 4 — Pruning

### RX-L2-15 · Composable `EvictionStrategy` × `PersistenceStrategy` under the `PruningMode` selector · Tier B · owner-layer L2 `storage`
- **Plan claim / disposition:** §5 gap #6 + §6 row 6 + §7 + `optimizations.md` L2 row "Composable `IPruningStrategy`/`IPersistenceStrategy` split" — `PruningMode` stays the user-facing named selector (Archive/Basic/InMemory), but each mode is **composed** of an `EvictionStrategy` ("when to evict RAM") × `PersistenceStrategy` ("where/when to flush disk"); kills the two independently-maintained refcount impls. DEFAULT (reconcile with the enum).
- **fukuii `july-fourth`:** two independent refcount impls — disk `ReferenceCountNodeStorage` vs in-memory `CachedReferenceCountedStorage` — no shared strategy interface between "when to evict" and "where to persist" (`storage-persistence.md` §Gotchas).
- **Reference source (byte-cited):**
  - nethermind `Nethermind.Trie/Pruning/IPruningStrategy.cs:8-11` — `DeleteObsoleteKeys`, `ShouldPruneDirtyNode(TrieStoreState)`, `ShouldPrunePersistedNode(TrieStoreState)` — the **eviction** policy (distinguishes dirty-in-RAM vs persisted-node pruning).
  - nethermind `Nethermind.Trie/Pruning/IPersistenceStrategy.cs:8-10` — `bool ShouldPersist(ulong blockNumber)` — the **flush-to-disk** policy. Two orthogonal interfaces, exactly the split the plan ports.
- **Q1 (appropriate?):** Yes — DEFAULT + reconcile-with-enum is correct; composing two strategies collapses two class hierarchies into one policy.
- **Q2 (what to implement?):** The named enum (preset) with composed strategy internals. Correct — the enum stays user-facing; the two strategies are its internals.
- **Q3 (mechanism + blast radius?):** Mechanism correct — one refinement on naming: nethermind's **eviction** interface is literally named `IPruningStrategy` (with a **two-method** dirty-vs-persisted split), and `IPersistenceStrategy` is the flush half. The plan's `EvictionStrategy` = nethermind `IPruningStrategy`; keep the dirty/persisted two-method distinction (evicting a dirty RAM node ≠ pruning a persisted disk node). Blast radius: internal to `PruningMode`; the composed Basic-mode must pass the same GC + rollback tests as the monolith (§7). No cross-layer ripple.
- **Q4:** n/a (SHARPENS on the two-method eviction split).
- **Verdict:** SHARPENS.
- **Plan edit (SHARPENS):** §5 gap #6 — note the `EvictionStrategy` (nethermind `IPruningStrategy`) is a **two-method** split (`ShouldPruneDirtyNode` vs `ShouldPrunePersistedNode`, `IPruningStrategy.cs:10-11`) — evicting a dirty in-RAM node and pruning a persisted disk node are distinct decisions; the composed strategy must express both, not a single "evict?" predicate.

### RX-L2-16 · Three pruning modes (Archive / Basic / InMemory) via the composed strategies · Tier B · owner-layer L2 `storage`
- **Plan claim / disposition:** §2 (state-trie DEFAULT) + §7 row "Three pruning modes" — Archive-never-prunes; Basic refcount-GC removes only 0-ref nodes past horizon + `rollback` replays (reorg); InMemory changelog rollback — each state-root-preserved. DEFAULT (the seam).
- **fukuii `july-fourth`:** `StateStorage.scala:133` dispatches `PruningMode { ArchivePruning | BasicPruning(history) | InMemoryPruning(history) }` → `ArchiveNodeStorage`/`ReferenceCountNodeStorage`+`FastSyncNodeStorage`/`CachedReferenceCountedStorage`; `prune(:154)` removes death-row 0-ref nodes past horizon, `rollback(:173)` replays snapshots (`state-trie.md`).
- **Reference source (byte-cited):**
  - besu Forest mark-and-sweep (archive/refcount) + Bonsai online trie-log pruner — the two-family model behind `DataStorageFormat`.
  - nethermind `IPruningStrategy`/`IPersistenceStrategy` (RX-L2-15) — the composable realization the modes decompose into.
- **Q1 (appropriate?):** Yes — three modes behind one seam is the DEFAULT (policy-as-data), matching besu/nethermind; fukuii already has it (coarser).
- **Q2 (what to implement?):** The three modes as presets over composed strategies (RX-L2-15). Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct (0-ref past horizon; snapshot replay). Blast radius: each mode must preserve the state root (forge-adjacent — a prune that removes a live node corrupts the root); `prune` now takes the safe-height barrier (RX-L2-17). Plan states the state-root-preserved assertion (§7/§8).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L2-17 · Online full-pruning (nethermind copy-live-and-swap) · Tier C (OPTIONAL custody) · owner-layer L2 (`PersistenceStrategy` seam, mode deferred)
- **Plan claim / disposition:** §2 OPTIONAL(custody/end-user) + §7 + `optimizations.md` L2 row "Online full-pruning" — nethermind `FullPruningDb` copy-live-to-fresh + atomic swap; "the biggest missing operator capability"; the pass may re-key Hash→HalfPath. Seam at L2 (`PersistenceStrategy`), mode deferred.
- **fukuii `july-fourth`:** none — no online/live-traffic-safe full-pruning path (`storage-persistence.md` §Pruning mode "diverges").
- **Reference source (byte-cited):**
  - nethermind `Nethermind.Db/FullPruning/FullPruningDb.cs` — the copy-live-and-swap online pruner (present in the tree; the design-idea authority).
- **Q1 (appropriate?):** Yes — OPTIONAL(custody) with a seam-now is correct; occupancy defers (role-gated) but the `PersistenceStrategy` hook that a full-pruning pass plugs into must exist.
- **Q2 (what to implement?):** The seam only now (the mode is deferred). Correct — and the note that the pass can re-key Hash→HalfPath ties it to RX-L2-08 (a full-prune is a natural migration point).
- **Q3 (mechanism + blast radius?):** Mechanism correct (copy-live, atomic swap, uninterrupted reads). Blast radius: composes with the `INodeStorage` scheme migration (RX-L2-08) and the R7 safe-height (a full-prune must respect consumer heights). Plan states the seam placement.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (Tier C — mechanism exists in nethermind, seam-now/defer disposition safe).

---

## Group 5 — Prune-barrier / R7 (structural spine)

### RX-L2-18 · Safe-height-gated `prune(safeHeight)` barrier (reth ExEx `FinishedHeight`) · Tier A (STRUCTURAL R7) · owner-layer L2 pruning seam
- **Plan claim / disposition:** §5 gap #2 + §6 row 2 + §7 + `requirements.md` R7/L2 + `optimizations.md` L2 STRUCTURAL — `prune(safeHeight): IO[PruneReport]` where `safeHeight = min(local reorg horizon, min(consumer FinishedHeights))`; never prunes purely on local reorg depth. The consumer registry lives at L9's gRPC seam; the *hook* is designed into L2 now (one-way to retrofit). Absent a consumer, `safeHeight` = local reorg horizon (today's behavior — additive, no regression).
- **fukuii `july-fourth`:** no external prune barrier — pruning keys purely on local reorg depth (`ReferenceCountNodeStorage.prune(blockNumber)`, `state-trie.md`).
- **Reference source (byte-cited):**
  - reth ExEx `FinishedHeight` — a consumer reports the height it has durably processed; the node must not prune above `min(consumer heights)` so an out-of-process consumer can still replay. The distributed prune barrier (`requirements.md` R7; `cross-cutting-themes.md` Theme 2). (The reth ExEx notification/`FinishedHeight` types are in `crates/exex/` — verified present in the L5 RX path-check; the L2 concern is only the *storage hook* that accepts the external height.)
  - besu synchronous callbacks lack backpressure/reorg-safety (the shape R7 improves on) — the plan's cited contrast.
- **Q1 (appropriate?):** Yes — STRUCTURAL(R7) is correct: retrofitting a safe-height gate into a pruner that only knows local reorg depth is a rewrite; the hook must exist before any out-of-process consumer. The "absent consumer ⇒ local horizon" default makes it a zero-regression addition.
- **Q2 (what to implement?):** The `prune(safeHeight)` signature + the `min(...)` composition now; the consumer registry defers to L9. Correct — building the registry now would be YAGNI (no consumer yet), but the *parameter* is the one-way part.
- **Q3 (mechanism + blast radius?):** Mechanism correct (`min(local horizon, min(consumer heights))`). Blast radius: L9 supplies `min(consumer heights)` via the gRPC seam; L4/L5 are the reorg-event source (RX-L2-19); the TrieLog reorg stream (RX-L2-19) is what a consumer subscribes to. The plan states all three (§"Layer boundaries": R7 consumer registry → L9 + L4/L5). One edge worth making explicit: the barrier interacts with online full-pruning (RX-L2-17) — a full-prune pass must also honor `safeHeight`, not just the incremental pruner.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.
- **Plan edit (SHARPENS-adjacent):** §7 / §9 — note the `safeHeight` barrier binds **both** the incremental refcount pruner **and** the online full-pruning pass (RX-L2-17); a full-prune copy-swap must not drop nodes above `safeHeight` either.

### RX-L2-19 · besu-`TrieLog` `{prior,updated}` leaf-diff as the R7 reorg-event source · Tier B · owner-layer L2 (`trie` builds it; format on the flat/pruned profile)
- **Plan claim / disposition:** §4d + §6 row 8 + §7 + `optimizations.md` L2 row "BonsaiTrieLog `{prior,updated}` leaf-diff" — build the flat/pruned (`StorageProfile`) path with a besu-`TrieLog`-shaped `{prior,updated}` journal so the reorg-event source is available where R7 needs it; keep the node-refcount snapshots for the hash-keyed archival profile. OPTIONAL(light) + R7 enabler.
- **fukuii `july-fourth`:** reorg substrate is `ReferenceCountNodeStorage`'s **node-level** death-row snapshots — coupled to the hash-keyed store, **not** a leaf diff, so it can't be handed to an out-of-process consumer as a state-change stream (`state-trie.md` §Reorg rollback).
- **Reference source (byte-cited):**
  - besu `plugin-api/…/trielogs/TrieLog.java:33,58,66,74,152,159` — `interface TrieLog` with `getAccountChanges(): Map<Address, LogTuple<AccountValue>>`, `getCodeChanges()`, `getStorageChanges(): Map<Address, Map<StorageSlotKey, LogTuple<UInt256>>>`; `LogTuple<T> { T getPrior(); T getUpdated(); isUnchanged(); }`. A per-block **account/code/storage leaf-value diff** (not node-level) — serializable, so an out-of-process consumer can roll forward/back.
- **Q1 (appropriate?):** Yes — OPTIONAL(light)+R7-enabler is correct; the TrieLog is the clean serializable reorg-event source R7's seam wants, and node-refcount snapshots demonstrably can't feed an out-of-process consumer.
- **Q2 (what to implement?):** The TrieLog on the flat/pruned profile, keeping node-refcount snapshots for archival. Correct — two reorg substrates for two profiles is the right call (the leaf-diff needs the flat/value-keyed layout to be cheap).
- **Q3 (mechanism + blast radius?):** Mechanism correct — and the byte-cite sharpens it: the diff is keyed by **`Address` / `StorageSlotKey`** (leaf-value), not node hash, which is precisely why it's serializable across a process boundary (a node-hash diff would require the consumer to share the trie). Blast radius: the TrieLog *format* is L2; the reorg-event *stream* it feeds is L4/L5 (the reorg authority) → L9 gRPC. The plan states the format-vs-stream split (§4d, §"Layer boundaries"). The `{prior,updated}` diff builder must live in `trie` (byte-pure `storage` can't parse it — §9 cycle risk).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Group 6 — Schema marker (structural)

### RX-L2-20 · `SchemaMarker(format, version, profile)` recording the active `StorageProfile` · Tier A (STRUCTURAL / DEFAULT) · owner-layer L2 `storage`
- **Plan claim / disposition:** §2 (highest-value single item) + §5 gap #7 + §6 row 7 + §7 + §9 + `optimizations.md` L2 STRUCTURAL — `enum StorageFormat` + a `SchemaMarker(format, version, profile)` value, `derives Codec`, written to a dedicated CF, **checked at open**; a datadir built under one profile reopened under a mismatched profile fails loud with a typed error. besu `BaseVersionedStorageFormat` analogue extended to carry the profile.
- **fukuii `july-fourth`:** **no** persisted schema-version marker — CF set implicit in `nsSeq`; a reused/renamed namespace silently corrupts; grep across `db/` for a besu-`DATABASE_METADATA`/reth-marker/erigon-per-file equivalent turns up nothing (`storage-persistence.md` §Gotchas — "the single most exportable pattern and fukuii's clearest gap").
- **Reference source (byte-cited):**
  - besu `DATABASE_METADATA.json` + `BaseVersionedStorageFormat` — a `(format, version)` marker with per-bump rationale, checked at open (the authority the plan names).
  - go-ethereum `core/rawdb/schema.go:46-47` — `persistentStateIDKey` ("path-based only") — geth **infers** the scheme + persists a state-id rather than a hard `(format,version,profile)` marker; the cross-check shows the weaker end (inference), which is exactly why the marker is "highest-value."
  - reth 3-marker / nethermind `VersionHistory`/auto-detect — what else a marker can carry.
- **Q1 (appropriate?):** Yes — DEFAULT/STRUCTURAL and "highest-value" is correct; besu proves the pattern and fukuii's total absence is the confirmed gap.
- **Q2 (what to implement?):** The `(format, version, profile)` marker checked at open. Correct — extending besu's `(format,version)` to carry the profile is the right sharpening for a multi-approach client (a mode-mismatched datadir must fail loud).
- **Q3 (mechanism + blast radius?):** Mechanism correct. **Blast radius under-stated (the RX-L2-07/11 fold):** because the resolved `StorageProfile` gates the **CF set** (besu `includeInDatabaseFormat` — Forest opens `WORLD_STATE`, Bonsai opens `ACCOUNT_INFO_STATE`/`TRIE_LOG` instead), the marker check is not only `(format,version,keying)` equality but **CF-set-vs-profile consistency** — opening a Bonsai datadir under a Forest profile must fail at the marker, before RocksDB hits a missing/extra CF. Namespace-ID immutability (`storage-rocksdb.md`) becomes enforced *by this marker*.
- **Q4 (correct answer):** The marker gate covers **CF-set-vs-profile**, not just scalar `(format,version,keying)` — the profile determines which CFs exist (RX-L2-07), so the open check must reconcile the on-disk CF set with the profile the marker records. Source: besu `KeyValueSegmentIdentifier.includeInDatabaseFormat` format-gating.
- **Verdict:** SHARPENS.
- **Plan edit (SHARPENS):** §5 gap #7 / §7 row "Schema-version marker" / §8 — the marker check reconciles the **opened CF set with the recorded profile** (format-gated CFs, besu `includeInDatabaseFormat`), not only scalar `(format,version,keying)`; a profile whose CF set doesn't match the datadir's actual CFs fails at the marker before RocksDB sees a missing/extra column family.

---

## Group 7 — Freezer / history / SNAP / checkpoint

### RX-L2-21 · Hot/cold freezer split + **TD retention** + write-time-boundary freeze · Tier B · owner-layer L2 `storage` (`ColdStore` seam; mode role-gated)
- **Plan claim / disposition:** §2 (historical-distribution DEFAULT storage-lifecycle) + §4e + §5 `ColdStore` gap #11 + §6 row 11 + §7 + `optimizations.md` L2 row "Hot/cold freezer" — a sealed `ColdStore` with `freeze(reorgSafeRange)` + number-addressed read; **TD retained** in the cold shard (core-geth ETC invariant); freeze at the **write-time boundary** (reth) not a separate pass (avoids the geth freeze-pass bug). DEFAULT storage-lifecycle — seam at L2, cutover OPTIONAL(archival).
- **fukuii `july-fourth`:** **no** hot/cold split — every header/body/receipt/node in one RocksDB for the node's life; grep across `db/` for `freezer`/`ancient`/`era1`/`4444` returns nothing (`historical-distribution.md` §Gotchas).
- **Reference source (byte-cited):**
  - core-geth `core/rawdb/ancient_scheme.go:35-36` — `ChainFreezerDifficultyTable = "diffs"` (the TD table); `:46` marks it retained; `chain_freezer.go:293` — `op.AppendRaw(ChainFreezerDifficultyTable, number, td)`. **The ETC byte-authority freezer retains total difficulty** — confirming the plan's "TD retained" invariant against the *only* living ETC authority (the post-merge ETH freezer drops it — the wrong template for a PoW successor).
  - go-ethereum `core/rawdb/freezer.go` — the append-only number-addressed ancient store (the mechanism origin); reth `crates/static-file/` — write-time-boundary freeze (mmap static files at the write boundary).
- **Q1 (appropriate?):** Yes — DEFAULT storage-lifecycle with seam-now/cutover-deferred is correct; fukuii's total absence is the gap and the ETC-TD-retention invariant is the one place core-geth is authoritative.
- **Q2 (what to implement?):** The `ColdStore` seam + TD retention + write-time freeze. Correct — and the core-geth cite makes TD-retention a **verified ETC invariant**, not a design guess.
- **Q3 (mechanism + blast radius?):** Mechanism correct and now byte-confirmed (TD table = `"diffs"`, retained). Blast radius: the cold store's read path must serve pre-freeze blocks to L9 (RPC historical queries) and L7 (serving); the freeze boundary must be reorg-safe (below the deepest reorg). Plan states the seam-at-L2 / transport-at-L7 split (§"Layer boundaries").
- **Q4:** n/a (CONFIRMS with the ETC-TD cite strengthening the invariant).
- **Verdict:** CONFIRMS. forge light-interest (TD is consensus data for PoW fork-choice; the cold store must not lose it).
- **Plan edit (SHARPENS):** §5 gap #11 / §7 — cite the exact ETC authority: core-geth `ancient_scheme.go:35-36` `ChainFreezerDifficultyTable = "diffs"` (retained, `:46`; appended `chain_freezer.go:293`) — the "TD retained" invariant is verified against the living ETC freezer, not inferred.

### RX-L2-22 · Unify freezer/era1/expiry into one fixed-range sharded `ColdStore` format + ERA1 · Tier C (OPTIONAL archival) · owner-layer L2 `storage`
- **Plan claim / disposition:** §2 cross-cutting + §4e + §5 + §6 row 11 + §7 + `optimizations.md` L2 rows "Unify freezer/era1/expiry" & "ERA1 standard" — one fixed-block-range sharded format so freezer + era1 export + EIP-4444 expiry all fall out of the same sharding (`rm` a shard = expiry); ERA1 for interop. For ETC only the *original* pre-merge era1 is needed. OPTIONAL(archival).
- **fukuii `july-fourth`:** none — no era1/`.seg`/static-file/segment format; the checkpoint archive is state-only, not a history segment (`historical-distribution.md`).
- **Reference source (byte-cited):**
  - go-ethereum `internal/era/` (`era.go`, `accumulator.go`, `e2store/e2store.go`) — the ERA1 reader/writer + SSZ accumulator over `(hash, TD)`; besu `util/era1` — the JVM ERA1 reference; reth `crates/static-file/` — whole-file freeze; erigon `.seg` sharding — the fixed-range-file transport-and-expiry unification.
- **Q1 (appropriate?):** Yes — OPTIONAL(archival) is correct; one sharded format buying freeze+distribute+expire is a genuine simplification, but it's a later cutover (the seam is RX-L2-21).
- **Q2 (what to implement?):** The seam-compatible format now; ERA1 conformance when built. Correct — "ETC needs only pre-merge era1; EraE/EIP-4444 machinery is PoS-specific" is the right scoping.
- **Q3 (mechanism + blast radius?):** Mechanism correct (whole-file granularity ⇒ transport + expiry from one design). Blast radius: the *format* is L2; distribution transport (HTTP range, BitTorrent, ERA1 downloader) is L7/product (§"Layer boundaries"). No consensus ripple (history, not state).
- **Q4:** n/a.
- **Verdict:** CONFIRMS (Tier C — pattern present across geth/besu/reth/erigon, defer-with-seam safe).

### RX-L2-23 · Checkpoint archive (state-pivot) + accumulator commitment · Tier B · owner-layer L2 `storage.checkpoint`
- **Plan claim / disposition:** §2 (historical-distribution DEFAULT state-pivot) + §6 row 12 + §7 + `optimizations.md`-adjacent — accumulator-committed checkpoint (SSZ-style over `(hash, TD)`, era1-shaped) so it verifies its *content* against a known root before trust; export/import through `StateStorage`. DEFAULT(state-pivot). *Sync-time engagement* → L7.
- **fukuii `july-fourth`:** `CheckpointArchive.scala:41-227` — a state-only archive (header + TD + trie nodes + bytecode) with a **CRC32 trailer only** — catches transport corruption, no content commitment; trust is operator-provenance-only (`historical-distribution.md` §Gotchas). Already occupies besu's checkpoint-anchor niche but ships the state, not a pointer.
- **Reference source (byte-cited):**
  - go-ethereum `internal/era/accumulator.go` — the SSZ accumulator over `(hash, TD)` baked into the filename, verifiable standalone before trust. The commitment the CRC32 trailer lacks.
  - besu checkpoint-anchor (concept: ship a trusted pointer past history, let SNAP fill state) — the conceptual analogue fukuii already exceeds (carries the state).
  - nethermind `Init.Snapshot` — the resumable/checksum-gated HTTP transport (fukuii's `CheckpointDownloader` independently arrived at the same shape).
- **Q1 (appropriate?):** Yes — DEFAULT(state-pivot) + upgrade-CRC32-to-accumulator is correct; the content-commitment gap is real (a malicious checkpoint is a trust-root attack).
- **Q2 (what to implement?):** Keep the state-pivot archive + the atomic all-or-nothing import; add the accumulator commitment. Correct — moves a bespoke non-interoperable format toward the era1 standard (R9/F8).
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: the *format* (accumulator) is L2; *sync-time engagement* (import-only-on-fresh-DB, config keys, resumable HTTP driver) is L7 (§"Layer boundaries"). The atomic single-batch import (`CheckpointImporter.scala:152-161`) is a strength to preserve. Note: the content commitment must verify against the state root the accumulator commits — a forge-adjacent check (the imported state's root must match).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L2-24 · SNAP serving storage primitive (`FlatAccountStorage.seekFrom` range-serve, F9) · Tier C (OPTIONAL role: server/archival) · owner-layer L2 (primitive); L7 drives
- **Plan claim / disposition:** §7 row "SNAP serving (F9)" + §"Layer boundaries" + `feature-ledger.md` F9 — L2 owns the path-keyed store + the `FlatAccountStorage.seekFrom` range-serve **primitive** as storage; L7 drives serving (range-proof, DoS budgeting, snap/1↔snap/2 `Syncer`). OPTIONAL(role, F9) — storage primitives at L2, engagement → L7.
- **fukuii `july-fourth`:** `FlatAccountStorage.seekFrom` (`:60-69`) is a range scan for SNAP `GetAccountRange` peer-serving (gated on `RocksDbDataSource`); `FlatSlotStorage.seekStorageRange` mirrors besu's `BonsaiFlatDbStrategy.storageToPairStream` (`storage-persistence.md`). The primitives exist; serving-as-a-role does not.
- **Reference source (byte-cited):**
  - besu `…/bonsai/storage/flat/BonsaiFullFlatDbStrategy.java:75` — `getFlatStorageValueByStorageSlotKey` / `storageToPairStream` (the range-serve primitive fukuii mirrors); go-ethereum snap/1 serving (`softResponseLimit`/`maxTrieNodeTimeSpent` DoS bounds — the L7 concern).
- **Q1 (appropriate?):** Yes — splitting the L2 *primitive* from the L7 *serving policy* is correct; range-serve is a storage capability, DoS budgeting is a sync/network policy.
- **Q2 (what to implement?):** Just the `seekFrom`/`seekStorageRange` primitive at L2; the serving role at L7. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: this reverses the `july-fourth` `db → blockchain.sync` back-edge (SNAP storage components lived under `db/`) — the primitive stays in `storage`, consumed *downward* by `sync` (§"Layer boundaries"). Good — the plan names it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (Tier C — primitive present, role-gated defer safe).

### RX-L2-25 · era1 verifiable history bootstrap / EIP-4444 expiry · Tier C (OPTIONAL fast-bootstrap / light) · owner-layer L2 (deferred; composes with checkpoint + `ColdStore`)
- **Plan claim / disposition:** §2 + §7 row "era1 verifiable history bootstrap / EIP-4444 expiry" — deferred; composes with the checkpoint pivot + `ColdStore` shard; accumulator-verified era1 segment import; expiry drops pre-cutoff bodies/receipts, retains headers+genesis. OPTIONAL.
- **fukuii `july-fourth`:** none — no `--history.chain` knob, no `prune-history` subcommand; grep for `4444`/`history-expir*` returns nothing (`historical-distribution.md` §Gotchas); matches core-geth's no-expiry row (ETC has no merge boundary, no expiry mandate).
- **Reference source (byte-cited):**
  - go-ethereum `internal/era/` + freezer tail-groups (expiry); core-geth "None — stores full history, no prune" (the ETC row — absence is evidence the ETC authority has no expiry mandate, so this is a light/archival option, not a requirement).
- **Q1 (appropriate?):** Yes — OPTIONAL/deferred is correct; ETC has no expiry mandate (core-geth confirms), so this is purely a light-client/disk-saving option composing with the `ColdStore` shard.
- **Q2 (what to implement?):** Nothing now beyond the `ColdStore` shard compatibility (RX-L2-22); the expiry `rm` and era1 import land when scheduled. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct (whole-shard `rm` = expiry; retain headers+genesis). No consensus ripple (history). Blast radius: composes with checkpoint (state pivot) + ColdStore (shard) — all three share the fixed-range sharding.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (Tier C — geth authority present, ETC-absence confirms OPTIONAL).

### RX-L2-26 · Bonsai thin-trie + TrieLog / sparse-trie + witness · Tier C (OPTIONAL light/custody) · owner-layer L2 (comparison target)
- **Plan claim / disposition:** §2 OPTIONAL(light/custody) + §7 row "Bonsai thin-trie + TrieLog / sparse-trie + witness" — flat overlay + `INodeStorage` are the start; trie-log `{prior,updated}` rollForward/rollBack to a shared ancestor when scheduled. OPTIONAL(light/custody), comparison target.
- **fukuii `july-fourth`:** no Bonsai thin-trie, no full-block witness / sparse-trie (reth `SparseStateTrie`/`TrieWitness`) — stateless execution isn't a capability fukuii has (`state-trie.md` §Gotchas).
- **Reference source (byte-cited):**
  - besu `TrieLog` rollForward/rollBack (RX-L2-19) + Bonsai thin proof trie; reth `crates/trie/sparse/src/state.rs` + `crates/trie/trie/src/witness.rs` (`SparseStateTrie`/`TrieWitness`) — present in the tree; the stateless/witness authority.
- **Q1 (appropriate?):** Yes — OPTIONAL(light/custody) is correct; these are genuinely-worth-having patterns for stateless/light roles, not baseline, and the flat overlay + INodeStorage seam are the right starting substrate.
- **Q2 (what to implement?):** Nothing now beyond the flat/INodeStorage seam (which is the on-ramp); the thin-trie/witness land role-gated. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct (leaf-diff rollForward/rollBack; witness = sparse trie over touched keys). Blast radius: witnesses feed stateless execution at L3/L4 (F11 ZK-rollup depth); the plan homes F11 witness at L2 (`feature-ledger.md` F11). No consensus ripple until stateless execution ships.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (Tier C — reth/besu authorities present, defer safe).

---

## Group 8 — OBSOLETE (named-and-avoided) + retirements

### RX-L2-27 · MDBX + flat Domains (erigon) / reth secondary-engine split — OBSOLETE · Tier C · owner-layer L2
- **Plan claim / disposition:** §2 (storage-persistence OBSOLETE) + §7 foot + §5 (`engine: KvEngine` "MDBX = OBSOLETE, not offered") + `optimizations.md` L2 OBSOLETE row — no mature JVM MDBX binding; COW-B+tree assumptions don't map onto RocksDB's LSM; filed as archival aspiration, the `backend` axis does not offer them. OBSOLETE (named-and-avoided).
- **fukuii `july-fourth`:** RocksDB only.
- **Reference source (byte-cited):**
  - erigon `db/kv/mdbx/` + `db/state/domain.go` — MDBX + flat Domains are **present in the erigon tree** (the mechanism exists) — but no JVM client uses MDBX (besu/nethermind are both RocksDB); the **absence of an MDBX-backed JVM client is the positive evidence** that the OBSOLETE call is right for fukuii's stack.
- **Q1 (appropriate?):** Yes — OBSOLETE is correct; the mechanism is real (erigon) but the JVM-ecosystem constraint (no mature MDBX binding, LSM-vs-COW mismatch) makes it a re-architecture, not a swap.
- **Q2 (what to implement?):** NOTHING — named-and-avoided. Correct; and the `engine` axis correctly does not offer it (which is why RX-L2-07 flags `engine` as a one-inhabitant reserved axis).
- **Q3 (mechanism + blast radius?):** Correct. Blast radius of *avoiding* it: the `StorageProfile.engine` axis has one inhabitant (RocksDB) — the OBSOLETE call is what makes that axis reserved-not-live (RX-L2-07 fold).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L2-28 · Retire `BootstrapDownload` (built-but-broken CLI verb) · Tier C · owner-layer L2
- **Plan claim / disposition:** §6 row 17 — retire `BootstrapDownload` (superseded by the checkpoint archive); it is a built-but-broken CLI verb, the "built-but-unshipped" anti-pattern the rebuild exists to kill.
- **fukuii `july-fourth`:** `BootstrapDownload.scala:26,32-39` — `cleanOutFolder` hard-asserts the target folder name `== "leveldb"`, but `App.scala:238-244` wires it to the `rocksdb/` datadir — so `assertAndLog` throws before any op (a leftover from the pre-RocksDB migration; `historical-distribution.md` §Gotchas — confirmed broken against today's default layout).
- **Reference source (byte-cited):**
  - nethermind `Init.Snapshot` — the working DB-snapshot transport (the pattern fukuii's checkpoint archive supersedes for the state-pivot case); no reference client keeps a broken bootstrap verb.
- **Q1 (appropriate?):** Yes — retiring a verb that throws before doing anything is correct (dead-code-review "Delete it": superseded + broken).
- **Q2 (what to implement?):** NOTHING — remove it. Correct.
- **Q3 (mechanism + blast radius?):** Correct. Blast radius: none — the verb is non-functional; the checkpoint archive (RX-L2-23) covers the state-pivot bootstrap use case, and the `ColdStore`/era1 (RX-L2-22) covers history distribution. No consumer depends on it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Coverage note

Every distinct L2 registry item is entered above: `optimizations.md` L2's 15 rows (StorageProfile RX-L2-07,
node-keying RX-L2-08, streaming/`HashBuilder` RX-L2-06, flat-accelerator RX-L2-09, online-full-pruning
RX-L2-17, composable-strategy RX-L2-15, live-tunable-compaction RX-L2-13, per-CF-config RX-L2-11,
per-table-tune RX-L2-12, freezer+TD RX-L2-21, unify-freezer/era1 RX-L2-22, ERA1 RX-L2-22, TrieLog RX-L2-19,
schema-marker RX-L2-20, MDBX-OBSOLETE RX-L2-27); the §5 idiom targets + §6 improvements (enum MptNode
RX-L2-01, `<32`/HP/keccak RX-L2-02, trie↔persistence RX-L2-03, db↔mpt RX-L2-04, getProof RX-L2-05,
prune-barrier RX-L2-18, DataSource/Ephem RX-L2-10, storage-traps/liveIterGauge RX-L2-14, dedicated-CFs
RX-L2-11, three-pruning-modes RX-L2-16, checkpoint+accumulator RX-L2-23, `BootstrapDownload`-retire
RX-L2-28); the §7 deferrals (SNAP-serving-primitive RX-L2-24, era1/EIP-4444 RX-L2-25, Bonsai/sparse-witness
RX-L2-26); and the R1/R2/R4/R5/R7/R8 downward-constraint cells (R8 RX-L2-07/08, R7 RX-L2-18, R5 RX-L2-10,
R4 RX-L2-01/02, R1/R2 network-neutrality + per-instance isolation are §9 invariants confirmed across
RX-L2-04/07/10/14 — no `isEtc()` in storage keys, per-`ChainInstance` handles).

**Verdict tally:** 28 items — **1 CORRECTS** (RX-L2-06 `GenerateTrie` mis-attributed to reth; it is
go-ethereum's), **6 SHARPENS** (RX-L2-02, 07, 08, 13, 15, 20), **21 CONFIRMS**. No MISSING paths (every cited
reference path is present in the vendored tree). The structural spine (StorageProfile, INodeStorage,
prune-barrier, SchemaMarker) is verified sound; the SHARPENS tighten mechanism detail (INodeStorage
account-scoped key; the format-gated CF-set blast radius) and blast-radius edges the plan under-stated,
none of which change a disposition.
