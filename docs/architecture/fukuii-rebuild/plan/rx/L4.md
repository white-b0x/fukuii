# RX — L4 `execution` per-item reference-client verification

> **Research grounding:** verify against reference-client source **and** cite this layer's binding SR
> observation(s); the RX crosscheck method + the asset→layer map are in
> [`../../research-index.md`](../../research-index.md) and [`../coherence-pass-02.md`](../coherence-pass-02.md) (WB-R5).

_The depth pass for L4 (execution). Method: [`README.md`](README.md). Each item is verified against the
**actual vendored source** under `.claude/repo-references/clients/{besu,core-geth,go-ethereum,nethermind,erigon,reth}/`,
byte-cited `client/path:line`. **Frozen-ETC items carry a SECOND ETC authority: `clients/besu-etc`** — besu's own
pre-removal ETC (commit `eb4248c997`, `ClassicBlockProcessor.java`/`ClassicProtocolSpecs.java`) — read alongside
core-geth so ECIP-1017 is not verified against a single implementation (operator directive). Registry enumerated from [`../L4.md`](../L4.md) §2/§2.1/§4/§5/§6/§7/§8,
[`../optimizations.md`](../optimizations.md) L4 rows, and [`../requirements.md`](../requirements.md) R1/R2/R7 cells._

**Four-questions format (operator directive).** Every entry answers, before its verdict:
- **Q1 — appropriate decision?** is the plan's disposition (DEFAULT/STRUCTURAL/OPTIONAL/OBSOLETE) the right call given the source?
- **Q2 — right thing to build?** the right thing vs. a better alternative the multi-client read reveals vs. NOTHING (YAGNI / seam suffices)?
- **Q3 — mechanism + blast radius correct?** (a) do we understand how it actually works; (b) everything it touches — layers/items/consensus paths it ripples into.
- **Q4 — if any "no": the correct answer + why** (source-grounded).

Verdict: **CONFIRMS** (all four yes) / **CORRECTS** (Q1/Q2/Q3 no → Q4 the fix) / **SHARPENS** (right decision, Q3 reveals a load-bearing mechanism detail or an under-stated blast-radius edge to fold in).

Consensus items note the **forge (ECIP-1017/1111) / beacon (ETH requests/withdrawals) build-gate co-sign** — RX confirms source-accuracy of the plan *now*; the byte-exact build validation is theirs.

---

## Group A — block loop / ProtocolSpec bundle

### RX-L4-01 · Family-agnostic block loop + reward as the sole abstract economics seam · Tier A · owner-layer L4
- **Plan claim / disposition:** §2 v1 + §4 — a fork-agnostic block loop reads all behavior off a per-fork bundle; **`rewardCoinbase` is the ONE abstract method** of an otherwise concrete loop; PoS sets `blockReward=ZERO, skipZeroBlockRewards=true`. "ECIP-1017 emission goes exactly where ETH's withdrawals go, so the ETC/ETH split needs no `if(isPoW)` anywhere." **DEFAULT (R1)**, selection fails LOUD.
- **fukuii `july-fourth`:** `BlockExecution`/`BlockPreparator` hold a `ConsensusEngine` field and `ConsensusEngine.finalizeBlock` calls back into `BlockPreparator.payBlockReward` (the `ledger↔consensus` cycle); reward reads `header.isPoS` per block at `payBlockReward:58`.
- **Reference source (byte-cited):**
  - besu `ethereum/core/.../mainnet/AbstractBlockProcessor.java:645-649` — `abstract boolean rewardCoinbase(MutableWorldState, BlockHeader, List<BlockHeader> ommers, boolean skipZeroBlockRewards)` — the **single** abstract method; the entire 400-line `processBlock` (169-564) is concrete.
  - besu `AbstractBlockProcessor.java:88-90` — `final Wei blockReward; final boolean skipZeroBlockRewards;` fields; `:485` — `rewardCoinbase(worldState, blockHeader, ommers, skipZeroBlockRewards)` is the only economics delegation in the loop.
  - besu `MainnetBlockProcessor.java` (concrete `rewardCoinbase`) vs the PoS path — PoS constructs with `Wei.ZERO`+`skipZeroBlockRewards=true` (per `MainnetProtocolSpecs`/merge definition).
  - **besu-etc `ClassicBlockProcessor.java:30,58-90`** — besu's OWN ETC implementation `extends AbstractBlockProcessor` and overrides **only** `rewardCoinbase` to apply ECIP-1017 era emission (`getCoinbaseReward`/`getOmmerReward`). This is the **strongest possible proof of the single-economics-seam DEFAULT**: the same client, the same one abstract method, routes both vanilla-ETH rewards (`MainnetBlockProcessor`) and ECIP-1017 (`ClassicBlockProcessor`) through the identical seam — no `if(isPoW)` in the loop, just a different `rewardCoinbase` override. Same `skipZeroBlockRewards && blockReward.isZero()` guard (`:64-66`).
  - go-ethereum `core/state_processor.go` — the reward is folded into `consensus.Engine.Finalize`; the block loop (`Process`) itself is fork-agnostic and delegates finalize to the engine (verified RX-L4-07).
  - core-geth `params/mutations/rewards.go:66` `AccumulateRewards` — the ETC reward lives in the same Finalize slot (called from the ethash engine's Finalize).
- **Q1 — appropriate decision?** Yes. besu's one-abstract-method shape is exactly the "single economics seam" the plan adopts; DEFAULT is correct — every client that supports both PoW and PoS (besu, core-geth via multi-geth, geth pre/post-merge) routes the family split through this one hook, never scattered branches.
- **Q2 — right thing to build?** Yes — a trait `RewardScheme` in `execution` carried by the bundle is the faithful Scala-3 analog of besu's abstract `rewardCoinbase`. Not NOTHING: the seam is load-bearing (it is the DAG-inversion fix). No better alternative — nethermind's `IRewardCalculator` is the same idea with a quiet-zero trap the plan correctly rejects.
- **Q3 — mechanism + blast radius correct?** Mechanism yes. **Blast radius is larger than §2 states and must be named:** (a) this seam is the physical realization of the `ledger↔consensus` DAG inversion (§6 row 1) — an upward `import consensus.*` re-forms the 13-package SCC, so the seam's placement *in* `execution` is a compile-gated structural invariant, not a stylistic choice; (b) the reward is the **last state mutation before `persistState`** (besu `:485` reward → `:532` persist), so its position ripples into the state root of *every* block — mis-ordering vs. withdrawals/requests is a consensus concern (see RX-L4-07 ordering SHARPENS); (c) `skipZeroBlockRewards` is not cosmetic — on PoS with a zero reward it prevents a spurious empty-coinbase touch that would otherwise dirty an account and shift the state root (besu `:485` passes it through to the abstract method).
- **Q4:** n/a (all yes; blast-radius detail folded via SHARPENS).
- **Verdict:** CONFIRMS (with a SHARPENS folded: name the compile-gated DAG invariant + the `skipZeroBlockRewards` empty-touch-avoidance as a state-root concern in §6/§9, not only §2). **forge/beacon co-sign at build.**

### RX-L4-02 · Immutable per-fork `ProtocolSpec` bundle, resolved ONCE by header, wraps L3 `EvmConfig` · Tier A · owner-layer L4
- **Plan claim / disposition:** §2 v2 + §2.1 + §4 — replace "mid-execution `EvmConfig.forBlock(...)` + branch" with an immutable per-fork bundle built once at startup, looked up by header; the reward is one field; the bundle **wraps L3's single `forBlock(header, schedule)` resolution** so the fork is resolved **once**, not twice. **STRUCTURAL.**
- **fukuii `july-fourth`:** re-derives `EvmConfig.forBlock(...)` at 6+ sites mid-execution (`BlockExecution.scala:151`, `BlockPreparator.scala:226/252/373/413/476`) plus scattered `forkBlockNumbers.*`/`isXTimestamp(...)` inline predicates — no bundle.
- **Reference source (byte-cited):**
  - besu `AbstractBlockProcessor.java:230` — `final ProtocolSpec protocolSpec = protocolSchedule.getByBlockHeader(blockHeader);` — resolved **once** at the top of `processBlock`; every subsequent behavior (`:232` pre-exec processor, `:277` fee market, `:357` gas strategy, `:420` withdrawals processor, `:443` request coordinator, `:498` BAL validator, `:485` reward) is a **method call on that one resolved instance** — zero re-resolution mid-loop.
  - besu `ProtocolSpec.java` — immutable value object; `ProtocolSpecBuilder.java` + `MainnetProtocolSpecs.java` (`shanghaiDefinition` calls `parisDefinition`, mutates the delta) — the "build once, delta from prior fork" pattern; `ScheduledProtocolSpec.java` carries the `MilestoneType` (block-number vs timestamp) dispatch.
  - reth `ForkCondition` / `NodeTypes` — the same "resolve fork once into a typed config" idea in Rust (structural cross-check).
  - go-ethereum `core/state_processor.go` — resolves `p.config` fork rules via `evm.ChainConfig().Rules(...)` per block; besu's precomputed-bundle is the JVM improvement over geth's per-call rule derivation.
- **Q1 — appropriate decision?** Yes. STRUCTURAL is right — this is the layer's chief `july-fourth` gap and besu proves the bundle is the canonical JVM shape. Not OPTIONAL: the 6+ re-derivation sites are a live rot-vector.
- **Q2 — right thing to build?** Yes, and the plan's "wrap L3's one resolution" is the correct scoping — **the bundle must not re-resolve what L3 already resolved.** The multi-client read reveals a YAGNI boundary: besu's `ProtocolSpec` bundles ~30 collaborators, but many (BAL, blob-gas, EIP-7778 dual-gas) are ETH-future — fukuii's bundle should carry only the collaborators its two families actually vary (EvmConfig, reward scheme, system-call processors, validators, fee/base-fee handling), not mirror besu's full field set.
- **Q3 — mechanism + blast radius correct?** Yes. **Blast radius — the "resolve once" invariant is a cross-layer (L3↔L4) contract:** if L3 resolves the fork for the VM *and* L4 re-resolves for rewards/system-calls, that is two resolutions of one fork (the exact shape besu eliminates). This ripples: L3 §2.1 must expose `forBlock(header, schedule)` returning a value the L4 bundle can *hold* (not a factory L4 re-invokes); the DoD grep-invariant "fork bundle resolved once, not re-derived mid-execution" (§8) is the enforcement. Adding a fork = one bundle definition + one milestone entry (besu `MainnetProtocolSpecs` + `milestones/`).
- **Q4:** n/a.
- **Verdict:** CONFIRMS (SHARPENS folded: state the L3-exposes-a-holdable-value / L4-holds-not-re-invokes contract explicitly in §2.1, and scope the bundle to fukuii's actually-varying collaborators, not besu's full ~30).

### RX-L4-03 · One `ProtocolSpec`/executor behind both verify (import) and produce · Tier B · owner-layer L4
- **Plan claim / disposition:** §2 v3 + §4 — "production and validation cannot diverge"; one `ProtocolSpec` behind both `MainnetBlockValidator` (import) and the block-creation path (produce); `getForNextBlockHeader` gives the producer the next spec. **DEFAULT** — `july-fourth` already aligns (`BlockPreparator` backs both), preserve it.
- **fukuii `july-fourth`:** `BlockPreparator` already backs both import and produce (`prepareBlock`/`executeForProposer` reuse `payBlockReward`+`persistState`).
- **Reference source (byte-cited):**
  - besu `AbstractBlockProcessor.java:230` — the same `protocolSchedule.getByBlockHeader(...)` resolution serves the import path; the producer resolves the *next* spec via `ProtocolSchedule.getForNextBlockHeader` (used in the block-creation package) — one schedule, two entry points.
  - reth — a single `Executor` type drives both payload-build and validation (SDK modularity note, structural cross-check).
  - go-ethereum `core/state_processor.go` `Process` is shared; `miner/` builds via the same `ApplyTransaction` primitives.
- **Q1 — appropriate decision?** Yes. DEFAULT/preserve is correct — divergence between produce and verify is a classic consensus-split source; every client shares the path.
- **Q2 — right thing to build?** Yes — build NOTHING new here beyond ensuring the rebuilt executor keeps the shared-path property. The risk is regression, not absence.
- **Q3 — mechanism + blast radius correct?** Yes. Blast radius: the produce path needs the *next* header's spec (fork boundary at N+1), so the bundle lookup must accept a to-be-built header, not only an existing one — a small but real API-shape constraint on the L4 bundle (mirrors besu `getForNextBlockHeader`).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Group B — rewards / ECIP-1017 / economics (all Tier A, consensus-critical)

### RX-L4-06 · ECIP-1017 era emission — byte-exact era math (core-geth SOLE authority) · Tier A · owner-layer L4
- **Plan claim / disposition:** §2 v6 + §5 + §6 + §9 — reproduce byte-exact from core-geth: **era index off-by-one**, **separate integer exponentiation of `4^era` and `5^era`** before multiply-divide, **Era-0-vs-Era-≥1 uncle/nephew formula switch**; replace the `july-fourth` `BigDecimal.precision` reduction-rate derivation with explicit integers. **DEFAULT (non-negotiable).** core-geth is the SOLE living byte-authority.
- **fukuii `july-fourth`:** `rewardReductionRateDenom = BigDecimal(1 - rate).precision * 10` — reconstructs the 4/5 disinflation ratio from a floating config value (flagged `VERIFY:` in the `july-fourth` snapshot). The top L4 correctness hazard.
- **Reference source (byte-cited):**
  - core-geth `params/mutations/rewards.go:109-128` `GetBlockWinnerRewardByEra` — **the byte-critical divisor.** Era 0: `return blockReward` unchanged (`:110-112`). Era ≥1: `q.Exp(DisinflationRateQuotient, era)` and `d.Exp(DisinflationRateDivisor, era)` — **separate integer exponentiation** (`:121-122`) — then `r.Mul(blockReward, q); r.Div(r, d)` (`:124-125`) = `blockReward * 4^era / 5^era`. Comment `:114-115`: "`MaxBlockReward * (4/5)**era == MaxBlockReward * (4**era) / (5**era)`."
  - core-geth `params/config_classic.go:144-145` — `DisinflationRateQuotient = 4`, `DisinflationRateDivisor = 5` (the exact integers; no float anywhere).
  - core-geth `params/vars/protocol_params.go:27` — `FrontierBlockReward = 5e18`; the ECIP-1017 base is **Frontier 5 ETH always** (`rewards_classic.go:28`), NOT the EIP-649/1234 Byzantium(3)/Constantinople(2) reductions — the era schedule is the sole reduction (`5,4,3.2,2.56,...` per the `:33` comment).
  - core-geth `params/mutations/rewards_classic.go:49-62` `GetBlockEra` — **era index off-by-one:** `remainder = (blockNum-1) mod eraLength` (`:55`), `base = blockNum - remainder` (`:56`), `d = base / eraLength` (`:58`), `dremainder = d mod 1` (`:59`, **a no-op — mod-by-1 is always 0**), `return d` (`:61`). Genesis guard `:51` `blockNum.Sign()<1 → 0`. Algebraically **`era = floor((blockNum-1)/eraLength)`** exactly (verified: block 5,000,000 → era 0; block 5,000,001 → era 1).
  - core-geth `rewards_classic.go:81-94` `GetBlockUncleRewardByEra` — **Era-0-vs-≥1 uncle switch:** Era 0 uses the distance formula `(uncle.Number + 8 - header.Number) * blockReward / 8` (`:86-89`); Era ≥1 falls to `getEraUncleBlockReward` = `GetBlockWinnerRewardByEra(era, blockReward) / 32` (`rewards.go:76-78`).
  - core-geth `rewards_classic.go:96-105` `GetBlockWinnerRewardForUnclesByEra` — the winner's **nephew** reward is `getEraUncleBlockReward` = `base(era)/32` **per uncle in all eras** (comment `:102`: "1/32 for winner's uncles remain unchanged from Era 1") — era-adjusted only through the era-scaled base.
  - **SECOND ETC AUTHORITY — besu-etc (besu's own pre-removal ETC, worktree `clients/besu-etc`, commit `eb4248c997`).** besu implemented the ECIPs in the JVM *before* removing ETC; this is the real thing (not vanilla besu's changelog). `ethereum/core/.../mainnet/ClassicBlockProcessor.java` — a byte-for-byte JVM confirmation of every core-geth claim:
    - **Era index** `getBlockEra` (`:105-112`): `long remainder = (blockNumber - 1) % eraLength; long base = blockNumber - remainder; long d = base / eraLength; return Math.toIntExact(d);` — **identical structure to core-geth**, and besu **drops the `mod 1` no-op** and returns `d` directly — an independent second authority corroborating that the era index is integer **DIVISION** and the core-geth `mod 1` (`rewards.go:59`) is dead code.
    - **Separate integer `4^era`/`5^era`** `getBlockWinnerRewardByEra` (`:116-139`): `if (era == 0) return this.blockReward;` else `BigInteger q = BigInteger.valueOf(4).pow(era); BigInteger d = BigInteger.valueOf(5).pow(era); BigInteger r = blockReward.toBigInteger().multiply(q); r = r.divide(d); return Wei.of(r);` — **exactly** core-geth's `q.Exp(4,era); d.Exp(5,era); r.Mul(reward,q); r.Div(r,d)`. Java `BigInteger.divide` and Go `big.Int.Div` both **truncate toward zero on positive operands** → no rounding divergence.
    - **Era-0-vs-≥1 uncle switch** `calculateOmmerReward` (`:94-100`): `if (era < 1) return winnerReward - winnerReward*distance/8; else return winnerReward/32;` (distance = `blockNumber - ommerBlockNumber`, `:145`).
    - **Base = 5 ETH:** `ClassicProtocolSpecs.java:60` `MAX_BLOCK_REWARD = Wei.fromEth(5)` (= 5e18), wired via `.blockReward(MAX_BLOCK_REWARD)` (`:139`); era length from `genesisConfigOptions.getEcip1017EraRounds()` (`:155`), default `5_000_000` (`ClassicBlockProcessor.java:34`).
    - **Same single seam:** `ClassicBlockProcessor extends AbstractBlockProcessor` and overrides **only** `rewardCoinbase` (`:58-90`) — besu's own ETC implementation proves ECIP-1017 slots into the exact `rewardCoinbase` abstract-method seam vanilla ETH uses (RX-L4-01/07).
  - **Absence as positive evidence (the OTHER four clients):** `ecip1017`/`DisinflationRate` appears in **zero** code files of go-ethereum, nethermind, erigon, reth. Two living authorities now agree (core-geth Go + besu-etc JVM); the other four removed ETC. Any cross-reference to a *currently-shipping* de-ETC'd client for an ETC reward value remains an error.
- **Q1 — appropriate decision?** Yes, non-negotiable — it IS ETC consensus. DEFAULT and "core-geth SOLE authority" are both source-confirmed (the absence sweep proves no other client can serve as authority or cross-check).
- **Q2 — right thing to build?** Yes, and the plan's specific fix (drop `BigDecimal.precision`, compute separate integer `4^era`/`5^era`) is exactly what the source does. A cleaner-but-equivalent build detail: the era index should be the direct integer division `era = (blockNum - 1) / eraLength` — do **not** transliterate core-geth's `remainder`/`base`/`mod-1` dance (the `mod 1` at `rewards.go:59` is dead no-op code; `(blockNum-1)/eraLength` is provably identical and clearer).
- **Q3 — mechanism + blast radius correct?** Mechanism yes (all three sub-claims verified against **both** authorities). **Blast radius — the single most consequential item in the layer; the plan under-states it:** the era reward feeds `state.AddBalance(coinbase, minerReward)` (`rewards.go:71`) / `coinbase.incrementBalance(coinbaseReward)` (besu `ClassicBlockProcessor:71`), mutating the state root of **every ETC block**. A wrong divisor, era boundary, or Era-0/≥1 uncle branch → a divergent state root → **a chain split**, surfacing far downstream. Ripples into L2 (commitment), L5 (fork-choice sees a divergent chain), every consensus vector.
  - **NEW two-authority finding (a load-bearing rounding subtlety fukuii MUST handle):** core-geth and besu-etc use **algebraically-equal but arithmetically-different formula shapes** for the two divided rewards, and they coincide byte-for-byte **only because the canonical 5-ETH era reward is divisible by 8 and 32**:
    - *Era-0 uncle reward:* core-geth computes `(uncleNum + 8 - headerNum) * R / 8` (multiply-then-divide); besu computes `R - R*distance/8` (`:97`, subtract form, distance = headerNum − uncleNum). Algebraically both = `(uncleNum+8−headerNum)·R/8`, but under integer truncation they differ by 1 wei **iff R is not divisible by 8**. At era 0, R = 5e18 = 2¹⁸·5¹⁹ **is** divisible by 8 → they agree.
    - *Nephew (winner-includes-uncle) reward:* core-geth adds `floor(R/32)` **per uncle** (`R + ommers·floor(R/32)`); besu computes `R + floor(R·ommers/32)` (`:154`, multiply-then-divide-once). These differ **iff R is not divisible by 32**. The era reward `R = 5e18·4^era/5^era = 2^(18+2era)·5^(19-era)` is divisible by 32 = 2⁵ for every realistic era (18+2era ≥ 5 always) → they agree.
    - **Implication for fukuii:** the two authorities agreeing is *not* an accident of transcription — it is guaranteed by the divisibility of the ECIP-1017 reward schedule. fukuii may reproduce **either** shape and will match ETC mainnet/Mordor, **but must not** assume the equivalence holds for an arbitrary (non-5e18) base reward on a custom network. Pick one canonical form (recommend core-geth's multiply-then-divide, since core-geth is the frozen spec authority) and record *why* the shapes coincide, so a future custom-network reward change doesn't silently pick the diverging path. This is the "BigInteger vs Go big.Int rounding subtlety" — resolved: **no divergence on ETC; latent divergence off-canonical**.
  - *Genesis-guard nuance:* core-geth guards `blockNum.Sign() < 1` (`:51`, → 0 for ≤0); besu guards only `blockNumber < 0` (`:107`) and relies on Java's **sign-preserving `%`** so block 0 still yields era 0 arithmetically (`(0-1)%L = -1`, base = 1, d = 0). Scala's `%` is likewise JVM sign-preserving, so a Scala port matches besu; moot in practice (genesis pays no reward), but note it so a "fix" to the guard doesn't introduce a discrepancy.
- **Q4:** n/a — a strengthened two-authority CONFIRMS with SHARPENS (below).
- **Verdict:** **CONFIRMS — now a strong TWO-authority CONFIRMS** (core-geth Go `rewards_classic.go`/`rewards.go` + besu-etc JVM `ClassicBlockProcessor.java` agree byte-for-byte on ETC). **Three SHARPENS to fold in:** (1) **the era index is integer DIVISION `(blockNum-1)/eraLength`, not `mod`** — §2 v6 and §9 shorthand it as "`(blockNum-1) mod eraLength`" (the intra-era *remainder*, an intermediate); §5 correctly says `/eraDuration`. Both authorities confirm DIV; besu drops the core-geth `mod 1` no-op entirely — align §2/§9 to §5. (2) **The ECIP-1017 base is 5 ETH (`FrontierBlockReward` / besu `Wei.fromEth(5)`) with era-only step-downs** — the §8 phrase "Byzantium/Constantinople base step-downs" must not be read as EIP-649/1234 (3/2 ETH); the step-downs to test are the era schedule `5→4→3.2→2.56`. (3) **Add the divisibility rationale + one-canonical-form directive** (above) to §6/§9 so the multiply-vs-divide formula-shape choice is deliberate and the off-canonical divergence is documented. **forge gate at build — validate byte-exact against BOTH `rewards_test.go` (Go) AND besu-etc's classic reward vectors (JVM), the strongest cross-check available.**

### RX-L4-07 · Reward/finalize seam — go-ethereum `Engine.Finalize` / besu `rewardCoinbase`; PoS zeroes it · Tier A · owner-layer L4
- **Plan claim / disposition:** §3 + §4 — the reward/finalize is the sole reward hook (`consensus.Engine.Finalize` in geth; besu `AbstractBlockProcessor.rewardCoinbase` the one abstract method); ETC keeps ECIP-1017 in this slot, ETH's beacon Finalize zeroes it. **DEFAULT (R1).**
- **fukuii `july-fourth`:** `ConsensusEngine.finalizeBlock` calls `BlockPreparator.payBlockReward`; reward reads `header.isPoS` per block.
- **Reference source (byte-cited):**
  - go-ethereum `core/state_processor.go` — after the tx loop, `p.chain.engine.Finalize(...)` applies rewards/withdrawals; for post-merge the beacon engine's `Finalize` is a no-op for issuance (reward = 0, withdrawals applied by the state processor). (Full read RX-L4-13 for the withdrawals path.)
  - core-geth `params/mutations/rewards.go:64-72` `AccumulateRewards` — `state.AddBalance(uncle.Coinbase, uncleRewards[i])` per uncle, then `state.AddBalance(header.Coinbase, minerReward)` — called from the ethash engine's `Finalize`. The ETC reward occupies exactly the geth Finalize slot.
  - **besu-etc `ClassicBlockProcessor.java:67-88`** — the JVM confirmation: `coinbase.incrementBalance(coinbaseReward)` then per-ommer `ommerCoinbase.incrementBalance(ommerReward)`, inside the `rewardCoinbase` override that AbstractBlockProcessor calls at its one reward-seam site (`:485`). ETC's reward lives in the *identical* structural slot as ETH's — second-authority proof the reward/finalize seam is family-agnostic.
  - besu `AbstractBlockProcessor.java:485` — `rewardCoinbase(...)` is the one delegation; PoS constructs with `Wei.ZERO`+`skipZeroBlockRewards=true` so the abstract method short-circuits (no coinbase touch).
  - nethermind `IRewardCalculator` — same seam shape; its quiet-zero path is the trap the plan bans (`fail-loud-invariants.md`).
- **Q1 — appropriate decision?** Yes — one seam, family-selected. DEFAULT confirmed across geth/besu/core-geth/nethermind (all put issuance in one finalize/reward hook).
- **Q2 — right thing to build?** Yes — a `sealed trait RewardScheme` in `execution`, selected by the bundle, `Ecip1017RewardScheme` vs `PosNoRewardScheme`. Fail-LOUD on unresolved selection (the nethermind trap is real and named).
- **Q3 — mechanism + blast radius correct?** Mechanism yes. **Blast radius — ordering (a SHARPENS):** besu applies the reward **after** withdrawals and requests (`AbstractBlockProcessor.java:419` withdrawals → `:443` requests → `:485` reward → `:532` persist), whereas plan §1 lists the order as "reward/finalize seam → post-execution system calls / withdrawals → state commitment" (reward *before* post-exec). The order is **functionally inert today** (PoW has a nonzero reward but no withdrawals/requests; PoS has withdrawals/requests but a zero reward — the two never both mutate state in one block), so it is not a live bug — but the plan's stated apply order **contradicts the reference** and would become a real state-root divergence if a future family ever had both a nonzero reward and withdrawals. Also: withdrawals/reward touch disjoint accounts (validator addrs vs coinbase), so "withdrawals apply outside the reward seam, never double-credited" (§9) is confirmed.
- **Q4:** n/a — CONFIRMS with the ordering SHARPENS.
- **Verdict:** CONFIRMS, **SHARPENS:** correct §1's apply order to besu's canonical `tx-loop → withdrawals → requests → reward → persistState` (reward is the last state mutation before commitment); note it is currently inert but is the reference order and the safe one. **forge/beacon co-sign at build.**

### RX-L4-08 · Per-block merge decorator principle (nethermind `MergeRewardCalculator`) — reward read per block · Tier A · owner-layer L4
- **Plan claim / disposition:** §2 v5 + §6 — decide the reward per block via an `IsPostMerge`-style predicate, correct across cross-boundary reorgs; **fukuii already satisfies this without a decorator object** — the reward reads `header.isPoS` per block; re-express as the injected `RewardScheme` selected per header, state the per-block guarantee, do **not** add a decorator fukuii doesn't need. **DEFAULT-adjacent (per-block-transition principle).**
- **fukuii `july-fourth`:** `BlockPreparator.payBlockReward:58` reads `header.isPoS` per block — already per-block-correct.
- **Reference source (byte-cited):**
  - nethermind `Consensus/Rewards/MergeRewardCalculator` (and `NoBlockRewards`) — wraps a base calculator, returns zero once `IsPostMerge(header)` — the decision is **per block/header**, not a set-once boolean. This is the DEFAULT-adjacent principle.
  - besu `AbstractBlockProcessor.java:230+485` — the spec (and thus reward) is re-resolved `getByBlockHeader` **every block**, so a cross-boundary reorg re-picks the correct fork's reward per block — the same guarantee besu gets structurally from per-header spec lookup.
  - core-geth — PoW-only, no merge decorator (absence: ETC never merges, so the principle is inert for ETC — positive evidence the plan's "inert for ETC" note is right).
- **Q1 — appropriate decision?** Yes — DEFAULT-adjacent (principle, not object) is precisely right: adopt the per-block guarantee, reject the decorator object. The source shows the guarantee is what matters, and besu achieves it via per-header spec lookup (no decorator), validating "satisfied without a decorator."
- **Q2 — right thing to build?** Build NOTHING extra — express the existing per-block `header.isPoS` read as the bundle-selected `RewardScheme` and *document* the per-block guarantee. Adding a nethermind-style decorator object would be YAGNI (besu proves the per-header lookup already gives the reorg-correctness).
- **Q3 — mechanism + blast radius correct?** Yes. Blast radius: the guarantee is only meaningful if the bundle is re-resolved per block (RX-L4-02) — i.e. this item depends on the "resolve once per block, by header" mechanism; if the bundle were resolved once per *chain* it would break merge-reorg correctness. So RX-L4-08 is a corollary of RX-L4-02's per-header resolution, worth stating as the linkage.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L4-09 · EIP-1559 base-fee burn / tip split (ETH) — go-ethereum authority · Tier A · owner-layer L4
- **Plan claim / disposition:** §3 + §7 + §9 — ETH burns the base fee, tip to coinbase; **ETC does not burn** (ECIP-1111 routes base fee to treasury — RX-L4-10). go-ethereum `consensus/misc/eip1559/eip1559.go` authority. DEFAULT (implicit), tested via burn-accounting (ETH) vs treasury-routing (ETC) divergence.
- **fukuii `july-fourth`:** EIP-1559 handled inline in `BlockPreparator` with per-site fork predicates.
- **Reference source (byte-cited):**
  - go-ethereum `consensus/misc/eip1559/eip1559.go` — `CalcBaseFee` computes the next block's base fee from parent gasUsed vs target; the base fee is **burned** (never credited) — the tx `effectiveTip = min(tip, feeCap - baseFee)` goes to the coinbase, `baseFee * gasUsed` is removed from supply. (Cross-check: `state_transition.go` credits only `effectiveTip*gasUsed` to the coinbase; the base-fee portion is not added anywhere → burned.)
  - besu London `FeeMarket`/`LondonFeeMarket` — same burn semantics (JVM lens).
  - core-geth — ETC has EIP-1559 gas-target math for base-fee *computation* but ECIP-1111 changes the *disposition* (to treasury, not burn) on the ETC path — the burn-vs-route divergence is real and network-specific.
- **Q1 — appropriate decision?** Yes — go-ethereum authority for the ETH burn/tip is correct; the base-fee *computation* is shared, the *disposition* (burn vs treasury) is the family split.
- **Q2 — right thing to build?** Yes — the base-fee handling is one collaborator of the bundle (per family): ETH burns, ETC routes-to-treasury. Not a scattered per-site predicate (the `july-fourth` gap).
- **Q3 — mechanism + blast radius correct?** Yes. **Blast radius:** the base-fee disposition mutates the state root (burn = supply removed and NOT credited; treasury = credited to a specific address) — a family-specific consensus value. It couples to RX-L4-10 (ECIP-1111): ETH and ETC feed the *same* base-fee-computation input but diverge at disposition, so the bundle must carry the disposition as a family field, and the divergence must be tested on identical base-fee input (§7).
- **Q4:** n/a.
- **Verdict:** CONFIRMS. **beacon co-signs the ETH burn; forge/banksy the ETC treasury route (RX-L4-10).**

### RX-L4-10 · ECIP-1111 base-fee floor + treasury routing (ETC Olympia) — fukuii's own work · Tier A · owner-layer L4
- **Plan claim / disposition:** §3 + §7 + §9 — **fukuii's own forward work, no ref-client equivalent**; validate against the ECIP-1111 draft + core-geth fukuii `main` overlay. **forge owns, banksy required consult** (security-budget economics). RX verifies only that **no ref client contradicts it**.
- **fukuii `july-fourth`:** partial/planned Olympia overlay; base-fee-to-treasury is forward work.
- **DRAFT-spec note (malleability):** ECIP-1111 is **fukuii's own forward work, status: Draft** — the design itself is in scope. Q1/Q2 evaluate whether the *draft design* is the right one vs. best practice (go-ethereum EIP-1559 + Ronin precedent), not merely whether the plan matches the draft. Any proposed spec change is surfaced source-grounded and **flagged for forge (economics) / banksy (ECIP-1122 tip-floor interaction) / operator sign-off** — RX does not rewrite the spec.
- **Reference source (byte-cited):**
  - **ECIP-1111 draft (the authority)** `/media/dev/2tb/dev/ECIPs/_specs/ecip-1111.md`: redirect pseudocode `:99-110` — `baseFeeAmount = block.gasUsed * block.baseFee`; `state.addBalance(treasuryAddress, baseFeeAmount)`; then `state.addBalance(block.miner, block.totalPriorityFees)`; `:120` "**The Treasury transfer occurs before miner rewards are applied**." Floor `:126-132` — `baseFee = max(computedBaseFee, INITIAL_BASE_FEE)`, `INITIAL_BASE_FEE = 1,000,000,000 wei (1 gwei)`, applied **after** the EIP-1559 adjustment and **before** header inclusion (`:143`); floor is **chain-config, not hardcoded per consensus mechanism** (`:143`). Precedent `:136` — Ronin enforces the same 1-gwei floor. Monetary-policy invariant `:162-164` — total supply/issuance (ECIP-1017) unchanged.
  - **No ref-client equivalent — confirmed by absence:** base-fee-to-treasury routing appears in no vendored client's code (go-ethereum/besu/nethermind/erigon/reth all *burn* per EIP-1559). Nothing to byte-mirror; the draft + core-geth `config_classic.go`/`config_mordor.go` overlay are the only authorities.
  - go-ethereum `consensus/misc/eip1559/eip1559.go:60-102` `CalcBaseFee` — the ETH computation ECIP-1111 reuses **unchanged** except disposition: proportional adjustment `parentBaseFee * gasUsedDelta / target / denom` (`:79-94`), ETH **floors at 0** (`:97-99` `if baseFee < 0 { baseFee = 0 }`). ECIP-1111 swaps the 0-floor for a 1-gwei clamp — a single-line, localized change.
  - go-ethereum `core/state_transition.go` — ETH credits only `effectiveTip*gasUsed` to the coinbase; `baseFee*gasUsed` is removed and **not** credited (burned). ECIP-1111 credits that same `baseFee*gasUsed` to the treasury — value-conserving (what leaves senders = what enters treasury).
- **Q1 — appropriate decision? IS THE DRAFT DESIGN RIGHT?** Yes on both. (a) The RX disposition ("fukuii's own, verify no ref contradicts") is correct. (b) **The draft design is best-practice-aligned:** treasury-redirect-instead-of-burn matches a live precedent (Ronin, `:136`); the 1-gwei floor as a post-adjustment `max()` clamp is the standard pattern; reusing go-ethereum's `CalcBaseFee` verbatim and changing only the disposition + floor is the minimal, correct surface (the draft's own "single consensus deviation" claim, `:427`, is accurate).
- **Q2 — right thing to build (vs. a better alternative / vs. nothing)?** Yes — a family-specific base-fee-**disposition** collaborator on the bundle (the same seam as RX-L4-09's ETH burn: one field, `Burn` vs `RedirectToTreasury(addr)`), plus a config-driven base-fee floor. **No better alternative;** the per-tx-vs-lump-sum choice is arithmetically neutral (see Q3). Building it is not YAGNI — it is the core of the Olympia fork and the ECIP-1112 treasury's only revenue source.
- **Q3 — mechanism + blast radius correct?** Mechanism yes (draft-defined; cross-checked value-conserving against geth). **Two mechanism/ blast-radius details to fold in (SHARPENS), one a proposed spec clarification:** (i) **The treasury-credit-vs-miner-reward ordering (`:120`) is state-root-COMMUTATIVE, not a consensus-divergence axis** — treasury and miner are distinct addresses and all three finalize credits (treasury base-fee, ECIP-1017 miner reward, miner tips) are additive `addBalance`s, so the *final balances are order-independent*. Keep a deterministic order for clarity, but the plan §7 must **not** frame "treasury-before-miner ordering" as a consensus risk; the real consensus risks are the **amount** (`gasUsed*baseFee`) and the **floor**. (ii) **`block.gasUsed * block.baseFee` (lump at finalize) is identical to the per-tx sum ONLY because `baseFee` is constant within a block** — worth stating in the spec's implementation notes so an implementer doesn't compute it per-tx against a varying value or double-count with the per-tx base-fee charge. (iii) **Blast radius:** ECIP-1111 sets the security-budget economics ECIP-1122's `MIN_MINER_TIP` (banksy) is sized against (`:148-158`) → the forge↔banksy co-review interface; the base-fee **floor** feeds the next block's `CalcBaseFee` parent input (floored parent → floored child), so the floor must be applied before header inclusion (draft `:143` — correct) or every subsequent block's base fee diverges.
- **Q4 — correct answer + why:** No CORRECTS to the draft design (it is sound). **Proposed spec CLARIFICATIONS (flagged for forge/operator sign-off, not RX-applied):** (1) add an implementation note that the redirect amount is computed lump-sum `block.gasUsed * block.baseFee` and is equal to the per-tx base-fee sum *because baseFee is block-constant* — to forestall per-tx double-counting; (2) soften/annotate the "Treasury transfer occurs before miner rewards" wording (`:120`) to note it is a determinism convention, not a state-root-affecting order (the amounts, not the sequence, are what MUST match cross-client). Both are wording clarifications, not design changes; **route to forge (ECIP-1111 economics) + operator.**
- **Verdict:** CONFIRMS (draft design is best-practice-aligned; no ref-client contradiction). **SHARPENS folded into §7:** reframe the "ordering" vectors as commutative-determinism (amount + floor are the consensus axes); add the block-constant-baseFee lump-sum note. **forge owns + banksy required consult + operator sign-off for the two proposed spec clarifications; not byte-verifiable against a reference client (none exists) — validated against the draft.**

---

## Group C — transaction processing

### RX-L4-04 · Per-tx engine (upfront deduction, VM run, EIP-3529 refund, EIP-7623 floor, EIP-7702 auth, tip settlement) · Tier B (gas-accounting sub-parts Tier-A-adjacent) · owner-layer L4
- **Plan claim / disposition:** §1 + §3 + §4 — the per-tx engine: upfront gas deduction, EIP-7702 authorizations, VM run (into L3), refund cap (EIP-3529), EIP-7623 floor, tip-vs-base-fee settlement, account cleanup. Authority go-ethereum `core/state_processor.go`+`state_transition.go`; besu `MainnetTransactionProcessor` the JVM structural mirror. DEFAULT.
- **fukuii `july-fourth`:** tx-apply body in `BlockPreparator` with the `@volatile` simulation flags (RX-L4-23).
- **Reference source (byte-cited):**
  - go-ethereum `core/state_processor.go:99-118` — the tx loop: `TransactionToMessage(tx, signer, header.BaseFee)` (`:100`) → `ApplyTransactionWithEVM(...)` (`:109`) → `ApplyMessage(evm, msg, gp)` (`:213`). The gas engine is `state_transition.go`.
  - go-ethereum `core/state_transition.go:190` `FloorDataGas` + `:672-687` — **EIP-7623 calldata floor**: `if msg.GasLimit < floorDataGas { return ErrFloorDataGas }`, and Amsterdam `max(cost.RegularGas, floorDataGas) > MaxTxGas` guard.
  - go-ethereum `core/state_transition.go:527-740` — **EIP-7702** set-code-tx: authorization-list well-formedness (`:622-625`), `Apply EIP-7702 authorizations` (`:740`).
  - go-ethereum `core/state_transition.go:37-38` — `UsedGas` (refund-deducted) vs `MaxUsedGas` (pre-refund) — the EIP-3529 refund cap distinction.
  - besu `MainnetTransactionProcessor.java` — the JVM per-tx engine (upfront deduction, VM run, refund, receipt) — fukuii's tx-apply structural mirror.
- **Q1 — appropriate decision?** Yes — DEFAULT; the per-tx engine is standard and shared. go-ethereum authority + besu JVM mirror is the right pairing.
- **Q2 — right thing to build?** Yes. Note: EIP-7623/7702 belong to the **fork bundle's** gas config (resolved once, RX-L4-02), not per-site predicates — the plan already routes them through the bundle (§6 row 4).
- **Q3 — mechanism + blast radius correct?** Mechanism yes. **Blast radius:** every gas sub-rule here (7623 floor, 3529 refund cap, 7702 auth gas) is a **consensus value** — a wrong refund cap or floor shifts `gasUsed` → the receipts-root and the block's `gasUsed` commitment → a state/consensus divergence. So although the *structure* is Tier-B, the individual gas constants are Tier-A and route through forge (ETC schedule) / beacon (ETH schedule) at build. The tip-vs-base-fee settlement couples to RX-L4-09 (only `effectiveTip*gasUsed` credited to coinbase; base fee burned/treasury-routed).
- **Q4:** n/a.
- **Verdict:** CONFIRMS (SHARPENS folded: flag that the per-tx gas constants — 3529/7623/7702 — are individually Tier-A consensus values gated by forge/beacon at build, even though the tx-engine *structure* is Tier-B).

### RX-L4-05 · EIP-658 typed receipts + bloom + receipts-root · Tier B · owner-layer L4
- **Plan claim / disposition:** §5 + §6 "carried forward unchanged" — receipts typed by `enum TxType` (Legacy / Type01–04), EIP-658 status receipts, bloom, receipts-root. DEFAULT/preserve.
- **fukuii `july-fourth`:** EIP-658 typed receipts already correct (§6 "carried forward").
- **Reference source (byte-cited):**
  - go-ethereum `core/state_processor.go:233-268` `MakeReceipt` — `receipt.Type = tx.Type()` (`:239`, typed); **EIP-658 status**: `if result.Failed() { receipt.Status = ReceiptStatusFailed } else { ReceiptStatusSuccessful }` (`:240-244`); `receipt.Bloom = types.CreateBloom(receipt)` (`:263`); `CumulativeGasUsed` (`:239`).
  - go-ethereum `core/state_processor.go:218-223` — **pre-Byzantium vs Byzantium+ split**: Byzantium+ uses `Finalise(true)` (status receipts, no per-tx intermediate root); pre-Byzantium computes `IntermediateRoot(...)` per tx as the receipt `PostState` — the EIP-658 transition point.
  - besu `AbstractBlockProcessor.java:382-388` — `transactionReceiptFactory.create(transaction.getType(), result, worldState, cumulativeReceiptGasUsed)` — typed receipt per tx.
- **Q1 — appropriate decision?** Yes — DEFAULT/preserve; receipts are standard and shared.
- **Q2 — right thing to build?** Yes — `enum TxType` typed receipts. Build the pre-Byzantium `PostState`-root vs Byzantium+ `Status` split (the EIP-658 boundary) since ETC's schedule (Agharta+) is post-Byzantium but the ETC pre-Atlantis history requires the PostState form for old blocks if full-history validation is in scope.
- **Q3 — mechanism + blast radius correct?** Yes. Blast radius: the receipt shape feeds the receipts-root commitment (§8 DoD); typed-tx (EIP-2718) aggregation is an L1 `domain` DoD item (§7 cross-ref) the receipt consumes — the boundary is correctly placed at L1.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Group D — system calls / EIP-7685 requests (Tier A, consensus; beacon-gated)

### RX-L4-11 · Two-phase system calls bracketing the tx loop (PreExecution 4788/2935; PostExecution) · Tier A · owner-layer L4
- **Plan claim / disposition:** §2 v4 + §5 — `PreExecution(4788, 2935)` before txs; `PostExecution(withdrawals, 7002, 7251, 6110)` after; modelled as pseudo-txs with the `SystemAddress` sender and the fixed 30M budget. DEFAULT.
- **fukuii `july-fourth`:** two-phase bracketing already present (§6 "carried forward"); the loop body is hard-coded (§6 row 5 → RX-L4-12).
- **Reference source (byte-cited):**
  - go-ethereum `core/state_processor.go:96` — `PreExecution(...)` called **before** the tx loop (`:99`); `PreExecution` (`:140-158`) runs **EIP-4788** beacon-root (`:150-152`) + **EIP-2935** parent-block-hash (`:154-156`).
  - go-ethereum `core/state_processor.go:119` — `PostExecution(...)` called **after** the tx loop; then `Engine().Finalize(...)` (`:129`).
  - go-ethereum `core/state_processor.go:284-296` `systemCallGasBudget` — `gasLimit = 30_000_000` (the fixed 30M budget); sender `params.SystemAddress` (`:309`, `:339`, `:394`).
  - besu `AbstractBlockProcessor.java:265-267` — `protocolSpec.getPreExecutionProcessor().process(...)` before the tx loop (`:297`); post-loop withdrawals (`:419`) → requests (`:443`).
- **Q1 — appropriate decision?** Yes — DEFAULT; the two-phase bracket is the canonical shape in both authorities.
- **Q2 — right thing to build?** Yes — explicit `PreExecution`/`PostExecution` phases modelled as `SystemAddress`/30M-budget pseudo-txs (§5). Both authorities agree on the bracket and the budget.
- **Q3 — mechanism + blast radius correct?** Mechanism yes. **Blast radius — an ordering SHARPENS the plan omits:** the *internal* order of the post-phase differs between the two authorities. go-ethereum runs **requests (PostExecution: 6110 → 7002 → 7251) THEN withdrawals+reward (in `Engine.Finalize`)** (`:119` then `:129`); besu runs **withdrawals (`:419`) THEN requests (`:443`) THEN reward (`:485`)**. Functionally commutative today (withdrawals credit validator balances; requests are system calls on disjoint queue-contract state; deposit-scrape only reads receipts) → same state root — but the two references genuinely differ, so the plan's §5 post-phase ordering (`withdrawals, 7002, 7251, 6110`) must be a **deliberate** pick of one reference, not left ambiguous. Recommend besu's order (withdrawals → requests → reward) since it matches the single-block-processor JVM structure fukuii mirrors, and note it is state-root-commutative with geth's for the current two families.
- **Q4:** n/a.
- **Verdict:** CONFIRMS, **SHARPENS:** fold the post-phase ordering choice into §5/§1 explicitly (pick besu's withdrawals→requests→reward; note commutativity with geth). **beacon co-signs at build.**

### RX-L4-12 · `RequestType`→processor map + `noOp` degradation (besu `RequestProcessorCoordinator`) · Tier A · owner-layer L4
- **Plan claim / disposition:** §2 v4 + §4 + §5 — adopt besu's data-driven `RequestType`→processor map with `noOp()` degradation for pre-Prague/PoW; `enum RequestType { Deposit; Withdrawal; Consolidation }` + a `given Map[RequestType, RequestProcessor]`; replaces the `july-fourth` hard-coded `processPragueSystemCalls` loop. DEFAULT.
- **fukuii `july-fourth`:** `processPragueSystemCalls`/`collectDepositRequests` inline, no registry (§6 row 5).
- **Reference source (byte-cited):**
  - besu `.../requests/RequestProcessorCoordinator.java:32` — `ImmutableSortedMap<RequestType, RequestProcessor> processors`; `process` (`:44-50`) streams the map values and calls each; **`noOp()` (`:52-54`) returns an empty-map coordinator** — the degradation.
  - besu `RequestProcessorCoordinator.java:66-73` — `build()` **throws `IllegalStateException` on an empty map** — you cannot *accidentally* get an empty coordinator; the empty/degraded path is the **explicit named `noOp()` factory** only. This is a fail-loud design that reinforces the plan's fail-LOUD philosophy (§6 row 6).
  - besu `.../requests/SystemCallRequestProcessor.java:29-60` — the reusable "call a system contract, wrap output as a `Request`" primitive: `process` (`:50`) → `systemCallProcessor.process(callAddress, context, ...)` → `new Request(requestType, systemCallOutput)` (`:60`).
  - go-ethereum `core/state_processor.go:385-424` `processRequestsSystemCall` — the Go equivalent: prefixes the requestType byte (`:420` `requestsData[0] = requestType`) onto the system-call return.
- **Q1 — appropriate decision?** Yes — DEFAULT; besu's data-driven map is the cleanest form and directly closes the `july-fourth` hard-coded-loop gap.
- **Q2 — right thing to build?** Yes — `enum RequestType` + `given Map[RequestType, RequestProcessor]`, `noOp` for PoW/pre-Prague. **A SHARPENS the plan should adopt from the source:** besu's `build()` refuses an empty map and forces `noOp()` to be *explicit* — fukuii's Scala map should mirror this (an empty processor map is only reachable via an explicit `RequestProcessors.noOp`, never a silent empty `Map()`), consistent with fail-LOUD.
- **Q3 — mechanism + blast radius correct?** Yes. Blast radius: the map is a **bundle collaborator** (RX-L4-02) selected per fork — PoW/pre-Prague binds `noOp`, Prague+ binds the {Deposit, Withdrawal, Consolidation} map. On ETC this must be `noOp` (withdrawals/requests hard-rejected, §9) — a mis-bind that put a live processor on the ETC path is a consensus divergence.
- **Q4:** n/a.
- **Verdict:** CONFIRMS, **SHARPENS:** adopt besu's "empty map only via explicit `noOp()`, `build()` fails loud on accidental empty" as the Scala idiom (folds into §5/§6). **beacon co-signs at build.**

### RX-L4-13 · Withdrawals (EIP-4895) post-tx-loop, outside the reward seam · Tier A · owner-layer L4
- **Plan claim / disposition:** §3 + §9 — withdrawals apply after the tx loop, *outside* the reward seam (never double-credited with issuance); ETC hard-rejects them (`noOp`). beacon path. DEFAULT.
- **fukuii `july-fourth`:** ETH withdrawals handled; ETC rejects.
- **Reference source (byte-cited):**
  - besu `AbstractBlockProcessor.java:419-437` — `protocolSpec.getWithdrawalsProcessor()`; if present + withdrawals present, `processWithdrawals(maybeWithdrawals.get(), worldState.updater(), ...)` — **after** the tx loop, **before** `rewardCoinbase` (`:485`). On PoW `getWithdrawalsProcessor()` is empty → skipped (the `noOp` equivalent).
  - go-ethereum `core/state_processor.go:129` — validator withdrawals applied inside `Engine().Finalize(..., block.Body(), ...)` (post-PostExecution); the beacon engine credits withdrawal balances, issuance reward = 0 (disjoint from the reward).
  - core-geth — ETC ethash `Finalize` has **no** withdrawals processing (absence: ETC rejects EIP-4895; positive evidence for the hard-reject claim).
- **Q1 — appropriate decision?** Yes — DEFAULT; withdrawals are a per-family bundle collaborator, empty on PoW.
- **Q2 — right thing to build?** Yes — a `WithdrawalsProcessor` bundle field, absent (`noOp`) on the PoW path; applied outside the `RewardScheme` seam.
- **Q3 — mechanism + blast radius correct?** Yes. **Blast radius:** withdrawals credit validator withdrawal addresses (disjoint from coinbase), so "outside the reward seam, never double-credited" (§9) is confirmed. ETC must hard-reject: a silently-applied withdrawal on ETC mutates state → chain split (§9 flag). The ordering vs. requests is the RX-L4-11 SHARPENS.
- **Q4:** n/a.
- **Verdict:** CONFIRMS. **beacon co-signs at build.**

### RX-L4-14 · EIP-7685 `requestsHash` (SHA256 fold) + EIP-6110 deposit-scrape + EIP-7002/7251 system calls · Tier A · owner-layer L4
- **Plan claim / disposition:** §7 — `requestsHash` double-SHA256 fold vectors; deposit log-scrape from receipts; `SystemAddress` 30M-gas system-call vectors. beacon gate.
- **fukuii `july-fourth`:** hard-coded `collectDepositRequests` (§6 row 5).
- **Reference source (byte-cited):**
  - besu `.../mainnet/BodyValidation.java:82` `requestsHash` — **the SHA256 fold**: for each non-empty request `requestHashes.add(sha256(request.getEncodedRequest()))`, then `return sha256(Bytes.wrap(requestHashes))` — i.e. `sha256( sha256(req_0) ‖ sha256(req_1) ‖ … )`; **empty requests excluded from the hash**.
  - go-ethereum `core/types/block.go:480` `CalcRequestsHash` — the same fold; `core/types/hashes.go:43` `EmptyRequestsHash = sha256("")` (the known empty-set hash).
  - go-ethereum `core/state_processor.go:172-186` — **EIP-6110** `ParseDepositLogs` (`:175`, `:426-445`: matches `depositTopic` on `DepositContractAddress`, first byte `0x00`), **EIP-7002** `ProcessWithdrawalQueue` (`:179`, type `0x01`), **EIP-7251** `ProcessConsolidationQueue` (`:183`, type `0x02`) — each a `SystemAddress` 30M-budget system call (`:392-405`) prefixing its type byte (`:420`).
  - besu `AbstractBlockProcessor.java:466-483` — validates `BodyValidation.requestsHash(maybeRequests)` against `header.getRequestsHash()`, fail-loud on mismatch.
- **Q1 — appropriate decision?** Yes — the request phases are per-fork bundle collaborators; DEFAULT/scheduled to L4 with beacon gate is correct.
- **Q2 — right thing to build?** Yes. The plan's "double-SHA256 fold" phrasing is accurate: **inner `sha256` per request, outer `sha256` over the concatenation, empty requests excluded** — build to that exact spec (both authorities agree byte-for-byte).
- **Q3 — mechanism + blast radius correct?** Mechanism yes (verified in both authorities). **Blast radius:** `requestsHash` is a **header commitment** (EIP-7685) — a wrong fold or a failure to exclude empty requests produces a header-hash mismatch → block rejected by the network (consensus divergence). The deposit-scrape reads **receipts** (`allLogs`), so it depends on RX-L4-05 receipt/log correctness; the 7002/7251 calls read queue-contract storage → depend on L3 EVM. ETC binds `noOp` (no requests) → `EmptyRequestsHash` never appears in an ETC header (ETC has no `requestsHash` field). 
- **Q4:** n/a.
- **Verdict:** CONFIRMS, **SHARPENS:** record the exact fold spec (inner-per-request sha256, outer sha256 over concat, **empty requests excluded**, `EmptyRequestsHash = sha256("")`) as a §7 DoD vector detail so it isn't rediscovered. **beacon gate at build.**

---

## Group E — per-block outcome / reorg-aware event source (Tier A structural spine, R7)

### RX-L4-15 · Per-block `BlockExecutionOutcome` + serializable state-diff (besu `BonsaiTrieLog {prior,updated}`; reth ExEx) · Tier A · owner-layer L4
- **Plan claim / disposition:** §1 + §4 + §5 + §7 — L4 computes a per-block serializable state-diff (besu `BonsaiTrieLog {prior,updated}` leaf-diff shape) and emits `enum BlockExecutionOutcome { Executed(block, stateDiff); RolledBack(block) }`; **L5 owns the reorg-aware `ChainNotification`** (it decides reorgs) — L4 supplies per-block outcomes, not the wire ADT. **STRUCTURAL (R7, L4's half).**
- **fukuii `july-fourth`:** no reorg-aware event source; node-refcount snapshots are not a clean serializable diff (`optimizations.md` L2); reorgs surface only as sync plumbing.
- **Reference source (byte-cited):**
  - besu `.../trie/pathbased/bonsai/trielog/BonsaiTrieLogFactory.java:44-66` — builds a trie-log layer from the world-state accumulator; each account/code/slot change carries **`getPrior()` and `getUpdated()`** (`:65-66`) — the `{prior,updated}` serializable leaf-diff the plan mirrors.
  - besu `AbstractBlockProcessor.java:46,338-339,532` — the `BonsaiWorldStateUpdateAccumulator` is the source of the diff; `worldState.persist(...)` (`:532`) commits it; a failed block `reset()`s the accumulator (`:339`) — the diff is produced exactly at the state-transition point, which is L4's job.
  - reth `crates/chain-state/src/` — `CanonStateNotification` (committed/reverted `Chain`) is the reorg-aware **segment** stream; the plan correctly places this shape at **L5** (reth's ExEx emits it from the chain/fork-choice layer, not the per-block executor) — cross-check confirming the L4/L5 split.
- **Q1 — appropriate decision?** Yes — STRUCTURAL and the L4/L5 split is source-justified: besu computes the diff at execution (`BonsaiTrieLog` from the execution accumulator), reth emits the *reorg segmentation* from the chain-state layer (≈ L5). Placing diff-computation at L4 and reorg-segmentation at L5 matches where each client actually does the work.
- **Q2 — right thing to build?** Yes — emit the per-block outcome now (the seam), even though the reorg-aware consumer is L5's. Building only L4's half now is correct scoping (not YAGNI: R7's product family — pool/validator/GUI/dRPC — needs it; not over-build: the wire ADT is deferred to L5 where reorg authority lives).
- **Q3 — mechanism + blast radius correct?** Yes. **Blast radius is cross-layer and the plan maps it well:** L4 emits `BlockExecutionOutcome` → L5 aggregates into `ChainNotification` (reth 3-case segments) → L9 grpc-seam transports → L2 gates pruning on `min(consumer heights)` via `FinishedHeight`. So the state-diff *shape* L4 picks constrains L5's ADT, L9's payload, and L2's prune barrier — a 4-layer ripple. The diff must be **byte-reproducible** (§7 DoD) because a consumer replays it; and it must be computed from the same accumulator that produces the state root (besu ties them together at `persist`), so the diff and the committed root cannot disagree.
- **Q4:** n/a.
- **Verdict:** CONFIRMS. (Structural — coheres with L5 §6.5 / L9 §5 / L2 pruning; no Wave-3 re-pass needed, the split is unchanged.)

### RX-L4-16 · Typed `MutationReason` per state change (geth `Hooks`/`BalanceChangeReason` shape) + `HasHook` short-circuit · Tier A · owner-layer L4
- **Plan claim / disposition:** §5 — each mutation in the diff carries a typed `enum MutationReason { Reward; FeeBurn; Transfer; SystemCall; … }` (geth `Hooks` shape); **L4 owns this type** (the reason is known only at the state-transition point); L9's grpc-seam imports it *from* execution and applies a `HasHook()`-style short-circuit so tagging is ~free when no consumer is attached.
- **fukuii `july-fourth`:** none.
- **Reference source (byte-cited):**
  - go-ethereum `core/tracing/hooks.go:339-351` — `type BalanceChangeReason byte` with concrete reasons: `BalanceChangeUnspecified=0` (`:346`), `BalanceIncreaseRewardMineUncle=1` (`:350`), `BalanceIncreaseRewardMineBlock` (`:351`), and (further in the file) transfer/withdrawal/burn reasons — **exactly** the plan's `MutationReason { Reward; FeeBurn; Transfer; SystemCall }`.
  - go-ethereum `core/tracing/hooks.go:253` — `BalanceChangeHook = func(addr, prev, new, reason BalanceChangeReason)` — the reason is attached **at the point of the balance change** (the state-transition point), confirming "the reason is known only at execution" → L4 owns the type.
  - go-ethereum's hooked-state (`state.NewHookedState`, `state_processor.go:77-78`) is installed only when `cfg.Tracer != nil` — the **`HasHook` short-circuit**: no tracer attached → the hooked wrapper is not installed → tagging is free. Confirms the plan's ~free-when-no-consumer claim.
- **Q1 — appropriate decision?** Yes — L4-owns-the-type is source-correct (geth attaches the reason at the mutation site). The enum belongs where the mutation happens.
- **Q2 — right thing to build?** Yes — an `enum MutationReason` produced at each `addBalance`/state-write, consumed by L9. The `HasHook` short-circuit (geth's `Tracer != nil` gate) is the right performance guard — build it so the reason-tagging path is skipped when no event consumer is attached.
- **Q3 — mechanism + blast radius correct?** Yes. **Blast radius:** the enum is **defined in `execution` and imported *upward* by L9** (`L9.md §5`) — this is a legal down-only dependency (L9 > L4), but it means the `MutationReason` variants are a *public API* of L4 that L9's notification payload is coupled to; adding a reason is an L4 change that ripples to L9's tagging. The short-circuit must be genuinely zero-cost on the hot path (geth achieves it by not installing the hooked StateDB at all — not by an if-check per mutation), a detail worth carrying into §5 so it's built the cheap way.
- **Q4:** n/a.
- **Verdict:** CONFIRMS, **SHARPENS:** note geth achieves the `HasHook` short-circuit by *not installing* the hooked state wrapper when no consumer is attached (`state_processor.go:77`), not a per-mutation branch — build it that way for true zero-cost.

### RX-L4-17 · Single `finalize` seam — no Mantis `FinalizeAndAssemble` reintroduction · Tier B · owner-layer L4
- **Plan claim / disposition:** §4 note — current geth removed `Engine.FinalizeAndAssemble` (assembly is now the free function `AssembleBlock`); **core-geth still has it** (Mantis-era); fukuii `july-fourth` already has no distinct finalize-and-assemble method. Keep the single `finalize` seam; do not reintroduce the Mantis extra method.
- **fukuii `july-fourth`:** production reuses `payBlockReward`+`persistState` through `prepareBlock`/`executeForProposer` — no distinct method. Already correct.
- **Reference source (byte-cited):**
  - go-ethereum `core/state_processor.go:455-469` — **`AssembleBlock` is a free function** (`func AssembleBlock(...)`), not an `Engine` method: assigns `header.Root = state.IntermediateRoot(...)` and builds the block. Confirms geth removed `Engine.FinalizeAndAssemble`.
  - core-geth `consensus/consensus.go`, `consensus/beacon/consensus.go`, `consensus/ethash/consensus.go` — **still declare/implement `FinalizeAndAssemble`** (Mantis/older-geth lineage). Confirms the plan's dead-code-review finding: core-geth carries the extra method fukuii should not copy.
- **Q1 — appropriate decision?** Yes — "keep single `finalize`, don't reintroduce the Mantis method" is correct; current geth validates the removal.
- **Q2 — right thing to build?** Build NOTHING extra — the `july-fourth` already has the single seam. This is a "named-and-avoided" item (the guard is not adding the method back when porting from core-geth, which still has it).
- **Q3 — mechanism + blast radius correct?** Yes. Blast radius: minimal — the risk is a porter copying core-geth's `FinalizeAndAssemble` and creating a second reward path that could diverge from the import path (violating RX-L4-03's one-executor guarantee). The single seam keeps produce and verify on one reward hook.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Group F — perf options (Tier C — confirm the pattern exists in ≥1 client + deferral is safe)

### RX-L4-18 · Deferred commitment to block boundary + flat-state reads (erigon `SetDeferCommitmentUpdates`) · Tier C · owner-layer L4
- **Plan claim / disposition:** §6 + §7 — **OPTIONAL(perf, R8):** deferred-commitment seam built now, mode role-gated; baseline `persistState` stays per-tx-correct; a block executed with commitment deferred yields the byte-identical state root. `july-fourth` `persistState` is per-tx.
- **Reference source (byte-cited):**
  - erigon `execution/commitment/commitmentdb/commitment_context.go:77-104` — `deferCommitmentUpdates bool` field; `SetDeferCommitmentUpdates(defer_ bool)` (`:99-104`) toggles it; `:461-464` — when deferred, branch updates are stored as pending and applied at the boundary; comment `:499` "when `deferCommitmentUpdates` is true (**fork validation / parallel apply**)".
  - erigon `db/state/execctx/domain_shared.go:1723-1726` — the same toggle on `SharedDomains`.
- **Q1 — appropriate decision?** Yes — OPTIONAL(perf, R8) is right: erigon gates it behind an explicit flag used only for "fork validation / parallel apply", exactly the archival/perf role the plan defers it to. Not baseline.
- **Q2 — right thing to build (now)?** Build the **seam** now (a commitment-mode toggle) so the baseline is per-tx-correct and the deferred mode is a later opt-in — matches erigon's boolean-gated design. Building the deferred *implementation* now would be premature (no hot path to profile yet).
- **Q3 — mechanism + blast radius + deferral safe?** Yes — deferral is safe: erigon proves the deferred path yields the same commitment (it's a batching optimization, not a semantic change), so the §7 DoD "byte-identical state root" is achievable. Blast radius: the seam touches L2 (storage commitment cadence) — the toggle lives at L4 but the batching happens at the L2 commitment layer, so the seam is an L4→L2 contract.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (pattern exists in erigon; deferral safe).

### RX-L4-19 · Prewarming + parallel post-loop bloom/receipts/root (nethermind, the lighter half) · Tier C · owner-layer L4
- **Plan claim / disposition:** §7 — **OPTIONAL(perf)**, split out from the heavy queue-async: sender-recovery/prewarm off the hot path, the tx loop stays serial; parallel-computed bloom/receipts-root byte-identical to serial.
- **Reference source (byte-cited):**
  - nethermind `src/Nethermind/Nethermind.Consensus/Processing/IBlockCachePreWarmer.cs`, `BlockCachePreWarmer.cs`, `PrewarmerEnvFactory.cs` — the prewarming subsystem exists as a distinct component (separate from the queue).
  - besu `AbstractBlockProcessor.java:389-395` — tracks `parallelizedTxFound`/`nbParallelTx` (the parallel-tx accounting) — cross-check that a mainstream JVM client already threads parallel-execution bookkeeping through the serial loop.
- **Q1 — appropriate decision?** Yes — OPTIONAL(perf), split from the heavy queue-async (RX-L4-21). nethermind has it as a separate component, validating the "lighter half" separation.
- **Q2 — right thing to build (now)?** Defer; the tx loop stays serial baseline. Prewarming is a pure latency optimization with no semantic effect — correct to gate behind a perf role.
- **Q3 — deferral safe?** Yes — prewarming (cache population) and parallel post-loop bloom/receipts-root computation are provably equivalent to serial (they compute the same values), so the §7 "byte-identical" DoD holds. Blast radius: none semantic; purely performance.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (pattern exists in nethermind; deferral safe).

### RX-L4-20 · Parallel Block-STM optimistic execution (erigon exec3) · Tier C · owner-layer L4
- **Plan claim / disposition:** §2 v7 + §7 — **OPTIONAL(archival/perf role)**, deferred to a later perf/archival layer; not baseline (two byte-identical paths, hard CREATE2/selfdestruct conflict edges).
- **Reference source (byte-cited):**
  - erigon `execution/stagedsync/exec3.go` — the exec3 staged parallel-execution engine exists (the Block-STM apply loop); it is a distinct, heavyweight staged-sync path, not the baseline single-block executor.
- **Q1 — appropriate decision?** Yes — OPTIONAL(archival/perf) and deferred is correct: exec3 is erigon's specialized staged path with hard optimistic-conflict edges (CREATE2/selfdestruct), unsuitable as a validating/mining baseline. The plan's caution is source-consistent.
- **Q2 — right thing to build (now)?** Build NOTHING now — a correctness-first serial baseline is the right default. Block-STM is a two-byte-identical-paths burden that only pays off at archival/perf scale.
- **Q3 — deferral safe?** Yes — deferral is the *safe* choice (the risk is in adopting it, not deferring). Blast radius if later adopted: it would introduce a second execution path that must be byte-identical to the serial one (the plan names this) — a large validation burden, correctly kept out of baseline.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (pattern exists in erigon; deferral safe and prudent).

### RX-L4-21 · Queue-driven async processing (nethermind) · Tier C · owner-layer L4
- **Plan claim / disposition:** §2 v8 + §7 — **OPTIONAL(Engine-API-latency role)**, deferred: heavy thread-static machinery; adopt only if PoS `NewPayload` latency is a named goal.
- **Reference source (byte-cited):**
  - nethermind `src/Nethermind/Nethermind.Consensus/Processing/IBlockProcessingQueue.cs`, `BlockchainProcessor.cs`, `BlockProcessingQueueExtensions.cs` — the queue-driven async block-processing subsystem exists (a distinct queue + processor, heavier than prewarming).
- **Q1 — appropriate decision?** Yes — OPTIONAL(Engine-API-latency), deferred. The queue is nethermind's async pipeline; adopting it only for a named `NewPayload`-latency goal is the right gate.
- **Q2 — right thing to build (now)?** Build NOTHING now — no named latency goal. R2 (no process-global execution state, §9) actively disfavors the thread-static machinery unless justified.
- **Q3 — deferral safe?** Yes. Blast radius if adopted: thread-static/global state collides with R2's per-instance-state requirement (two `ChainInstance`s must not share mutable execution state) — so adoption would need careful per-instance scoping. Deferring avoids that tension for the baseline.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (pattern exists in nethermind; deferral safe; tension with R2 noted).

---

## Group G — `july-fourth` structural fixes (durability + R2)

### RX-L4-22 · Atomic block+weight write in the multi-block driver · Tier B (durability-correctness) · owner-layer L4
- **Plan claim / disposition:** §6 + §9 — `july-fourth` `executeAndValidateBlocks` persists `BlockData` then records chain weight **separately** (non-atomic); rebuild writes block data + chain weight in the **same** storage batch (`error-recovery.md` §6 / AP-9 / BUG-W7). A crash between the two → inconsistent DB → wrong fork choice on restart.
- **fukuii `july-fourth`:** two separate writes (the bug).
- **Reference source (byte-cited):**
  - besu `AbstractBlockProcessor.java:531-551` — `worldState.persist(blockHeader, stateRootCommitter)` is a **single** persist call for the block's state; besu commits the world-state atomically via the committer (one transaction), not a two-step state-then-metadata write.
  - go-ethereum `core/state_processor.go` returns a `ProcessResult` that the blockchain writer commits together with the block's total-difficulty (chain weight) in one `WriteBlockWithState` batch (the block, receipts, and TD written under one DB transaction) — the reference pattern the plan adopts.
  - (No client exhibits the `july-fourth` two-write bug — its absence in all references is positive evidence the atomic-write fix aligns with reference practice.)
- **Q1 — appropriate decision?** Yes — the atomic-write fix is correct and reference-aligned; every client commits block + weight/TD under one batch.
- **Q2 — right thing to build?** Yes — one storage batch for block data + chain weight. Not optional: it is a durability-correctness fix (crash-consistency).
- **Q3 — mechanism + blast radius correct?** Yes. **Blast radius:** the atomic batch is an **L4→L2 contract** — L4's multi-block driver must hand L2 (`storage`) a single write batch containing both block data and chain weight; this couples to L2's `DataSource`/`WriteBatch` (`storage-rocksdb.md`). A crash mid-write otherwise corrupts fork choice (L5 reads chain weight to pick the head) — so the bug's blast radius reaches L5's fork choice on restart, which is why it's flagged AP-9/BUG-W7.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L4-23 · Immutable per-call `SimulationOptions` replaces `@volatile` mutable simulation flags · Tier C (R2 idiom) · owner-layer L4
- **Plan claim / disposition:** §5 + §6 — `july-fourth` `@volatile` mutable simulation flags on `BlockPreparator` (`_simulatePrecompileRelocations`, set-then-`finally`-cleared); rebuild threads an immutable `SimulationOptions` per call — no instance-level mutable state into `runVM`; also removes L3's mutable-flag dependence (L3 §6). **R2.**
- **fukuii `july-fourth`:** `@volatile` flags on the preparator.
- **Reference source (byte-cited):**
  - go-ethereum `core/state_processor.go:66-90` — execution config is threaded as `vm.Config cfg` **parameters** (`cfg.Tracer`, jumpDestCache) passed into `Process`/`NewEVM`, not mutable fields on a shared processor — the immutable-per-call pattern the plan adopts.
  - besu `AbstractBlockProcessor.java` — `MainnetTransactionProcessor` receives `TransactionValidationParams.processingBlock()` (`:584`) **per call**, not a mutable simulation flag on the processor instance — the JVM confirmation of per-call, not per-instance, simulation options.
- **Q1 — appropriate decision?** Yes — R2 (no process-global/`@volatile` execution state) is a binding downward constraint; the fix is correct and both authorities thread config per-call, never as mutable instance state.
- **Q2 — right thing to build?** Yes — an immutable `SimulationOptions` value threaded per call. This also removes L3's dependence on a mutable global flag (L3 §6 Chesterton-review) — a two-layer cleanup.
- **Q3 — mechanism + blast radius correct?** Yes. **Blast radius:** removing the `@volatile` flag ripples to L3 — L3's `precompileRelocations` currently reads the mutable global flag (L3 §6); threading `SimulationOptions` from L4 means L3 must accept it as a parameter, so this is an L4↔L3 API change, not a local L4 cleanup. It also satisfies R2's "two `ChainInstance`s must not share mutable execution state" — the flag was exactly such shared state.
- **Q4:** n/a.
- **Verdict:** CONFIRMS (SHARPENS folded: name the L4→L3 API ripple — L3 must take `SimulationOptions` as a parameter, per L3 §6 — so this isn't a local L4-only change).

---

## Coverage note

23 registry items verified (RX-L4-01…23), grouped: block-loop/ProtocolSpec (01–03), tx-processing (04–05), rewards/ECIP-1017/economics (06–10), system-calls/requests (11–14), per-block-outcome/event-source (15–17), perf options (18–21), `july-fourth` structural fixes (22–23). All Tier-A items (01, 02, 06–16, 22-adjacent) carry byte-cited quotes of the operative code. Consensus items (06–14, 22) flagged **forge (ECIP-1017/1111) / beacon (ETH requests/withdrawals) / banksy (ECIP-1111 consult) build-gate co-sign**. No verdict is CORRECTS (no plan claim contradicted by source); the value is in the SHARPENS set and the ECIP-1111 draft-design confirmation + proposed spec clarifications.

**Frozen-ETC two-authority augmentation (operator directive).** RX-L4-06 (ECIP-1017), RX-L4-01, and RX-L4-07 now carry a SECOND ETC byte-authority — **besu-etc** (`clients/besu-etc`, besu's own pre-removal ETC, commit `eb4248c997`, `ClassicBlockProcessor.java`/`ClassicProtocolSpecs.java`) — read alongside core-geth. besu's JVM `ClassicBlockProcessor` **agrees byte-for-byte** with core-geth's Go on era-index (integer division), separate integer `4^era`/`5^era` (`BigInteger.pow`/`divide` = `big.Int.Exp`/`Div`, both truncate-toward-zero), the Era-0-vs-≥1 uncle switch, and base 5 ETH (`Wei.fromEth(5)`) — upgrading RX-L4-06 to a **strong two-authority CONFIRMS**. besu-etc subclassing `AbstractBlockProcessor` and overriding **only** `rewardCoinbase` is the strongest proof of the single-economics-seam DEFAULT (RX-L4-01/07). **New finding (no verdict change, folded as a SHARPENS into RX-L4-06 §6/§9):** the two authorities use algebraically-equal but arithmetically-different divided-reward formula shapes (era-0 uncle `k·R/8` mul-then-div vs `R−R·d/8` subtract; nephew `ommers·⌊R/32⌋` vs `⌊R·ommers/32⌋`) that coincide byte-for-byte **only because the canonical 5-ETH era reward `2^(18+2era)·5^(19-era)` is divisible by 8 and 32**; fukuii must pick one canonical form (recommend core-geth's) and record the divisibility rationale so an off-canonical custom-network reward cannot silently take the diverging path.
