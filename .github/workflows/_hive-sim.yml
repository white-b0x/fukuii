name: _hive-sim (reusable)

# Reusable workflow for running a single ethereum/hive simulator against the
# fukuii client. Per-simulator caller workflows (hive-smoke-genesis.yml,
# hive-rpc.yml, …) invoke this with `uses: ./.github/workflows/_hive-sim.yml`
# so each has its own workflow name, run history, and badge in the README.
#
# Build steps mirror hive-prague.yml so the two pipelines stay in sync:
#   * sbt assembly -> thin JRE overlay tagged chipprbots/fukuii:latest
#   * git clone ethereum/hive into external/hive (NOT ./hive — that would
#     shadow the fukuii adapter dir) with retries + blob:none
#   * go build -o hive . (ethereum/hive's main binary lives at repo root;
#     cmd/hive does not exist — cmd/ holds hivechain + hiveview)
#   * pass/fail counts harvested from the simulator log and posted to the
#     job summary; optional threshold gate fails the run if under budget.

on:
  workflow_call:
    inputs:
      sim:
        description: 'Simulator path, e.g. ethereum/rpc or smoke/genesis'
        required: true
        type: string
      label:
        description: 'Filename-safe label for artifacts and summaries'
        required: true
        type: string
      sim_limit:
        description: 'Optional regex passed to --sim.limit'
        required: false
        type: string
        default: ''
      sim_buildarg:
        description: 'Optional value for --sim.buildarg (e.g. disable_strict_exception_matching=fukuii)'
        required: false
        type: string
        default: ''
      sim_timelimit:
        description: 'Value for --sim.timelimit (e.g. 40m, 1h)'
        required: false
        type: string
        default: '40m'
      parallelism:
        description: 'Value for --sim.parallelism'
        required: false
        type: number
        default: 4
      pass_threshold:
        description: 'Fail the run if passed < threshold (0 = never fail)'
        required: false
        type: number
        default: 0
      hive_ref:
        description: 'ethereum/hive git ref to test against'
        required: false
        type: string
        default: 'master'
      clients:
        description: |
          Comma-separated list of hive client names to test (e.g. "fukuii"
          or "fukuii,go-ethereum,nethermind"). Defaults to fukuii alone
          for the single-client compliance suites. Sync-style sims override
          this to bring in geth/nethermind so we exercise cross-client
          interop instead of fukuii-vs-fukuii self-tests.
        required: false
        type: string
        default: 'fukuii'
      gate_pattern:
        description: |
          Optional substring (case-insensitive) used to gate the build on a
          specific subset of tests. The tabulator counts tests whose name
          contains this substring separately, and the build fails if any of
          those tests fail. Defaults to empty (no subset gating). Cross-client
          sims set this to "fukuii" so upstream geth↔nethermind failures
          don't pollute the build signal but any fukuii-touching failure
          does fail the build.
        required: false
        type: string
        default: ''
      gate_exclude:
        description: |
          Optional regex (case-insensitive) of test names to EXCLUDE from the
          gate even when they match `gate_pattern`. Use this to acknowledge
          specific known-failing tests as separately tracked bugs without
          giving up the rest of the gate. Format is a bash extended regex
          alternation (e.g. "sync foo from bar|sync baz from quux").
          Defaults to empty (no exclusions).
        required: false
        type: string
        default: ''
    outputs:
      passed:
        description: 'Number of simulator tests that passed'
        value: ${{ jobs.hive.outputs.passed }}
      failed:
        description: 'Number of simulator tests that failed'
        value: ${{ jobs.hive.outputs.failed }}
      total:
        description: 'passed + failed'
        value: ${{ jobs.hive.outputs.total }}
      gate_passed:
        description: 'Tests matching gate_pattern that passed (0 if no gate)'
        value: ${{ jobs.hive.outputs.gate_passed }}
      gate_failed:
        description: 'Tests matching gate_pattern that failed (0 if no gate)'
        value: ${{ jobs.hive.outputs.gate_failed }}

permissions:
  contents: read

jobs:
  hive:
    name: ${{ inputs.label }}
    runs-on: ubuntu-latest
    timeout-minutes: 90

    outputs:
      passed: ${{ steps.tab.outputs.passed }}
      failed: ${{ steps.tab.outputs.failed }}
      total: ${{ steps.tab.outputs.total }}
      gate_passed: ${{ steps.tab.outputs.gate_passed }}
      gate_failed: ${{ steps.tab.outputs.gate_failed }}

    steps:
      - name: Checkout fukuii
        uses: actions/checkout@v5
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: 'sbt'

      - name: Cache Coursier / Ivy / sbt
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/coursier
            ~/.ivy2/cache
            ~/.sbt
          key: sbt-${{ runner.os }}-${{ hashFiles('**/*.sbt', 'project/**') }}
          restore-keys: sbt-${{ runner.os }}-

      - name: Install sbt
        run: |
          set -euxo pipefail
          # Retry curl — the GitHub->Azure blob redirect occasionally serves a
          # 92-byte garbage body instead of the tarball, causing tar xz to die
          # with "not in gzip format". --retry-all-errors handles that.
          curl -L --fail --retry 5 --retry-delay 5 --retry-all-errors \
            -o /tmp/sbt.tgz \
            https://github.com/sbt/sbt/releases/download/v1.10.7/sbt-1.10.7.tgz
          file /tmp/sbt.tgz
          tar xz -C /tmp -f /tmp/sbt.tgz
          sudo ln -sf /tmp/sbt/bin/sbt /usr/local/bin/sbt
          sbt --version

      - name: Build fukuii assembly
        run: sbt -batch assembly

      - name: Tag base Docker image
        run: |
          # The hive adapter Dockerfile does `FROM chipprbots/fukuii:latest`, so we build the
          # base image by copying the just-built assembly into a thin overlay.
          mkdir -p target/quick-docker
          cp target/scala-3.*/fukuii-assembly-*.jar target/quick-docker/
          cat > target/quick-docker/Dockerfile <<'DOCKER'
          FROM eclipse-temurin:25-jre-noble
          RUN apt-get update && apt-get install -y --no-install-recommends jq curl && rm -rf /var/lib/apt/lists/*
          WORKDIR /app
          RUN mkdir -p /app/fukuii/lib /app/data /app/hive-conf
          COPY fukuii-assembly-*.jar /app/fukuii/lib/fukuii-assembly.jar
          ENTRYPOINT ["java", "-jar", "/app/fukuii/lib/fukuii-assembly.jar"]
          DOCKER
          docker build -t chipprbots/fukuii:latest target/quick-docker/

      - name: Check out hive
        env:
          HIVE_REF: ${{ inputs.hive_ref }}
        run: |
          set -euxo pipefail
          mkdir -p external
          for i in 1 2 3 4 5; do
            rm -rf external/hive
            if git -c http.lowSpeedLimit=1000 -c http.lowSpeedTime=30 \
                 clone --depth=1 --filter=blob:none \
                 https://github.com/ethereum/hive.git external/hive; then
              break
            fi
            echo "Clone attempt $i failed; sleeping before retry..."
            sleep $((i * 5))
          done
          if [ ! -d external/hive/.git ]; then
            echo "::error::Failed to clone ethereum/hive after 5 retries."
            git --version
            curl -I https://github.com/ethereum/hive.git || true
            exit 1
          fi
          # If the caller pinned a specific ref (not master), check it out.
          if [ -n "$HIVE_REF" ] && [ "$HIVE_REF" != "master" ]; then
            git -C external/hive fetch --depth=1 origin "$HIVE_REF"
            git -C external/hive checkout FETCH_HEAD
          fi
          test -f external/hive/hive.go

      - name: Sync fukuii hive adapter
        run: |
          mkdir -p external/hive/clients/fukuii
          cp -v "$GITHUB_WORKSPACE/hive/fukuii/Dockerfile" external/hive/clients/fukuii/Dockerfile
          cp -v "$GITHUB_WORKSPACE/hive/fukuii/fukuii.sh" external/hive/clients/fukuii/fukuii.sh
          cp -v "$GITHUB_WORKSPACE/hive/fukuii/mapper.jq" external/hive/clients/fukuii/mapper.jq
          cp -v "$GITHUB_WORKSPACE/hive/fukuii/enode.sh" external/hive/clients/fukuii/enode.sh 2>/dev/null || true
          cp -v "$GITHUB_WORKSPACE/hive/fukuii/hive.yaml" external/hive/clients/fukuii/hive.yaml 2>/dev/null || true
          cp target/scala-3.*/fukuii-assembly-*.jar external/hive/clients/fukuii/

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: 'stable'
          # We build hive from external/hive (cloned at runtime); this repo has
          # no go.sum, so disable the default dependency cache to silence the
          # "Dependencies file is not found … go.sum" warning.
          cache: false

      - name: Build hive
        working-directory: external/hive
        run: |
          go version
          # ethereum/hive's main binary lives at the repo root (hive.go),
          # NOT under cmd/hive.
          go build -o hive .
          ./hive --help | head -5

      - name: Run simulator
        id: sim
        working-directory: external/hive
        env:
          SIM: ${{ inputs.sim }}
          SIM_LIMIT: ${{ inputs.sim_limit }}
          SIM_BUILDARG: ${{ inputs.sim_buildarg }}
          SIM_TIMELIMIT: ${{ inputs.sim_timelimit }}
          PARALLELISM: ${{ inputs.parallelism }}
          CLIENTS: ${{ inputs.clients }}
        # Do not inherit GitHub's default `-e -o pipefail` — hive returns
        # non-zero for a wide range of per-sim outcomes (test failures,
        # client timeouts, flaky docker pulls) and the tabulate step below
        # is what produces the signal we actually care about: the
        # passed/failed count from the simulator log. Any hive catastrophic
        # failure is surfaced in the step summary and the uploaded logs.
        shell: bash --noprofile --norc {0}
        run: |
          args=(
            --sim "$SIM"
            --client "$CLIENTS"
            --sim.parallelism "$PARALLELISM"
            --sim.timelimit "$SIM_TIMELIMIT"
            # 120s (was 60s): fukuii is a JVM client whose cold class-load + JIT
            # warmup can consume most of a 60s budget before the sink reaches the
            # sync target, tipping slow fukuii-sink sync tests over the deadline.
            # Benign for native clients (geth/nethermind respond well within 60s);
            # the timeout only matters when a client is genuinely slow to get ready.
            --client.checktimelimit=120s
            --loglevel 3
          )
          [ -n "$SIM_LIMIT" ]   && args+=(--sim.limit   "$SIM_LIMIT")
          [ -n "$SIM_BUILDARG" ] && args+=(--sim.buildarg "$SIM_BUILDARG")

          ./hive "${args[@]}" 2>&1 | tee hive-run.log
          hive_exit=${PIPESTATUS[0]}
          echo "hive_exit=${hive_exit}" >> "$GITHUB_OUTPUT"
          echo "Hive exited with code ${hive_exit}"
          exit 0

      - name: Tabulate results
        id: tab
        if: always()
        working-directory: external/hive
        # Override the default GitHub Actions shell (`bash -e`) with plain
        # `bash` so a single non-zero command (e.g. jq parsing an unexpected
        # JSON shape) doesn't abort tabulation entirely. The script handles
        # errors explicitly with `|| true` and value defaults; we WANT it to
        # always emit a summary row even when individual parses fail.
        shell: bash
        env:
          LABEL: ${{ inputs.label }}
          SIM: ${{ inputs.sim }}
          THRESHOLD: ${{ inputs.pass_threshold }}
          HIVE_EXIT: ${{ steps.sim.outputs.hive_exit }}
          GATE_PATTERN: ${{ inputs.gate_pattern }}
          GATE_EXCLUDE: ${{ inputs.gate_exclude }}
        run: |
          set -uo pipefail
          shopt -s nullglob

          # Hive writes structured per-suite results to workspace/logs/*.json
          # (each contains a `testCases` map; cases have `summaryResult.pass`).
          # The simulator stdout log format varies wildly between simulators —
          # devp2p, rpc, sync, consensus, EELS pytest-xdist, etc all use
          # different verdict strings — so grepping the log is unreliable and
          # was producing 0/0 even when tests ran. JSON is canonical.
          passed=0
          failed=0
          gate_passed=0
          gate_failed=0
          gate_failed_names=""
          json_count=0

          # Extract pass=true and pass=false counts in a single jq pass per
          # file, with `?` operators and `// 0` defaults so unexpected schema
          # shapes (null testCases, missing summaryResult, array vs object,
          # etc) yield zeros rather than aborting the script.
          # When $GATE_PATTERN is non-empty, also extract the subset matching
          # the pattern (case-insensitive substring on the test name) so we
          # can gate the build on a specific client's tests without letting
          # upstream client-vs-client failures break the signal.
          # When $GATE_EXCLUDE is non-empty, also drop any subset test whose
          # name matches the exclusion regex — used to acknowledge tracked
          # bugs as expected failures without giving up the rest of the gate.
          # Exclusion uses jq `test()` which is POSIX extended regex; the
          # caller passes a `|`-alternation of substrings, e.g.
          # `"sync foo from bar|sync baz from quux"`.
          jq_query='
            def cases: try [.testCases | to_entries[]? | .value] catch [];
            def matches($p): (.name // "") | ascii_downcase | contains($p | ascii_downcase);
            def excluded($e):
              if $e == "" then false
              else (.name // "") | test($e; "i")
              end;
            (cases | map(.summaryResult.pass)) as $all |
            (cases
              | map(select(matches($p)))
              | map(select(excluded($x) | not))
              | map(.summaryResult.pass)) as $sub |
            (cases
              | map(select(matches($p)))
              | map(select(excluded($x) | not))
              | map(select(.summaryResult.pass == false) | .name // "?")) as $failed_names |
            "\($all | map(select(. == true))  | length) " +
            "\($all | map(select(. == false)) | length) " +
            "\($sub | map(select(. == true))  | length) " +
            "\($sub | map(select(. == false)) | length)\n" +
            ($failed_names | join("\n"))
          '

          for jf in workspace/logs/*.json; do
            # Only count files that look like hive suite results.
            jq -e 'has("testCases")' "$jf" >/dev/null 2>&1 || continue

            out=$(jq -r --arg p "$GATE_PATTERN" --arg x "$GATE_EXCLUDE" "$jq_query" "$jf" 2>/dev/null || echo "0 0 0 0")
            counts_line=$(printf '%s\n' "$out" | head -n1)
            names_block=$(printf '%s\n' "$out" | tail -n +2)
            read -r p f gp gf <<< "${counts_line:-0 0 0 0}"
            for v in p f gp gf; do
              eval "val=\$$v"
              [[ "$val" =~ ^[0-9]+$ ]] || eval "$v=0"
            done

            json_count=$((json_count + 1))
            passed=$((passed + p))
            failed=$((failed + f))
            gate_passed=$((gate_passed + gp))
            gate_failed=$((gate_failed + gf))
            if [ -n "$names_block" ]; then
              gate_failed_names="${gate_failed_names}${names_block}"$'\n'
            fi
          done

          # Pick newest simulator log via a bash glob (with nullglob set
          # above, an empty match yields an empty array). DON'T use
          # `ls -t pattern` — under nullglob the unmatched pattern expands
          # to nothing and `ls -t` lists the current directory instead,
          # picking up garbage.
          sim_log=""
          sim_logs=(workspace/logs/*-simulator-*.log)
          if [ ${#sim_logs[@]} -gt 0 ]; then
            sim_log="${sim_logs[0]}"
            for f in "${sim_logs[@]}"; do
              [ "$f" -nt "$sim_log" ] && sim_log="$f"
            done
          fi

          # Fallback: if no JSON results parsed (e.g. simulator died before
          # writing them), grep the simulator log against the union of common
          # verdict patterns: pytest-xdist (`[gwN] PASSED `), classic hive
          # (`PASSED tests/`), and go-test (`--- PASS:`). Use `|| true`, NOT
          # `|| echo 0` — `grep -c` already prints `0` on no-match (exit 1),
          # and the echo would append a second `0`, producing the literal
          # string "0\n0" that breaks subsequent arithmetic.
          if [ "$json_count" -eq 0 ] && [ -n "$sim_log" ]; then
            passed=$(grep -cE '(\] PASSED |PASSED tests/|--- PASS:)' "$sim_log" 2>/dev/null || true)
            failed=$(grep -cE '(\] FAILED |FAILED tests/|--- FAIL:)' "$sim_log" 2>/dev/null || true)
            [[ "$passed" =~ ^[0-9]+$ ]] || passed=0
            [[ "$failed" =~ ^[0-9]+$ ]] || failed=0
          fi

          total=$((passed + failed))
          {
            echo "passed=$passed"
            echo "failed=$failed"
            echo "total=$total"
            echo "gate_passed=$gate_passed"
            echo "gate_failed=$gate_failed"
            echo "json_count=$json_count"
          } >> "$GITHUB_OUTPUT"

          if [ "$json_count" -gt 0 ]; then
            src="${json_count} JSON result(s)"
          elif [ -n "$sim_log" ]; then
            src="grep fallback"
          else
            src="(none)"
          fi

          {
            echo "### Hive · ${LABEL} (\`${SIM}\`)"
            echo
            echo "| passed | failed | total | threshold | hive exit | source |"
            echo "|-------:|-------:|------:|----------:|----------:|:-------|"
            echo "| ${passed} | ${failed} | ${total} | ${THRESHOLD} | ${HIVE_EXIT:-?} | ${src} |"
            if [ -n "$GATE_PATTERN" ]; then
              echo
              echo "Gate (\`${GATE_PATTERN}\`-matching subset): **${gate_passed} passed, ${gate_failed} failed**"
              if [ "$gate_failed" -gt 0 ]; then
                echo
                echo "<details><summary>Failed gate-matching tests</summary>"
                echo
                echo '```'
                printf '%s' "$gate_failed_names" | sed '/^$/d' | sort -u
                echo '```'
                echo "</details>"
              fi
            fi
          } >> "$GITHUB_STEP_SUMMARY"

          # Diagnostics: surface tail of the relevant log when results look
          # suspicious so the failure is actionable from the PR check page.
          if [ -z "$sim_log" ] && [ "$json_count" -eq 0 ]; then
            {
              echo
              echo "**No simulator log or JSON results produced.** Tail of \`hive-run.log\`:"
              echo
              echo '```'
              tail -n 80 hive-run.log 2>/dev/null || echo "(hive-run.log missing)"
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          elif [ "$total" -eq 0 ] && [ -n "$sim_log" ]; then
            {
              echo
              echo "**Simulator produced no test results.** Tail of simulator log:"
              echo
              echo '```'
              tail -n 80 "$sim_log" 2>/dev/null || true
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          elif [ "$failed" -gt 0 ] && [ -n "$sim_log" ]; then
            {
              echo
              echo "<details><summary>First 20 FAILED tests</summary>"
              echo
              echo '```'
              grep -E '(\] FAILED |FAILED tests/|--- FAIL:)' "$sim_log" | head -20 || true
              echo '```'
              echo
              echo "</details>"
            } >> "$GITHUB_STEP_SUMMARY"
          fi

          # Always succeed — the threshold gate runs as a separate step.
          exit 0

      - name: Upload simulator logs
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: hive-logs-${{ inputs.label }}
          path: |
            external/hive/workspace/logs/
            external/hive/hive-run.log
          retention-days: 14
          if-no-files-found: warn

      - name: Enforce pass threshold
        if: ${{ inputs.pass_threshold > 0 }}
        env:
          PASSED: ${{ steps.tab.outputs.passed }}
          THRESHOLD: ${{ inputs.pass_threshold }}
          LABEL: ${{ inputs.label }}
        run: |
          if [ "$PASSED" -lt "$THRESHOLD" ]; then
            echo "::error::Hive ${LABEL} only passed ${PASSED} tests (threshold ${THRESHOLD})."
            exit 1
          fi

      - name: Enforce gate (zero failures in subset)
        if: ${{ inputs.gate_pattern != '' }}
        env:
          GATE_FAILED: ${{ steps.tab.outputs.gate_failed }}
          GATE_PATTERN: ${{ inputs.gate_pattern }}
          LABEL: ${{ inputs.label }}
        run: |
          if [ "$GATE_FAILED" -gt 0 ]; then
            echo "::error::Hive ${LABEL} has ${GATE_FAILED} failing test(s) matching gate '${GATE_PATTERN}'."
            exit 1
          fi
