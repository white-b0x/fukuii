# foundational-modules — Overview (placeholder)

Populated when `SR-10` (`.claude/sprints/QUEUE.md`, "Systemic Review" section) is dispatched.
Covers the `crypto`, `rlp`, `bytes` sibling sbt modules. Tier B. Ready now — recommended
first dispatch among the ready-now group, since other items cite its findings.
