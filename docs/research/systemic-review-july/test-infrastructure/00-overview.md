# test-infrastructure — Overview (placeholder)

Populated when `SR-12` (`.claude/sprints/QUEUE.md`, "Systemic Review" section) is dispatched.
Covers `ets/tests` submodule, `src/it`, `src/evmTest`, `src/rpcTest`. Tier B. Ready now.
