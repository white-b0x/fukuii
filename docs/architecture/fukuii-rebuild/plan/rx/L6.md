# RX — L6 `network` per-item reference-client verification

> **Research grounding:** verify against reference-client source **and** cite this layer's binding SR
> observation(s); the RX crosscheck method + the asset→layer map are in
> [`../../research-index.md`](../../research-index.md) and [`../coherence-pass-02.md`](../coherence-pass-02.md) (WB-R5).

_The depth pass for the devp2p/RLPx networking layer. Each item the L6 plan (`plan/L6.md` §1–§10 +
`optimizations.md` §L6 rows + `requirements.md` R1/R2/R3/R5/R6/R9/R11 cells + `feature-ledger.md`
F7/F8/F10) commits to implement, verified against the **actual vendored reference-client source** under
`.claude/repo-references/clients/{besu,core-geth,go-ethereum,nethermind,reth}/`. Method: `rx/README.md`._

**Every entry answers the four questions before its verdict:**
1. **Q1 — appropriate decision?** is the plan's disposition (DEFAULT/STRUCTURAL/OPTIONAL/OBSOLETE) right given what the source shows?
2. **Q2 — what we should implement?** the right thing to build, vs a better alternative the multi-client read reveals, vs NOTHING (YAGNI / seam suffices)?
3. **Q3 — mechanism + blast radius correct?** (a) how it actually works (source is the check); (b) blast radius — every layer/item/consensus-path it ripples into.
4. **Q4 — if any "no": the correct answer + why** (source-grounded).

Verdict: **CONFIRMS** / **CORRECTS** / **SHARPENS**.

**Authority caveat (README §"all clients" / "absence is evidence").** L6 is **not consensus** (no state
root changes), but **wire byte-exactness is held to consensus-grade discipline** — a ForkId byte error is a
network partition. Wire byte formats are matched against **go-ethereum + besu together** (both maintained,
must agree); the JVM-implementation lens is **besu**. The **only ETC-frozen L6 value** is the ForkId
**fork-schedule** (which forks at which block/time) — authority **core-geth** (`config_classic.go`/
`config_mordor.go`); the CRC32 **algorithm** is network-neutral (go-ethereum `core/forkid`). ETC networkId
is a frozen fact (core-geth). besu-etc (pre-2026) predates eth/69–71 and carries no wire authority here.
ForkId byte-computation is flagged **forge (ETC) / beacon (ETH) co-sign at build**.

**Path-existence pre-check (every cited path verified present in the vendored tree):** go-ethereum
`core/forkid/forkid.go`, `eth/protocols/eth/{protocol.go,handshake.go}`, `p2p/{server.go,transport.go,rlpx/rlpx.go}`,
`p2p/discover/{v4wire,v5wire,…}`, `p2p/{enr,dnsdisc,enode}/` ✓; core-geth `core/forkid/forkid.go`,
`params/{config_classic.go,bootnodes_classic.go,bootnodes_mordor.go,bootnodes.go}` ✓; besu
`ethereum/p2p/…/rlpx/wire/CapabilityMultiplexer.java`, `…/rlpx/RlpxAgent.java`,
`…/rlpx/handshake/ecies/ECIESEncryptionEngine.java`, `…/rlpx/framing/Framer.java`,
`…/network/PeerDenylistManager.java`, `…/permissions/PeerPermissionsDenylist.java`,
`ethereum/eth/…/{EthProtocol.java,EthProtocolVersion.java,messages/EthProtocolMessages.java,manager/PeerReputation.java}` ✓;
nethermind `Nethermind.Network.Stats/NodeStatsLight.cs` ✓; reth `crates/net/eth-wire-types/src/{version.rs,capability.rs}` ✓.
**One citation drift surfaced (RX-L6-08):** the plan's `SetDNSDiscoveryDefaults2` core-geth symbol is not in
the vendored tree — the actual ETC DNS-tree symbols are `KnownDNSNetwork` + `ClassicDNSNetwork1/2` + `dnsPrefixETC`.

---

## Group A — RLPx transport / framing

### RX-L6-01 · Streams-Tcp RLPx stages (MOD-13; kill the Classic `pekko.io.Tcp` bridge) · Tier B (STRUCTURAL R5) · owner-layer L6
- **Plan claim / disposition:** §5 + §6.1 + `optimizations.md` §L6 row "Streams-Tcp RLPx stages" — STRUCTURAL(R5). Model RLPx as `pekko.stream.scaladsl.Tcp` stream stages (ECIES-auth → frame → Snappy → message-codec), mirroring besu's per-connection channel pipeline; **no** Classic `OutboundTcpBridge`/`InboundTcpBridge`/`TcpEventBridge`, no `sender()` capture.
- **fukuii `july-fourth`:** (SR snapshot `clients/fukuii/networking-p2p.md`) `RLPxConnectionHandler` is a Typed 6-state behavior delegating raw I/O to Classic `OutboundTcpBridge`/`InboundTcpBridge` children (`:185-240`); `ServerActor` uses a Classic `TcpEventBridge` to capture `sender()` for `Tcp.Connected` (`:198-203`) — the last Classic-IO boundary.
- **Reference source (byte-cited):**
  - besu `ethereum/p2p/…/rlpx/framing/Framer.java` + `…/handshake/ecies/ECIESHandshaker.java` + the Netty `connections/netty/{DeFramer,MessageFramer}.java` — besu's per-connection channel pipeline is **discrete stages** (handshake → frame → snappy-in-frame → session), the structural template for stream stages. Not an actor bridge.
  - go-ethereum `p2p/rlpx/rlpx.go` + `p2p/transport.go` — the transport is a synchronous read/write pipeline over one `net.Conn` (a single goroutine owns the socket), the "one owner per connection" shape a Streams-Tcp `Flow` reproduces.
- **Q1 (appropriate?):** Yes — STRUCTURAL is right: Streams-Tcp is current/non-deprecated in Pekko 1.6, needs no new dep, and retrofitting the stage pipeline over a Classic bridge later is the rewrite MOD-13 exists to avoid.
- **Q2 (what to implement?):** Stream stages mirroring besu's pipeline, from line one. Correct — besu proves the pipeline decomposes into stages; the Classic bridge exists in `july-fourth` only because no Typed IO-Tcp existed, a reason Streams-Tcp removes.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: this is `flow`-owned stream work (materialization/backpressure/`preMaterialize`/uncompleted-`Future` traps — §9), and the `MsgPipe` in-memory harness (RX-L6-29) is a Streams-Tcp `Flow` so it rides the same seam. The plan states both (§9 flow review, §5 test kit).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-02 · ECIES auth handshake (pre-EIP-8 + EIP-8/v4) + non-exception `AuthHandshakeResult` (NETWORK-02) · Tier B · owner-layer L6
- **Plan claim / disposition:** §1 (ECIES auth, both pre-EIP-8 and EIP-8/v4 forms) + §6 "Preserved as correct" (the non-exception `AuthHandshakeResult` return path, NETWORK-02) + §8 DoD (ECIES auth under `ActorTestKit`).
- **fukuii `july-fourth`:** (snapshot) `AuthHandshaker` returns an `AuthHandshakeResult` ADT rather than throwing (NETWORK-02); supports both handshake forms.
- **Reference source (byte-cited):**
  - besu `…/rlpx/handshake/ecies/ECIESHandshaker.java` + `ECIESEncryptionEngine.java` — the ECIES auth message build/parse (both the legacy fixed-size and the EIP-8 RLP/size-prefixed forms). The JVM reference for the handshake stage.
  - go-ethereum `p2p/rlpx/rlpx.go` `(*handshakeState) handleAuthMsg`/`makeAuthMsg` + `readMsg` — the auth-packet byte layout authority; geth returns `error`, fukuii's ADT is a Scala-idiom improvement over both (no control-flow-by-exception).
- **Q1 (appropriate?):** Yes — supporting both handshake forms is mandatory interop (old peers still send pre-EIP-8). Preserving the non-exception result path is a correct Scala-3/effect-discipline choice (fail-loud without exceptions-as-control-flow).
- **Q2 (what to implement?):** Both forms + the ADT return. Correct; no simpler option (dropping pre-EIP-8 loses peers).
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: the auth-tag compare is the R11 constant-time consumer (RX-L6-05); the handshake stage feeds the frame secrets. Plan states the const-time link (§8).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-03 · AES-CTR + Keccak-MAC frame codec + CVE-2026-26313 frame-body-size bound · Tier B · owner-layer L6
- **Plan claim / disposition:** §1 (AES-CTR + Keccak-MAC frame codec) + §6 "Preserved as correct" (the CVE-2026-26313 frame-body-size bound, `FrameCodec.scala:130-136`). §8 DoD: framing round-trips as stream stages.
- **fukuii `july-fourth`:** (snapshot) `FrameCodec` with an explicit frame-body-size bound (CVE-2026-26313 fix); a prior double-encoding bug regressed interop vs geth/besu/nethermind (§8) — that fix is preserved.
- **Reference source (byte-cited):**
  - besu `…/rlpx/framing/Framer.java:203-209,273-321` — header MAC via AES `macEncryptor.processBlock` then `updateIngress(xor(...))`; frame MAC computed the same way; a bounded read. The JVM frame-codec reference.
  - go-ethereum `p2p/rlpx/rlpx.go` (`readFrame`/`writeFrame`, `frameHeaderSize`, `readUint24`) — the AES-CTR + Keccak-MAC frame layout byte-authority; enforces a 24-bit frame length (`maxUint24`) ceiling that bounds the body.
- **Q1 (appropriate?):** Yes — the frame codec is fixed-format interop; the size bound is a real DoS guard (a peer-supplied length must be capped).
- **Q2 (what to implement?):** Port the frame layout verbatim + keep the body-size bound. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct (24-bit length ceiling ⇒ ≤16MiB frame, matching the Snappy bound in RX-L6-04). Blast radius: as a stream stage this is flow-reviewed; a framing byte-error is the interop-partition class (§8 interop trio). Plan states it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-04 · Snappy compress/decompress policy — post-Hello timing + decompressed-size bound · Tier B · owner-layer L6
- **Plan claim / disposition:** §1 (Snappy compress-outbound/expect-inbound framing policy + post-Hello timing) + §3 (go-ethereum `transport.go` `SetSnappy` timing; core-geth 16MiB decompressed bound) + §6 "Preserved as correct" (decompress-first/heuristic-fallback Snappy policy).
- **fukuii `july-fourth`:** (snapshot) decompress-first with a heuristic fallback; Snappy enabled after the p2p Hello.
- **Reference source (byte-cited):**
  - go-ethereum `p2p/transport.go:148-149` — `t.conn.SetSnappy(their.Version >= snappyProtocolVersion)` — Snappy is upgraded **immediately after** the protocol Hello exchange, gated on the negotiated base-protocol version (≥5). The exact "post-Hello timing" fukuii preserves.
  - go-ethereum `p2p/transport.go:159-160` / core-geth `p2p/transport.go:161` — pre-Hello base-protocol messages are bounded by `baseProtocolMaxMsgSize` (`"message too big"`); the per-message ceiling is the 24-bit frame length (RX-L6-03).
- **Q1 (appropriate?):** Yes — enabling Snappy only after Hello (so both peers agree the base version supports it) is mandatory; a decompressed-size bound is a decompression-bomb guard (R11).
- **Q2 (what to implement?):** The deferred-enable + bounded decompress. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct (gate on `their.Version >= snappyProtocolVersion`, upgrade the conn in place). Blast radius: the Snappy stage sits between frame and message-codec in the Streams-Tcp pipeline (flow-reviewed). **Under-verified detail:** the specific **"16MiB decompressed bound"** the plan attributes to core-geth was **not byte-located** in `core-geth/p2p/transport.go` in this pass — the located bounds are `baseProtocolMaxMsgSize` (pre-Hello) and the 24-bit frame ceiling. The decompressed-size cap belongs in the Snappy stage regardless; the plan should cite the concrete guard site rather than an unlocated 16MiB literal.
- **Q4:** n/a (the disposition is correct; the citation is thin).
- **Verdict:** CONFIRMS.
- **Plan edit (SHARPENS):** §3 — replace the bare "core-geth 16MiB decompressed bound" with the located bound (`baseProtocolMaxMsgSize` pre-Hello + the 24-bit `maxUint24` frame ceiling in `rlpx.go`); state the decompressed-size cap as an L6-owned Snappy-stage guard (R11 decompression-bomb bound) rather than a cited core-geth constant not present in the tree.

### RX-L6-05 · ECIES auth-tag constant-time compare (R11-2, L0 `constantTimeEquals` consumer); per-frame MAC plain-equals · Tier A (R11 security spine) · owner-layer L6
- **Plan claim / disposition:** §8 DoD (R11-2) + `requirements.md` R11 L6 cell ("ECIES-tag constant-time") — the ECIES auth-handshake tag/MAC comparison **must be constant-time** (L0 §7 names L6 the ECIES-tag const-time consumer); the per-frame integrity MAC **may** use plain equals ("the conditional standard" L0 §4 cites).
- **fukuii `july-fourth`:** (snapshot) keystore/compare paths historically used `==`; L0 exposes `constantTimeEquals`; L6 is the declared ECIES-tag consumer.
- **Reference source (byte-cited):**
  - besu `…/rlpx/handshake/ecies/ECIESEncryptionEngine.java:276` — `if (!Arrays.constantTimeAreEqual(T1, T2))` — the ECIES **auth tag (HMAC over the ciphertext)** is compared **constant-time**. The exact R11-2 precedent.
  - besu `…/rlpx/framing/Framer.java:321` — `if (!Arrays.equals(expectedMac, candidateMac))` — the **per-frame** integrity MAC uses **plain `Arrays.equals`** (not constant-time). Confirms the asymmetry the plan states: auth tag ⇒ const-time, frame MAC ⇒ standard equals.
- **Q1 (appropriate?):** Yes — a data-dependent compare on the auth tag is a timing side-channel (an attacker who can influence/observe the auth-message compare can forge); besu treats it exactly this way. The per-frame MAC being plain-equals is besu's deliberate "conditional standard" (per-frame keys rotate; the timing surface is negligible), so not gating it is correct, not lax.
- **Q2 (what to implement?):** Route the ECIES auth-tag compare through L0 `constantTimeEquals`; leave the per-frame MAC on plain equals. Correct — matches besu byte-for-byte.
- **Q3 (mechanism + blast radius?):** Mechanism correct (two distinct compare sites, two distinct policies). Blast radius: this closes L0 §7's declared L6 consumer link — a **downward dependency on the L0 R11 primitive** (`crypto`/`bytes`); if L6 hand-rolls `Arrays.equals` on the auth tag the R11 spine breaks silently (no compile error). The plan names the test ("the auth-tag compare is timing-safe", §8) — keep it as a DoD grep, not just prose.
- **Q4:** n/a.
- **Verdict:** CONFIRMS. (R11 security spine — the const-time site is a named L0→L6 structural link.)

---

## Group B — discovery / ENR / DNS

### RX-L6-06 · discv4/v5 wire in vendored `scalanet`; L6 = lifecycle/glue only · Tier C · owner-layer L6
- **Plan claim / disposition:** §1 + §9 — discv4/v5/ENR wire bodies live in the sibling vendored `scalanet` module; the L6 `network/discovery/` package is a lifecycle/glue wrapper. "Check go-ethereum `p2p/discover` for the protocol truth, not the wrapper."
- **fukuii `july-fourth`:** (snapshot) self-impl DNS + vendored discv4/v5 (scalanet), matching besu's "self-impl discv4, vendor discv5 (Teku)" split.
- **Reference source (byte-cited):**
  - go-ethereum `p2p/discover/{v4wire/,v5wire/,v4_udp.go,v5_udp.go,table.go}` — the discv4/v5 Kademlia + UDP wire truth (present in the tree). besu `p2p/discovery/{discv4,discv5,dns}/` mirrors the same split (self-discv4 / vendored-discv5).
- **Q1 (appropriate?):** Yes — keeping the audited discovery wire in scalanet and glueing at L6 is correct; re-implementing discv4/v5 would be a large unforced risk. The disposition (glue-only) is safe.
- **Q2 (what to implement?):** The lifecycle wrapper only. Correct (YAGNI on re-implementing the wire).
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: any wire-level discv4/v5/ENR bug is a **scalanet** bug, not an L6 bug — the plan states this so debugging aims at the right module. The **F8 authoring** side (RX-L6-27) is new L6 code and is the exception.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-07 · ENR (EIP-778) read + the `eth` ForkId entry · Tier B · owner-layer L6
- **Plan claim / disposition:** §1 (ENR EIP-778) + §3 (go-ethereum `p2p/enr/`; besu ENR `eth` ForkId entry) + §7 F8 (authored ENR round-trips with correct `eth` ForkId + networkId).
- **Reference source (byte-cited):**
  - go-ethereum `p2p/enr/{enr.go,entries.go}` — the ENR record + typed entries (present); `eth/protocols/eth/discovery.go` carries the `eth` ENR entry (the ForkId) used for pre-dial filtering.
  - besu ENR authoring path (`NodeRecordManager`/`DiscoveryPeer`, §4) — the JVM ENR reference; carries the `eth` ForkId entry.
- **Q1 (appropriate?):** Yes — ENR read is a DEFAULT capability; the `eth` ForkId entry in the ENR is what enables reject-before-dial (RX-L6-09).
- **Q2 (what to implement?):** ENR read now; ENR **authoring** is the F8 serve-side seam (RX-L6-27). Correct split.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: the ENR `eth` entry couples to ForkId (RX-L6-12) and networkId (RX-L6-28) — a wrong value here silently attracts wrong peers. Plan states the F8-side risk.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-08 · DNS discovery (EIP-1459) resolve; core-geth ETC DNS-tree citation · Tier B · owner-layer L6
- **Plan claim / disposition:** §1 (DNS discovery EIP-1459) + §3 (go-ethereum `p2p/dnsdisc/`; core-geth ETC `blockd.info` ENR-tree via `SetDNSDiscoveryDefaults2`).
- **Reference source (byte-cited):**
  - go-ethereum `p2p/dnsdisc/{client.go,tree.go,sync.go}` — the EIP-1459 DNS-tree client/resolver (present). The protocol truth for both resolve and (F8) author.
  - core-geth `params/bootnodes_classic.go:28-31` — `dnsPrefixETC = "enrtree://AJE62Q4DUX4QMMXEHCSSCSC65TDHZYSMONSD64P3WULVLSF6MRQ3K@"`, `ClassicDNSNetwork1 = dnsPrefixETC + "all.classic.blockd.info"`, `…2 = …"all.classic.etcdisco.net"`; `bootnodes_mordor.go:25-26` `MordorDNSNetwork1/2` (`all.mordor.blockd.info` / `…etcdisco.net`); `bootnodes.go:77` `KnownDNSNetwork(genesis, protocol)` is the accessor. **`SetDNSDiscoveryDefaults2` is NOT present** in the vendored core-geth tree.
- **Q1 (appropriate?):** Yes — DNS discovery resolve is a DEFAULT capability; the ETC DNS-tree is a real, live source (blockd.info/etcdisco.net).
- **Q2 (what to implement?):** Resolve now; DNS-tree **author** is the F8 seam. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: F8 authoring must produce a tree with the correct `eth` ForkId + networkId=1 (RX-L6-28) or it advertises the wrong network.
- **Q4:** The core-geth ETC DNS-tree symbol the plan cites (`SetDNSDiscoveryDefaults2`) does not exist in the vendored tree; the actual symbols are `KnownDNSNetwork` + `ClassicDNSNetwork1/2` + `dnsPrefixETC`. Source: `core-geth/params/{bootnodes.go:77,bootnodes_classic.go:28-31}`.
- **Verdict:** SHARPENS (citation correction).
- **Plan edit (SHARPENS):** §3 discovery-row ETC cell — replace `SetDNSDiscoveryDefaults2` with `KnownDNSNetwork` / `ClassicDNSNetwork1` (`enrtree://…@all.classic.blockd.info`) / `dnsPrefixETC`, per `core-geth/params/bootnodes_classic.go:28-31` + `bootnodes.go:77`.

### RX-L6-09 · `EnrForkIdFilter` reject-before-dial fork-ID filter · Tier B · owner-layer L6
- **Plan claim / disposition:** §6 "Preserved as correct" — the reject-before-decoder DNS/ENR fork-ID filter (`EnrForkIdFilter`), citing the real BSC/Core-Chain dial-slot-burning incident. §8 DoD: proven on a wrong-chain ENR.
- **Reference source (byte-cited):**
  - go-ethereum `eth/protocols/eth/discovery.go` + `core/forkid` `NewFilter`/`NewStaticFilter` — the ForkId filter applied to ENR-advertised `eth` entries pre-dial (the geth mechanism the filter mirrors). `forkid.go:114-129` `NewFilter`/`NewStaticFilter`.
- **Q1 (appropriate?):** Yes — filtering on the ENR ForkId before spending a dial slot is a proven optimization (the plan's cited BSC incident); DEFAULT-correct to preserve.
- **Q2 (what to implement?):** Preserve the `july-fourth` filter. Correct — no better alternative (this is the geth-standard pre-dial gate).
- **Q3 (mechanism + blast radius?):** Mechanism correct (reject an ENR whose `eth` ForkId fails the local filter, before dialing). Blast radius: depends on RX-L6-12 (the filter) + RX-L6-07 (the ENR entry). Plan states it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-10 · Static-nodes-only "enterprise mode": sysprop → HOCON key (R2) · Tier B · owner-layer L6
- **Plan claim / disposition:** §6.6 — old-fukuii's static-nodes-only mode is a JVM system property (`fukuii.network.discovery.use-bootstrap-nodes` via `System.getProperty`, `DiscoveryConfig.scala:52-54`), invisible in `application.conf`; L6 makes it a HOCON key (R2 — two instances can't select modes via a process-global sysprop).
- **fukuii `july-fourth`:** (snapshot) mode selected via `System.getProperty` — a process-global, breaks multi-instance.
- **Reference source (byte-cited):**
  - besu config (`CascadingDefaultProvider` / chain-spec loader, cross-ref `optimizations.md` L10 "Profile layer replaces sysprop") — besu selects network/mode via a config profile, never a process-global sysprop. The anti-pattern (a process-global `SignatureAlgorithmFactory`) is named in `optimizations.md` L0/L10.
- **Q1 (appropriate?):** Yes — a process-global sysprop is a direct R2 multi-instance violation (two `ChainInstance`s in one JVM cannot pick different modes). Moving it to HOCON is correct.
- **Q2 (what to implement?):** A per-instance HOCON key. Correct (YAGNI to keep the sysprop; it's strictly a defect).
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: R2 isolation (the same class as the `IORuntime` diamond, RX-L6-33, and the L10 profile layer). Plan states R2.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-11 · Cats-Effect `Resource` for the discovery service, bridged via Typed self-messages (TL1/TL2) · Tier B (STRUCTURAL R5) · owner-layer L6
- **Plan claim / disposition:** §5 — the vendored discovery service runs as `Resource[IO, DiscoveryService]`, started/stopped via `.allocated`/`release`, completion routed back through `ctx.self ! StartAttempt(...)`; never call actor state from an IO callback; `ctx.log` captured once on the actor thread (TL1/TL2). "The reusable pattern from old-fukuii `PeerDiscoveryManager`, preserved."
- **fukuii `july-fourth`:** (snapshot) `PeerDiscoveryManager` already bridges the CE `Resource` discovery service via self-messages — the pattern to preserve.
- **Reference source (byte-cited):**
  - `.agents/protocols/code-style/pekko-typed-api.md` TL1/TL2 (the fukuii Cats-Effect↔Typed integration rules) — the governing standard. besu `ServiceManager` DI (constructor-injected refs, §5) is the structural analogue for spawn-time wiring.
- **Q1 (appropriate?):** Yes — bridging an `IO`/`Resource` lifecycle through self-messages (not calling into actor state from an IO thread) is the correct effect-discipline; it's an `july-fourth` pattern worth preserving, not inventing.
- **Q2 (what to implement?):** Preserve the pattern, born-Typed. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: TL1/TL2 violations are the `pekko-resource-audit` class (dispatcher starvation, captured-log races); flow/mithril + the audit skill review it (§10). The `IORuntime` must be injected (RX-L6-33), not a per-builder `lazy val`. Plan states both.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Group C — ETH wire eth68–71 / ForkId

### RX-L6-12 · ForkId CRC32 (EIP-2124 block + EIP-6122 time) + `Next` + fork-checksum ruleset · Tier A (consensus-grade; forge/beacon co-sign) · owner-layer L6
- **Plan claim / disposition:** §1 (EIP-2124 + EIP-6122 CRC32 rolling checksum + the "3-rule compatibility state machine") + §3 (go-ethereum `core/forkid/forkid.go` "4-byte rolling CRC32 + `Next`, 4-rule filter"; core-geth `confp.BlockForks`/`TimeForks` = the ETC schedule) + §6 "Preserved as correct" (`ForkId.gatherBlockForks` with the Olympia-sentinel exception) + §7 FORKSCHEDULE-TIMESTAMP-MIGRATION + §8 DoD (golden vectors per network, forge ETC / beacon ETH). DEFAULT (all roles/networks), "non-optional network-citizenship."
- **fukuii `july-fourth`:** (snapshot) config-driven ForkId derivation (`ForkId.gatherBlockForks`/`gatherTimestampForks`) with an Olympia-sentinel exception; decoder-complete.
- **Reference source (byte-cited):**
  - go-ethereum `core/forkid/forkid.go:74-98` `NewID` — `hash := crc32.ChecksumIEEE(genesis.Hash().Bytes())`; for each passed block-fork then time-fork `hash = checksumUpdate(hash, fork)`; return `{Hash: checksumToBytes(hash), Next: fork}`. `checksumUpdate:227-231` = `crc32.Update(hash, IEEETable, big-endian-uint64(fork))`; `checksumToBytes:234-238` = big-endian uint32. **The exact CRC32 rolling algorithm.**
  - go-ethereum `forkid.go:155-222` `newFilter` — the validator comment enumerates **rules 1 (+1a/1b), 2, 3, 4** where **rule 4 = "Reject in all other cases"** (`:174`). So it is a **4-rule** ruleset (3 accept-rules + reject-otherwise), not 3.
  - go-ethereum `forkid.go:242-296` `gatherForks` — **load-bearing byte details:** (i) **skip block-0 forks** (`forksByBlock[0] == 0 → drop`, the genesis ruleset, `:288-290`); (ii) **skip forks ≤ genesis time** (`forksByTime[0] <= genesis → drop`, `:292-294`); (iii) **dedup consecutive-equal** fork numbers (`:275-286`); (iv) sort both lists. `newFilter:147-153` appends `math.MaxUint64` sentries (a separate block-list sentry when `len(forksByTime)==0`).
  - go-ethereum `forkid.go:47-51,192` — `timestampThreshold = 1438269973` (ETH mainnet genesis ts) splits block-`Next` from time-`Next` in the single `Next` field. (ETC fork **blocks** are ≪ 1.4e9 and ETC's Olympia **timestamp** ≫ 1.4e9, so the hack holds for ETC too.)
  - core-geth `core/forkid/forkid.go:71-235` — **byte-identical algorithm** (same `crc32.ChecksumIEEE`, `checksumUpdate`, `checksumToBytes`, `timestampThreshold=1438269973`, same `newFilter`); the only difference is `gatherForks:238-240` = `confp.BlockForks(config), confp.TimeForks(config, genesis)` (config-driven) vs geth's reflection. **Confirms: the CRC32 algorithm is network-neutral; only the fork schedule differs per network** — exactly the plan's authority split (geth = format, core-geth = ETC schedule).
- **Q1 (appropriate?):** Yes — DEFAULT/non-optional is correct; ForkId is mandatory network-citizenship and a byte error partitions the network.
- **Q2 (what to implement?):** The CRC32 rolling checksum + `Next` + the filter ruleset, per-network schedule. Correct — no alternative (EIP-2124/6122 are finalized/locked).
- **Q3 (mechanism + blast radius?):** Mechanism correct **but the plan's rule-count is internally inconsistent** — §1 and §8 say "3-rule," §3 says "4-rule"; geth's canonical comment documents **4 rules** (rule 4 = reject-otherwise). Blast radius (large, mostly stated): (a) ForkId is computed over the **L5 fork schedule** — a wrong schedule → wrong hash → partition; the per-network golden vectors are **forge (ETC) / beacon (ETH) co-signed** (§8, §9). (b) The `eth` ENR entry (RX-L6-07) and the pre-dial filter (RX-L6-09) consume it. (c) The **byte details** above (genesis-fork skip, ≤-genesis time skip, consecutive-dedup, the block/time sentry split, the 1438269973 threshold) are each a chain-partition hazard if fukuii's `gatherBlockForks`/`gatherTimestampForks` diverge — the plan preserves the derivation but does not enumerate these guards.
- **Q4 (correct answer):** Call it the geth-canonical **4-rule** ruleset (3 accept-rules + reject-otherwise) everywhere. And **enumerate the byte-guards** as DoD vectors: block-0 fork skip, time-fork-≤-genesis skip, consecutive-equal dedup, `MaxUint64` sentry (+ block-only sentry when no time forks), and the `timestampThreshold=1438269973` block-vs-time `Next` split (with the note that ETC block numbers are safely below it). Source: go-ethereum + core-geth `forkid.go` (byte-identical).
- **Verdict:** SHARPENS. **forge (ETC) / beacon (ETH) co-sign at build** — golden ForkId vectors per network are the gate that must not be waved through (§9).
- **Plan edit (SHARPENS):** (1) §1 + §8 — change "3-rule compatibility state machine" to **"4-rule fork-checksum ruleset (3 accept-rules + reject-otherwise)"** to match §3 and geth's `forkid.go:155-222` comment. (2) §7 FORKSCHEDULE-TIMESTAMP-MIGRATION row / §8 DoD — list the byte-guard vectors: block-0 skip, time-fork-≤-genesis skip, consecutive-equal dedup, `MaxUint64` sentry (+ block-only sentry when `forksByTime` empty), and the `1438269973` block-vs-time `Next` threshold; note ETC block numbers sit below the threshold so the same hack carries ETC's Olympia timestamp fork.

### RX-L6-13 · Full advertised wire range ETH68/69/70/71 per-network (F10) · Tier A (DEFAULT F10) · owner-layer L6
- **Plan claim / disposition:** §2 (eth/68 retained deliberately; advertise 68/69/70/71 + eth/72 landing slot) + §6.4–6.5 + `feature-ledger.md` F10 + `optimizations.md` §L6 "Full wire range ETH68-71" — ETC = 68-frozen (core-geth), ETH = 69/70/71 rolling; the advertised set is contributed per `NetworkFamily`.
- **fukuii `july-fourth`:** (snapshot) decoders exist for 68/69/70; eth/70 handshake-unwired (RX-L6-15); no eth/71.
- **Reference source (byte-cited):**
  - go-ethereum `eth/protocols/eth/protocol.go:32-48` — `ETH69/70/71` consts; `ProtocolVersions = []uint{ETH71, ETH70, ETH69}` — **geth HEAD advertises ONLY 69/70/71; ETH68 is fully removed from the protocol package** (not merely "dropped from default"). `protocolLengths = {ETH71:20, ETH69:18, ETH70:18}`.
  - besu `EthProtocolVersion.java:28-31,98-105` — `V68/V69/V70/V71` all defined; `getSupportedMessages` returns lists for all four → **besu retains and advertises 68**. `EthProtocol.messageSpace:69` returns `V68→17, V69/V70→18, V71→20` (matching geth's spans for 69/70/71).
  - reth `crates/net/eth-wire-types/src/version.rs:26-45,72` — `Eth68=68 … Eth72=72`; `has_eth68_metadata` includes 68 → reth still carries 68.
- **Q1 (appropriate?):** Yes — DEFAULT/F10 is correct, and the evidence **strengthens** the "do not drop 68" position: geth-HEAD has *removed* 68 entirely, but besu (68-71) and reth (still carries 68) retain it — fukuii deliberately aligns with the JVM-implementation guide (besu) and diverges from geth-HEAD, exactly as §2 argues (the geth removal is an internal receipt-encoding cleanup, not a wire-compat signal).
- **Q2 (what to implement?):** Advertise 68/69/70/71 per-network. Correct — the per-network split (ETC=68-frozen, ETH=69/70/71) is the right shape and is contributed through `NetworkFamily` (RX-L6-20).
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: the advertised set ripples to L5 (`NetworkFamily` contributes it) and L7 (sync negotiates over it); a version advertised-but-unwired burns handshakes (the eth/70 `july-fourth` bug, RX-L6-15). Plan states it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-14 · eth/68 retention is deliberate (do not "modernize" it away) · Tier B · owner-layer L6
- **Plan claim / disposition:** §2 note + §9 risk — eth/68 is retained deliberately; dropping it to match geth-HEAD reduces peer compatibility; the SR is explicit this is a geth-internal cleanup, not a wire signal.
- **Reference source (byte-cited):**
  - go-ethereum `protocol.go:44` — `ProtocolVersions = {ETH71, ETH70, ETH69}` (68 removed from geth). besu `EthProtocolVersion.java:34-48` — `eth68Messages` still defined + advertised. reth `version.rs:72` — `has_eth68_metadata` still includes 68.
- **Q1 (appropriate?):** Yes — 2-of-3 maintained non-fukuii clients (besu, reth) still speak 68; retaining it maximizes peer reach on a low-population network (ETC). Correct call.
- **Q2 (what to implement?):** Keep 68 in the advertised set. Correct (dropping it is a peer-loss, not a modernization).
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: purely additive (one more advertised capability); no consensus impact.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-15 · ETH70 handshake arm + EIP re-attribution (7706→7975) + reuses eth/69 message list · Tier B · owner-layer L6
- **Plan claim / disposition:** §6.4 (build the `EthNodeStatus70ExchangeState` arm so a negotiated eth/70 reaches Connected; **eth/70 reuses eth/69's message list** — `protocolLengths[ETH70]=18`, same as eth/69; the change is partial-block-receipt serving via `lastBlockIncomplete`/`RequestPartialReceipts`, not new codes) + §6.5 (fix `ETHPackets.scala:279`'s docstring mislabel of eth/70 as **EIP-7706** → geth attributes eth/70 to **EIP-7975**) + §7 (two rows).
- **fukuii `july-fourth`:** (snapshot) `ETH70MessageDecoder`/`Status70`/`p.eth70` exist and two peers *negotiate* `Capability.ETH70`, but `HelloExchangeState:72-108` has only ETH68/ETH69 arms → a negotiated eth/70 falls through to `IncompatibleP2pProtocolVersion` and disconnects. `ETHPackets.scala:279` docstring says "EIP-7706."
- **Reference source (byte-cited):**
  - go-ethereum `eth/protocols/eth/protocol.go:48` — `protocolLengths = {ETH71:20, ETH69:18, ETH70:18}` → **eth/70 has the same 18-code span as eth/69**. `:219-247` — `GetReceiptsPacket70{RequestId, FirstBlockReceiptIndex, GetReceiptsRequest}` + `ReceiptsPacket70{RequestId, LastBlockIncomplete bool, List}` — eth/70 is **partial block-receipt serving** (a `FirstBlockReceiptIndex` request offset + a `LastBlockIncomplete` resume flag), **no new message codes**.
  - besu `EthProtocolVersion.java:101` — `case V69, V70 -> eth69Messages` — besu **shares the identical message list** between eth/69 and eth/70; `EthProtocol.messageSpace:69` — `V69, V70 → 18`. Byte-confirms "eth/70 reuses eth/69's list."
  - fukuii-external corroboration of the mislabel: the **herald agent charter** currently states "ETH70 (EIP-7706)" (per the agent description) — the same wrong attribution the plan flags at `ETHPackets.scala:279` and §10.
- **Q1 (appropriate?):** Yes — building the missing handshake arm (so an advertised version actually connects) and fixing a wrong EIP attribution are both correct, non-optional (a version you advertise but can't complete burns peers).
- **Q2 (what to implement?):** The `EthNodeStatus70ExchangeState` arm + the partial-receipt serving semantics (buffer + `lastBlockIncomplete`), **not** a new decoder set (the codes are eth/69's). Correct — the multi-client read (geth spans + besu shared list) confirms no new codes.
- **Q3 (mechanism + blast radius?):** Mechanism correct (eth/70 = eth/69 codes + partial-receipt serving semantics). Blast radius: (a) L7 sync consumes the partial-receipt resume (`FirstBlockReceiptIndex`/`lastBlockIncomplete`); (b) the EIP mislabel has **propagated into the herald agent charter** — §10 already schedules the charter fix, RX confirms the propagation is real. The plan's own §6.5 correction (7706→7975) is the fix; RX corroborates it against geth's semantics (partial receipt lists ⇒ 7975, not 7706).
- **Q4:** n/a — the plan already carries the correct answer (build the arm; 7706→7975; reuse eth/69 codes). RX confirms it against source + flags the charter propagation.
- **Verdict:** CONFIRMS (the plan's fix is source-correct). Fold the charter-propagation note.
- **Plan edit (SHARPENS):** §10 herald row — keep the "ETH70=EIP-7975 (not 7706), eth/71=EIP-8159" charter correction; RX confirms the wrong "EIP-7706" attribution is **live in the herald agent description**, so the §10 charter repoint is load-bearing, not cosmetic.

### RX-L6-16 · eth/71 EIP-8159 Block Access Lists message pair `0x12`/`0x13` (WIRE-ETH71-PARITY) · Tier A (DEFAULT F10) · owner-layer L6
- **Plan claim / disposition:** §6.5 + §7 WIRE-ETH71-PARITY — add eth/71 (**EIP-8159 Block Access Lists**): `GetBlockAccessListsMsg=0x12` / `BlockAccessListsMsg=0x13`, raising the code count to 20; **partial serving-only at geth HEAD** (`a484a8506`) — land the message pair + serve side, gate the requester.
- **Reference source (byte-cited):**
  - go-ethereum `eth/protocols/eth/protocol.go:71-72` — `GetBlockAccessListsMsg = 0x12`, `BlockAccessListsMsg = 0x13`; `:48` `protocolLengths[ETH71]=20` (eth/69's 18 + the 2 BAL codes). `:295-311` — `GetBlockAccessListsPacket{RequestId, GetBlockAccessListsRequest}`, `BlockAccessListPacket{RequestId, List rlp.RawList[RawBlockAccessList]}` (BAL = `bal.AccountAccess` lists).
  - besu `EthProtocolVersion.java:73-90` — `eth71Messages` = eth/69's 14 messages + `GET_BLOCK_ACCESS_LISTS` + `BLOCK_ACCESS_LISTS`; `EthProtocolMessages` `GET_BLOCK_ACCESS_LISTS=0x12`, `BLOCK_ACCESS_LISTS=0x13`; `EthProtocol.messageSpace:V71→20`. **besu and geth agree byte-for-byte on `0x12`/`0x13` and span 20.**
  - reth `version.rs:39` — `Eth71=71` present in the codec.
- **Q1 (appropriate?):** Yes — DEFAULT/F10 for ETH; the codes are fixed by EIP-8159 and geth+besu agree, so byte-parity is mandatory. Gating the requester (serve-only, since it's partial at geth HEAD) is the correct caution.
- **Q2 (what to implement?):** The `0x12`/`0x13` pair + serve side, requester gated. Correct — matches geth's "partial serving-only" state.
- **Q3 (mechanism + blast radius?):** Mechanism correct (`0x12`/`0x13`, span 20). Blast radius: this is an **ETH-only** capability (BAL is an ETH-side feature); it must be contributed through the ETH `NetworkFamily` (RX-L6-20), never advertised for ETC. The BAL payload type (`bal.AccountAccess`) couples to L4/L5 (block access lists are an execution artifact) — but at L6 only the wire codec + serve path lands. Plan states the serve-only gating.
- **Q4:** n/a.
- **Verdict:** CONFIRMS. (Wire byte-exactness held consensus-grade; interop-tested vs a geth-71 peer, §7.)

### RX-L6-17 · eth/72 (EIP-8070) forward landing slot · Tier C (OPTIONAL forward) · owner-layer L6
- **Plan claim / disposition:** §1 (the eth/72 landing slot) + §6.5 — "reth's codec already carries eth/72 (EIP-8070) — the leading edge, advertised nowhere by default → fukuii's forward landing slot."
- **Reference source (byte-cited):**
  - reth `crates/net/eth-wire-types/src/version.rs:42-45` — `Eth72 = 72` with the comment "[EIP-8070] adds sparse blobpool"; `capability.rs:128-130` `EthVersion::Eth72` capability constructor. **Confirms reth carries eth/72 = EIP-8070.**
  - go-ethereum `protocol.go` — **no ETH72 const** (geth HEAD stops at 71); besu — no V72. So eth/72 is reth-only leading-edge, advertised by no default set (reth's `ALL_VERSIONS` = 66-69 here) — matches "advertised nowhere by default."
- **Q1 (appropriate?):** Yes — a landing slot (not built, reserved) for eth/72 is the right OPTIONAL-forward posture; building it now would be YAGNI (only reth has it, not advertised).
- **Q2 (what to implement?):** Nothing yet — reserve the slot in the multiplexer's version enum so adding it later is additive (RX-L6-18/20). Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct (eth/72 = sparse blobpool, EIP-8070, ETH-side). Blast radius: none until built; the generic multiplexer must not hardcode a max version (RX-L6-19 config-bounded range covers this).
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-18 · Generic range-advertising wire multiplexer (besu `CapabilityMultiplexer`); kill the hand-picked `0x30` SNAP offset · Tier A (DEFAULT; structural) · owner-layer L6
- **Plan claim / disposition:** §2 + §4 + §5 + §6.3 + `optimizations.md` §L6 row 1 — a generic `List[SubProtocol]`→disjoint-RangeMap multiplexer (besu shape), offsets **computed** not hand-picked; replaces old-fukuii's `Capability.best()`-collapse + the hand-computed two-family `InboundTranslator` with a **hardcoded `0x30` SNAP offset** that already needed one manual widening. §9: "the hand-picked SNAP-offset trap is a design smell to eliminate, not reproduce."
- **fukuii `july-fourth`:** (snapshot) `Capability.best()` collapses negotiation to a single `Option[Capability]` (`Capability.scala:85-104`); SNAP recovered as a side-channel via a hand-computed `InboundTranslator` special-cased to two families (`RLPxConnectionHandler.scala:346-397`) with a hardcoded `0x30` SNAP offset (`SNAP.scala:21-29`) that needed a manual widening to dodge eth/69 `BlockRangeUpdate`.
- **Reference source (byte-cited):**
  - besu `CapabilityMultiplexer.java:34` — `WIRE_PROTOCOL_MESSAGE_SPACE = 16` (p2p wire reserves codes 0–15); `:118-145` `calculateAgreedCapabilities` — starting `offset = 16`, per agreed subprotocol `builder.put(Range.closedOpen(offset, offset + messageSpace), cap); offset += messageSpace` — **offsets are cumulative/computed**, one disjoint `Range` per subprotocol, in an `ImmutableRangeMap`. `:66-91` `multiplex`/`demultiplex` add/subtract `capabilityOffsets.get(cap)` / `-range.lowerEndpoint()`. **The exact "computed disjoint RangeMap" the plan wants.**
  - besu `CapabilityMultiplexer.java:35-36,128-135` — `CAPABILITY_COMPARATOR` sorts name-asc then version-**desc**, and skips a lower version of an already-seen protocol (`equalsIgnoreCase(prevProtocol) → continue`) — so besu **still selects the best version *per subprotocol***, but keeps **all** subprotocols (eth **and** snap) simultaneously, each with its own computed range. besu `EthProtocol.messageSpace` supplies the per-version span (17/18/18/20) — the offset reservation includes the reserved `0x0b–0x0e` gap.
- **Q1 (appropriate?):** Yes — DEFAULT/structural is correct; a generic computed-offset RangeMap is strictly better than a two-family hand-computed translator, and it is the single fix for the `0x30`-collision class.
- **Q2 (what to implement?):** The generic N-subprotocol RangeMap. Correct. **But the plan's framing "replaces `best()`-collapse" needs one nuance:** besu *also* picks best-version-per-subprotocol (like fukuii's `best()`) — the real improvement is that besu keeps **all subprotocols** (eth+snap) simultaneously with computed disjoint ranges, whereas fukuii collapses to **one** capability total then bolts snap back with a hand-picked offset. The win is "N-subprotocol computed multiplex," not "eliminate best-per-protocol selection."
- **Q3 (mechanism + blast radius?):** Mechanism correct. **Load-bearing detail to fold:** the per-subprotocol `messageSpace` is the **code-SPAN (offset)** — besu returns 17/18/18/20 (= geth `protocolLengths` for 69/70/71), which **includes the reserved `0x0b–0x0e` gap**, not the count of live messages (13/14/14/16). If fukuii reserves the live-message *count* instead of the span, every downstream subprotocol's offset shifts and the wire codes desync. And `WIRE_PROTOCOL_MESSAGE_SPACE=16` (offsets start at 16, not 0). Blast radius: the multiplexer is where `NetworkFamily` injects the advertised set (RX-L6-20); the SNAP subprotocol rides the same RangeMap (retiring the `0x30` special-case); a wrong span here is an interop partition. Plan states the anti-`0x30` intent (§9) but not the span-vs-count subtlety.
- **Q4 (correct answer):** (i) Reframe "replaces `best()`-collapse" as "replaces the single-`best()` + hand-computed two-family translator with a generic **N-subprotocol** computed-offset RangeMap (besu keeps best-per-subprotocol, but keeps eth **and** snap simultaneously)." (ii) The per-version `messageSpace` **must be the code-span** (17/18/18/20, incl. the `0x0b–0x0e` gap), and offsets start at `WIRE_PROTOCOL_MESSAGE_SPACE=16`. Source: `CapabilityMultiplexer.java:34,118-145` + `EthProtocol.messageSpace:69`.
- **Verdict:** SHARPENS.
- **Plan edit (SHARPENS):** §6.3 / §4 — (1) reframe the `best()`-collapse improvement as "generic **N-subprotocol** computed-offset RangeMap keeping eth+snap simultaneously" (besu still selects best-per-subprotocol via `CAPABILITY_COMPARATOR`). (2) §5 / §9 — state that per-subprotocol `messageSpace` is the **wire code-span** (eth/69=18, eth/71=20, incl. the `0x0b–0x0e` reserved gap), offsets begin at `WIRE_PROTOCOL_MESSAGE_SPACE=16` — reserving the live-message *count* instead of the span desyncs downstream offsets (the same collision class as the `0x30` smell).

### RX-L6-19 · Config-bounded advertised range (`getMin/MaxEthCapability`) · Tier C (OPTIONAL R2 enterprise) · owner-layer L6
- **Plan claim / disposition:** §2 + §7 + `optimizations.md` §L6 — cap the advertised wire window from config so a fleet stages a new version without a code change; build the cap **seam** now, ship the mode role-gated (besu `getMin/MaxEthCapability`).
- **Reference source (byte-cited):**
  - besu `CapabilityMultiplexer` (`getAgreedCapabilities`) + the eth subprotocol's min/max capability bounds — the advertised set is a configured range, not a hardcoded list; besu caps the window via config. (The `getMin/MaxEthCapability` accessors live on the eth-subprotocol config; the multiplexer negotiates over whatever range is supplied via `List<Capability> a`.)
- **Q1 (appropriate?):** Yes — OPTIONAL(R2 enterprise) with the seam built now is correct; a staged fleet rollout (advertise vN-1 while vN bakes) is a real enterprise need, and the seam is cheap given the generic multiplexer already takes a capability list.
- **Q2 (what to implement?):** The config-bounded range as a cap over the multiplexer's input list. Correct — the multiplexer (RX-L6-18) already accepts a `List<Capability>`, so the cap is just filtering that list from config; no transport change.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: R2 (per-instance config, not a global) — ties to RX-L6-10/33 (no process-global mode); the cap must be per-`ChainInstance`. Plan states R2.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-20 · Advertised set contributed through the `NetworkFamily` typeclass, never `isEtc()`/`isPoS()` (R3) · Tier A (structural spine R3) · owner-layer L6
- **Plan claim / disposition:** §2 (Advertised ≠ implemented decoupling) + §5 (the wire-version registry as a Scala-3 `given`/enum seam; each family contributes its set through the `given`; ETC→{68}, ETH→{69,70,71}) + §9 (the multiplexer must inject through the typeclass, never a shared reader; erigon `FrozenBorBlocks` is the counter-example) + `requirements.md` R3 L6 cell ("neutral wire").
- **Reference source (byte-cited):**
  - besu `CapabilityMultiplexer.java:42-49,118-122` — the multiplexer takes `List<SubProtocol>` + two `List<Capability>` and is **family-blind**: it negotiates whatever capability lists it is handed, with **no** `isEtc()`/`isMainnet()` branch. The advertised set is contributed by the caller (per-network config), exactly the typeclass-injection shape.
  - erigon `FrozenBorBlocks` (the named counter-example, `optimizations.md`/SR) — a per-family counter baked into shared code = the leak R3 forbids; besu's multiplexer avoids it by construction (caller supplies the list).
- **Q1 (appropriate?):** Yes — R3 family-neutrality via typeclass injection is a structural spine item; besu proves the multiplexer needs no family branch (it consumes a supplied list).
- **Q2 (what to implement?):** The `given NetworkFamily` contributes the advertised capability set; the multiplexer stays family-blind. Correct — this is the B7.0.5 `NetworkFamily` typeclass surfacing at the wire layer (memory `consult-sr-research-before-design`: the typeclass already designs this).
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: the advertised set couples to L5 (`NetworkFamily` is defined at L5, contributed down/consumed at L6) and L7 (sync negotiates over it); a `isEtc()` branch inside the multiplexer is the exact R3 leak. The grep DoD (§8: "no `isEtc()`/`isPoS()` in the multiplexer") enforces it. Plan states it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS. (Structural spine — the `NetworkFamily` wire-seam depth ripples to L5/L7.)

### RX-L6-21 · eth/69 no-TD Status + `BlockRangeUpdate`; `NewPooledTransactionHashes` equal-length; `GetNodeData`/`NodeData` rejection (EIP-4938) · Tier B · owner-layer L6
- **Plan claim / disposition:** §8 DoD — wire round-trips byte-exact incl. the eth/69 **no-TD Status** + `BlockRangeUpdate`, the `NewPooledTransactionHashes` **equal-length** check, and `GetNodeData`/`NodeData` **rejection** (EIP-4938).
- **Reference source (byte-cited):**
  - go-ethereum `eth/protocols/eth/protocol.go:93-103` — `StatusPacket{ProtocolVersion, NetworkID, Genesis, ForkID, EarliestBlock, LatestBlock, LatestBlockHash}` — **no `TotalDifficulty` field** (removed in eth/69; the block-range fields replace it). `:288-293` `BlockRangeUpdatePacket{EarliestBlock, LatestBlock, LatestBlockHash}` (code `0x11`).
  - go-ethereum `protocol.go:252-257` — `NewPooledTransactionHashesPacket{Types []byte, Sizes []uint32, Hashes []common.Hash}` — three parallel arrays that **must be equal-length** (the eth/68 announcement-metadata format; the equal-length invariant is the decode check).
  - go-ethereum `protocol.go:56-73` — the message-code table has a **gap at `0x0b–0x0e`** (no `GetNodeData`/`NodeData` — removed by **EIP-4938** in eth/67); a peer sending those codes is out-of-range for the negotiated protocol span and is rejected. besu `EthProtocolMessages` has the same gap (no NodeData codes).
- **Q1 (appropriate?):** Yes — these are DEFAULT byte-exact wire requirements; each is a real interop check.
- **Q2 (what to implement?):** All three, byte-exact vs geth+besu. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct (no-TD Status is the visible eth/69 change; equal-length is a decode invariant; NodeData rejection is by-absence from the code span). Blast radius: eth/69's Status shape ties to the multiplexer span (RX-L6-18) and L7 (the block-range fields feed pivot selection); the equal-length check is an L8 txpool-announce concern surfaced at the wire decode. Plan lists them in the DoD.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Group D — blacklist / ban policy (L6 owns the type → owns the policy)

### RX-L6-22 · Move `Blacklist`/`BlacklistId` into `network` (break the network↔blockchain cycle) · Tier B (STRUCTURAL) · owner-layer L6
- **Plan claim / disposition:** §6.2 + `requirements.md` R11 — old-fukuii's `ServerActor`/`PeerManagerActor`/`Peer` (via `PeerId extends BlacklistId`) import `com.chipprbots.ethereum.blockchain.sync.Blacklist` (an upstream package consuming a downstream type — the `dependency-graph.md:51` back-edge). L6 **owns** `Blacklist`/`BlacklistId`; L7 sync depends *down* on it. Owning the type ⇒ owning the policy (§7).
- **fukuii `july-fourth`:** (snapshot) `Blacklist` lives in `blockchain.sync` while `network` types extend `BlacklistId` — the network↔blockchain 2-cycle back-edge.
- **Reference source (byte-cited):**
  - besu `PeerPermissionsDenylist.java` + `PeerDenylistManager.java` live in `ethereum/p2p/{permissions,network}/` — **the denylist type is owned by the p2p (network) layer**, and consumers depend down on it. The structural proof that the ban type belongs at the network layer, not sync.
- **Q1 (appropriate?):** Yes — moving the type down to `network` (where besu keeps it) is the correct cycle-break; an upward edge (sync→network for the type) becomes a compile error (§"Upward edges are compile errors").
- **Q2 (what to implement?):** `Blacklist`/`BlacklistId` in `network`; L7 depends down. Correct — matches besu's layering exactly.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: **ripples to L7** (sync's blacklist consumers now depend down on the L6 type — the plan states it: "L7 sync depends *down* on it"); and owning the type pulls the **policy** decision to L6 (RX-L6-23/24). Correctly stated.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-23 · Direction-keyed ban policy (besu `PeerDenylistManager`): self-vs-received `BreachOfProtocol` asymmetry, node-ID keying, LRU-bound, maintained-exempt, cleared-on-restart · Tier A (R11 security) · owner-layer L6
- **Plan claim / disposition:** §7 blacklist-policy row + §4 + `optimizations.md` §L6 — adopt besu `PeerDenylistManager` **direction-asymmetry** (self-detected `BreachOfProtocol` denylists; a *received* one does **not**); node-ID vs IP keying; LRU-bound (≤500) + maintained-peer exemption; direction-keyed durations (nethermind `Local`/`Remote` tables, reth flat received-penalty). Wire the two self-detected `BreachOfProtocol` sites (`PeerActor.scala:578`, `RLPxConnectionHandler.scala:445`, NETWORK-03).
- **fukuii `july-fourth`:** (snapshot) IP-keyed, 365-day `Permanent` tier (RX-L6-24); the two self-detected `BreachOfProtocol` sites not yet wired to blacklist.
- **Reference source (byte-cited):**
  - besu `PeerDenylistManager.java:31-36` — `locallyTriggeredDisconnectReasons = {BREACH_OF_PROTOCOL, INCOMPATIBLE_P2P_PROTOCOL_VERSION}`; `remotelyTriggeredDisconnectReasons = {INCOMPATIBLE_P2P_PROTOCOL_VERSION}`. `:68-71` `shouldBlock = (!initiatedByPeer && locallyTriggered.contains(reason)) || (initiatedByPeer && remotelyTriggered.contains(reason))`. **So: self-detected `BREACH_OF_PROTOCOL` → denylist; a peer-initiated (received) `BREACH_OF_PROTOCOL` → NOT denylisted; `INCOMPATIBLE_P2P_PROTOCOL_VERSION` → denylisted in BOTH directions.** Exactly the plan's asymmetry — plus the `INCOMPATIBLE` both-directions rule the plan omits.
  - besu `PeerDenylistManager.java:55-64` — `maintainedPeers.contains(connection.getPeer()) → skip` (maintained-peer exemption). besu `PeerPermissionsDenylist.java:38-42` — `LimitedSet.create(initialCapacity, maxSize, Mode.DROP_LEAST_RECENTLY_ACCESSED)` (**LRU eviction**); `:58-66` keyed by `remotePeer.getId()` = **node-ID** (not IP); `:92-95` `close() = denylist.clear()` (**in-memory, cleared on restart**).
  - nethermind `NodeStatsLight.cs` (direction/reputation param tables) + reth `crates/net/network-types/` `ReputationChangeWeights` — the direction-keyed-duration / flat-received-penalty cross-checks.
- **Q1 (appropriate?):** Yes — DEFAULT/schedule-the-policy is correct; besu's direction-asymmetry is a real anti-abuse design (a hostile peer can't get itself denylisted by *claiming* a breach, only a self-*detected* breach denylists), and node-ID keying (vs fukuii's IP keying) resists NAT/shared-IP collateral. Adopting it is the right call.
- **Q2 (what to implement?):** besu's `shouldBlock` direction rule + node-ID keying + LRU-bound + maintained-exempt + cleared-on-restart, with fukuii's ETC/Mordor snap-scarcity carve-out (RX-L6-25). Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct **but the plan under-states the rule**: besu denylists on **`INCOMPATIBLE_P2P_PROTOCOL_VERSION` in BOTH directions** (it's in both sets), not just `BreachOfProtocol` (self-only) — the plan names only the `BreachOfProtocol` asymmetry. Blast radius: **ripples to L7** (sync depends down on the blacklist type/policy — a too-harsh policy partitions a low-pop network from its few snap servers, §9); node-ID keying couples to `domain` `PeerId` (RX-L6-22). The two NETWORK-03 wiring sites are named. The "LRU-bounded 500" mechanism is confirmed (`DROP_LEAST_RECENTLY_ACCESSED`), but the **`500` literal was not located** — it is a caller-supplied `maxSize` (`PeerPermissionsDenylist.create(maxSize)`), not a constant in these files; the plan should cite the construction site or state it as fukuii's chosen cap.
- **Q4 (correct answer):** Fold the **full besu rule**: `{BREACH_OF_PROTOCOL, INCOMPATIBLE_P2P_PROTOCOL_VERSION}` denylist when **self-initiated**; `{INCOMPATIBLE_P2P_PROTOCOL_VERSION}` denylist when **peer-initiated** — i.e. `INCOMPATIBLE` denies both directions, `BREACH_OF_PROTOCOL` denies self-only. Source: `PeerDenylistManager.java:31-36,68-71`. And treat "LRU-bounded 500" as a fukuii-chosen `maxSize` (mechanism = `Mode.DROP_LEAST_RECENTLY_ACCESSED`), not a besu literal.
- **Verdict:** SHARPENS. (R11 security — direction rule + keying feed the peer-DoS surface.)
- **Plan edit (SHARPENS):** §7 blacklist row / §4 — (1) state the full besu direction rule: self-initiated `{BREACH_OF_PROTOCOL, INCOMPATIBLE_P2P_PROTOCOL_VERSION}` deny; peer-initiated `{INCOMPATIBLE_P2P_PROTOCOL_VERSION}` deny (so `INCOMPATIBLE` is a both-directions denylist, `BREACH` self-only) — `PeerDenylistManager.java:31-36,68-71`. (2) Mark "LRU-bounded 500" as a fukuii-chosen `maxSize` over besu's `LimitedSet` `DROP_LEAST_RECENTLY_ACCESSED` mechanism (`PeerPermissionsDenylist.java:38-42`), not a besu constant. (3) confirm node-ID keying (`remotePeer.getId()`) + cleared-on-restart (`close()=clear()`).

### RX-L6-24 · The 365-day `Permanent` ban tier — ratify or re-scope (the ~2-orders-of-magnitude outlier) · Tier B · owner-layer L6
- **Plan claim / disposition:** §6.2 + §7 + §9 — fukuii's 365-day `Permanent` tier is a ~2-orders-of-magnitude outlier (next-longest is reth's 12h; geth's is a flat 35s cooldown); L6 must **ratify or re-scope** it with a recorded decision, not silently inherit. Neither reference extreme (geth 35s / reth 12h) transfers unchanged given ETC/Mordor snap-server scarcity.
- **Reference source (byte-cited):**
  - besu `PeerPermissionsDenylist.java:92-95` — `close() = denylist.clear()` → **besu's denylist is in-memory, cleared on restart** (no persistent 365-day tier at all). besu has no permanent ban.
  - core-geth p2p — the flat 35s inbound cooldown (`inboundThrottleTime = 30 * time.Second`, go-ethereum `server.go:53`, the geth family's only "ban") is the **floor** (no ban tier). reth — a 12h max penalty (`network-types` reputation). **All three references sit at seconds-to-hours; fukuii's 365 days is the outlier.**
- **Q1 (appropriate?):** Yes — flagging it for ratify/re-scope is the right posture; a 365-day tier on a low-population network risks permanently partitioning a node from scarce snap servers after a transient fault. The **decision is operator-owned** (a security-vs-connectivity tradeoff), so RX surfaces the evidence, doesn't decide it.
- **Q2 (what to implement?):** Re-scope toward besu (cleared-on-restart) / reth (bounded hours) semantics with an ETC/Mordor-specific carve-out, decision recorded. The multi-client read strongly suggests re-scoping (no reference keeps a permanent tier), but the final cap is an operator call.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: **R11 security + L7 connectivity** — a too-harsh ban partitions the node (fewer sync sources); interacts with the `snapCapableHosts` exemption (RX-L6-25). Plan states the tension (§9).
- **Q4:** n/a (the plan already schedules "ratify or re-scope with a recorded decision"; RX confirms every reference sits far below 365 days, so the evidence favors re-scoping).
- **Verdict:** CONFIRMS. (Operator-owned decision; RX supplies the ratify-vs-rescope evidence.)

### RX-L6-25 · `snapCapableHosts` scarcity exemption — re-evaluated on ETC/Mordor merits · Tier C · owner-layer L6
- **Plan claim / disposition:** §6 "Preserved as correct (re-evaluated)" + §7 — the `snapCapableHosts` scarcity exemption is preserved but re-evaluated on its own merits; it exists because the `Permanent` tier + tiny ETC snap-server population interact; a re-scoped ban policy (RX-L6-24) may reduce the need for it.
- **Reference source (byte-cited):**
  - besu `PeerDenylistManager.java:55-64` — the only besu exemption is `maintainedPeers` (operator-pinned peers). besu has **no scarcity/snap-capability exemption** — it's a fukuii-original tied to ETC/Mordor's small snap-server count (no reference-client analogue, consistent with the "absence is evidence" that ETC network conditions differ).
- **Q1 (appropriate?):** Yes — re-evaluating (not blindly keeping) is correct; the exemption is a fukuii-original that only makes sense coupled to the `Permanent` tier. If RX-L6-24 re-scopes the ban, the exemption's necessity shrinks.
- **Q2 (what to implement?):** Keep the exemption only to the extent the re-scoped ban policy still needs it; decide with RX-L6-24. Correct (couple the two decisions).
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: couples to RX-L6-24 (ban re-scope) + the maintained-peer exemption (besu's only exemption). No reference analogue — a genuinely ETC-specific policy knob. Plan states the coupling.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-26 · `PeerScore`/`PeerReputation` weighted composite reputation (pre-disconnect signal) · Tier B · owner-layer L6
- **Plan claim / disposition:** §4 + §6 "Preserved as correct" — the `PeerScore` weighted composite reputation (handshake/response/latency/compliance/recency), a **pre-disconnect** signal orthogonal to the post-disconnect denylist (besu `PeerReputation`, corroborated by nethermind `NodeStatsLight`, reth `ReputationChangeWeights`).
- **Reference source (byte-cited):**
  - besu `PeerReputation.java:35-80` — `implements Comparable<PeerReputation>`; `DEFAULT_INITIAL_SCORE = 100`, `DEFAULT_MAX_SCORE = 150`; `int score`; `recordRequestTimeout(...)` → `score -= LARGE_ADJUSTMENT` / `SMALL_ADJUSTMENT` → returns an `Optional<DisconnectReason>` when the score crosses a floor. **A numeric, adjustable, comparable score = the pre-disconnect signal.**
  - nethermind `NodeStatsLight.cs` + reth `crates/net/network-types/` `ReputationChangeWeights` — the two cross-checks (both maintain a numeric reputation adjusted by event weights). All three corroborate the shape.
- **Q1 (appropriate?):** Yes — a weighted reputation score orthogonal to the denylist is DEFAULT-correct (besu + nethermind + reth all have it); preserving fukuii's `PeerScore` is right.
- **Q2 (what to implement?):** Preserve the composite `PeerScore`; keep it distinct from the ban policy. Correct (the two are orthogonal — one degrades gracefully pre-disconnect, one bans post-disconnect).
- **Q3 (mechanism + blast radius?):** Mechanism correct (numeric score, event-weighted, threshold-triggers a disconnect). Blast radius: feeds dial scheduling / peer selection (L6 internal) and, on threshold, the disconnect path (which may then feed the denylist via RX-L6-23). Plan states the orthogonality.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## Group E — bootnode / network-infrastructure serve (F7/F8)

### RX-L6-27 · Bootnode + ENR/DNS-tree serving/authoring (F7/F8) · Tier B (OPTIONAL infra, R9) · owner-layer L6
- **Plan claim / disposition:** §7 F8 row + `feature-ledger.md` F7/F8 + `optimizations.md` §L6 — fukuii covers the *consume* side only (discv4/v5 dial, DNS resolve, ENR read); build the *serve/author* side: run as a bootnode, **author + publish** an ENR/DNS-tree (EIP-1459), network-identity-as-data. OPTIONAL(infra, R9) — build the seam now, ship role-gated.
- **fukuii `july-fourth`:** (snapshot) consume-side only; no serve/author path (the F8 gap).
- **Reference source (byte-cited):**
  - go-ethereum `p2p/dnsdisc/{tree.go,client.go}` — the DNS-tree **author** path (`tree.go` builds/signs an `enrtree://`); the serve-side reference. `cmd/devp2p/dnscmd.go` (geth's `devp2p dns sign/publish`) is the tooling analogue.
  - core-geth `params/bootnodes_classic.go:28-31` — the live ETC DNS-tree fukuii would author-compatible with (`enrtree://…@all.classic.blockd.info`). besu `NodeRecordManager`/ENR-authoring (§4) — the JVM ENR-author reference.
- **Q1 (appropriate?):** Yes — OPTIONAL(infra, R9) with the seam built now is correct; it's the F8 "erect + serve networks" positioning (memory `planned-work-is-scope-floor`: build the seam, don't skip it). Not building the serve side would leave R9 half-satisfied.
- **Q2 (what to implement?):** The bootnode serve + ENR/DNS-tree author seam, role-gated. Correct — go-ethereum's `dnsdisc/tree.go` proves the author path is a bounded addition over the resolve path already present.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: authored records must carry the correct `eth` ForkId (RX-L6-12) + **networkId=1 not 61** (RX-L6-28) or they attract the wrong peers (a silent failure). This is **new fukuii code** (unlike the consume side in scalanet), so it must be validated against besu ENR-authoring + core-geth's ETC DNS config (§9). Plan states it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-28 · networkId = 1 (NOT 61) for ETC — the F8 serve-side silent-failure trap · Tier A (DEFAULT; interop-critical) · owner-layer L6
- **Plan claim / disposition:** §3 network-identity fact + §9 — ETC's `networkId = 1` (**NOT 61** — 61 is `chainId`; ETC split from ETH before EIP-155 and kept networkId=1). networkId alone can't distinguish ETC from ETH (both networkId=1); **forkId is the distinguishing filter** (order: networkId → genesisHash → forkId). An F8-authored ENR/bootnode emitting `networkId=61` silently fails to attract the right peers.
- **Reference source (byte-cited):**
  - core-geth `params/config_classic.go:37` — `NetworkID: 1` (ETC mainnet); `:149` — `NetworkID: 1` (a second ETC config). **Byte-confirms ETC networkId = 1, not 61.** (`chainId` 61/63 is a distinct field.)
  - go-ethereum `eth/protocols/eth/handshake.go` — the Status handshake compares `NetworkID` then `Genesis` then filters `ForkID`; two networkId=1 chains (ETH + ETC) are separated **only** by genesis hash + ForkID. Confirms the "forkId is the distinguisher" ordering.
- **Q1 (appropriate?):** Yes — DEFAULT/carry-the-fact is correct and interop-critical; a wrong networkId in an authored record is a real silent-failure (the F8 serve side).
- **Q2 (what to implement?):** Emit networkId=1 for ETC in all authored records + the Status handshake; rely on forkId as the distinguisher. Correct — matches core-geth exactly.
- **Q3 (mechanism + blast radius?):** Mechanism correct (networkId→genesis→forkId ordering). Blast radius: **ripples to F8 authoring** (RX-L6-27), the Status handshake (RX-L6-21), and the ENR `eth` entry (RX-L6-07); a `networkId=61` leak is silent (no error, just no right peers). Plan names it as a §9 risk.
- **Q4:** n/a.
- **Verdict:** CONFIRMS. (Interop-critical; forge co-interest since ForkId is the actual distinguisher.)

### RX-L6-29 · `transport`-interface `MsgPipe` test seam (whole stack over an in-memory pipe) · Tier C (OPTIONAL test) · owner-layer L6
- **Plan claim / disposition:** §5 + §7 + §8 — the whole RLPx+wire stack over an in-memory Streams-Tcp `Flow`, zero sockets — the deterministic interop harness (geth `transport`-interface `MsgPipe` shape maps onto a Streams-Tcp in-memory `Flow`).
- **Reference source (byte-cited):**
  - go-ethereum `p2p/message.go` `MsgPipe()` — returns a paired `*MsgPipeRW` so a full protocol stack runs over an in-memory pipe with no TCP; `p2p/transport.go` `transport` interface (the seam swapped for the pipe). The exact test-seam shape.
- **Q1 (appropriate?):** Yes — OPTIONAL(test) is correct; an in-memory pipe harness (deterministic, socket-free) is a genuinely useful test vehicle, cheap because Streams-Tcp already exposes an in-memory `Flow`.
- **Q2 (what to implement?):** The in-memory `Flow` harness. Correct (build at L6, per §7 — it's a named test, not "optional/later").
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: rides the RX-L6-01 Streams-Tcp seam; enables deterministic eth-wire/handshake round-trips without a socket (the §8 DoD harness). No production blast radius.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-30 · Sentry (P2P-as-gRPC-service) seam — NOT built at L6 · Tier C (OPTIONAL product-family) · owner-layer L6
- **Plan claim / disposition:** §2 (Sentry OPTIONAL(product-family/enterprise) — "not needed for a single embedded node") + §7 + §9 — recorded as the Theme-2 seam; keep the consumer API message-passing-first so the later lift is non-invasive; carry the two erigon-Sentry gotchas (`SetStatus` handshake-ordering race → `awaitStatus` before send; remote-mode serialization tax → in-proc shim). **NOT built at L6.**
- **Reference source (byte-cited):**
  - erigon Sentry (`p2p/sentry/`, the gRPC P2P service) — the reference; a standalone process exposing send-by-id / subscribe-inbound / manage-peers over gRPC. The `SetStatus` ordering race + remote-serialization tax are the named gotchas.
- **Q1 (appropriate?):** Yes — OPTIONAL(product-family), NOT built, is correct: a single embedded node doesn't need P2P-as-a-service (YAGNI now); building it now would be premature. Keeping the consumer API message-passing-first is the right cheap-lift-later posture.
- **Q2 (what to implement?):** Nothing now — keep the L7/L8 consumer-facing peer/wire API narrow (send-by-id/subscribe/manage) so the later gRPC lift is non-invasive. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: the *design constraint* (message-passing-first consumer API) ripples to L7/L8 (they must consume the peer stack through a narrow seam, not embed `PeerManagerActor`); the two erigon gotchas are carried into that API's contract. Plan states it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS. (Correctly NOT built; the seam constraint is the deliverable.)

---

## Group F — peer management / admission

### RX-L6-31 · Channel-ownership → single-owner `PeerManagerActor` (geth serialized `Server.run()`, two-phase re-validated admission) · Tier A (structural spine, R5) · owner-layer L6
- **Plan claim / disposition:** §2 (Channel-ownership → single-owner-actor, "DEFAULT — fukuii's Pekko model maps directly") + §5 — `PeerManagerActor` owns the peer set (geth's serialized `Server.run()` loop — lock-free, **two-phase admission re-validated inside the single owner, avoiding TOCTOU**); one owning behavior per connection; request-ID dispatcher → `replyTo`/ask. Do not 1:1-port Mantis's finer-grained Classic actors.
- **Reference source (byte-cited):**
  - go-ethereum `p2p/server.go:689,698,756-778` — the `run()` loop calls `postHandshakeChecks(peers, inboundCount, c)` (`:689`, after the encryption handshake) then `addPeerChecks(peers, inboundCount, c)` (`:698`, at add-peer time); `addPeerChecks:771-778` **calls `postHandshakeChecks` again** → the peer-count/inbound checks run **twice, both inside the single `run()` goroutine** (the serialized owner). `postHandshakeChecks:756-758` — `!c.is(trustedConn) && len(peers) >= srv.MaxPeers`. **The exact two-phase, re-validated-in-the-owner admission the plan maps onto one Pekko Typed actor.**
  - besu `RlpxAgent.java:92,400-515` — `maxPeers` owned by the connection-set object (the single-owner analogue); dial admission + disconnect reasons centralized. The JVM structural mirror for `PeerManagerActor`.
- **Q1 (appropriate?):** Yes — DEFAULT/single-owner is correct; geth's serialized `run()` loop is the "how coarse" reference (one owning actor = what one geth goroutine exclusively owns), and re-validating admission inside the owner is the TOCTOU fix. Mapping directly onto `PeerManagerActor` + `replyTo` is right (R5).
- **Q2 (what to implement?):** One owning `PeerManagerActor` (peer set) + one owner per connection + `replyTo`/ask correlation; **two-phase admission re-checked inside the owner**. Correct — the multi-client read (geth serialized loop + besu `RlpxAgent`) confirms the coarse granularity; do NOT 1:1-port Mantis's finer Classic actors.
- **Q3 (mechanism + blast radius?):** Mechanism correct (two-phase, both inside the serialized owner). Blast radius: the actor-granularity decision ripples to every L6 actor boundary (R5 Typed structure) and the R2 no-global-state rule (RX-L6-33); getting granularity wrong re-introduces the TOCTOU the single-owner avoids. Plan states the mapping.
- **Q4:** n/a.
- **Verdict:** CONFIRMS. (Structural spine — channel-ownership granularity is the R5 actor-boundary decision.)

### RX-L6-32 · DialRatio (⅔ inbound reservation) + per-IP inbound throttle (30s, LAN-exempt) · Tier B (OPTIONAL R2 consortium) · owner-layer L6
- **Plan claim / disposition:** §7 + `optimizations.md` §L6 — geth reserves ⅔ inbound via DialRatio + a 30s per-IP inbound throttle **exempting LAN peers** (co-located fleet friendliness); build the seam at L6, ship OPTIONAL(R2 consortium).
- **Reference source (byte-cited):**
  - go-ethereum `p2p/server.go:50,556-559` — `defaultDialRatio = 3`; `limit = srv.MaxPeers / defaultDialRatio` (or `/ srv.DialRatio`) = the max **dialed (outbound)** conns → **⅓ outbound, ⅔ reserved for inbound**. Confirms "reserves ⅔ inbound."
  - go-ethereum `p2p/server.go:53,852-869` — `inboundThrottleTime = 30 * time.Second`; `checkInboundConn:864` — `if !netutil.AddrIsLAN(remoteIP) && srv.inboundHistory.contains(remoteIP.String()) → "too many attempts"` → **LAN peers are exempt** from the per-IP throttle; `:867` `inboundHistory.add(remoteIP.String(), now.Add(inboundThrottleTime))`. Also `:857-860` `NetRestrict` (a netrestrict allowlist). Byte-confirms the full mechanism.
- **Q1 (appropriate?):** Yes — OPTIONAL(R2 consortium) with the seam built is correct; the LAN-exemption is exactly what a co-located fleet needs (a consortium's own nodes share a LAN and must not throttle each other), and geth ships it by default.
- **Q2 (what to implement?):** DialRatio inbound reservation + per-IP 30s throttle with LAN-exempt bypass, role-gated. Correct — matches geth byte-for-byte.
- **Q3 (mechanism + blast radius?):** Mechanism correct (⅔ inbound; 30s window; `AddrIsLAN` exemption; `NetRestrict` allowlist). Blast radius: R2 consortium (per-instance config); interacts with the admission gate (RX-L6-31) and the LAN-exempt notion also appears in the blacklist policy discussion. Plan states R2.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-33 · `IORuntime` injected from composition, never a per-builder `lazy val` (R2-F4 diamond) · Tier B · owner-layer L6
- **Plan claim / disposition:** §9 — the old `ioRuntime` diamond formed because `PeerDiscoveryManagerBuilder` and `PortForwardingBuilder` (**both L6 components**) each declared the same `lazy val IORuntime` (`NodeBuilder.scala:1172`); L6's builders take the runtime as a constructor/`using` param from L10's composition — a per-builder `lazy val IORuntime` in `network` re-forms the diamond (R2 isolation break). L10 retires the existing diamond; L6 states the constraint so it can't re-form where the components live.
- **fukuii `july-fourth`:** (snapshot) two L6 builders each declare `lazy val IORuntime` → the diamond.
- **Reference source (byte-cited):**
  - besu `ServiceManager` DI (constructor-injected refs, §5) — the anti-pattern reference is besu's own process-global `SignatureAlgorithmFactory` (`optimizations.md` L0/L10: global-static breaks multi-instance). The fix (spawn-time DI of the runtime) is besu's `ServiceManager` shape.
- **Q1 (appropriate?):** Yes — stating the R2 constraint at L6 (where the components live) is correct even though R2's matrix marks L6 `—`; a per-builder `lazy val` runtime is a real multi-instance leak.
- **Q2 (what to implement?):** Constructor/`using`-injected `IORuntime` from L10 composition; no per-builder `lazy val`. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: R2 multi-instance isolation; ripples to L10 (composition owns the runtime, retires the diamond) — the plan cross-references L10 §2. A grep DoD (no `lazy val IORuntime` in `network`) enforces it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

### RX-L6-34 · Every actor a sealed `Command` ADT + explicit `replyTo`; no `Behavior[Any]`, no `sender()` (R5) · Tier B · owner-layer L6
- **Plan claim / disposition:** §5 + §8 DoD (zero `Behavior[Any]`) — `PeerManagerActor`, `PeerDiscoveryManager`, `ServerActor`, and the per-connection owner each carry a closed command set (`Behavior[Command]`, never `Behavior[Any]`, never `sender()`); the old code's ~179 `Behavior[Any]`/`Any`-selector narrowing debt never comes into existence.
- **fukuii `july-fourth`:** (snapshot) ~179 `Behavior[Any]`/`Any`-selector narrowings in the Classic-era network actors.
- **Reference source (byte-cited):**
  - `.agents/protocols/code-style/pekko-typed-api.md` P1–P25 (the governing Typed-API standard) — sealed `Command` ADT + `replyTo`, no `Behavior[Any]`. besu `RlpxAgent` uses typed callbacks (`DisconnectCallback`, `ConnectCallback`) rather than untyped message dispatch — the structural analogue of a closed command set.
- **Q1 (appropriate?):** Yes — R5 born-Typed from line one is correct; L6 is the first actor-heavy layer, so establishing the closed-command-set discipline here prevents the `july-fourth` debt from re-forming.
- **Q2 (what to implement?):** Sealed `Command` ADTs + `replyTo` for every L6 actor. Correct.
- **Q3 (mechanism + blast radius?):** Mechanism correct. Blast radius: sets the Typed-idiom baseline for L7+ (the actor-heavy layers below inherit the pattern); a grep DoD (`grep -r 'Behavior[Any]'` empty, `grep -r 'pekko.io.Tcp'` empty) enforces it (§8). Plan states it.
- **Q4:** n/a.
- **Verdict:** CONFIRMS.

---

## RX-L6 rollup (this layer)

**Total items:** 34 (RX-L6-01 … RX-L6-34), grouped RLPx/framing (5) · discovery/ENR/DNS (6) · wire-eth68-71/ForkId (10) · blacklist-policy (5) · bootnode/F7-F8-serve (4) · peer-management (4).

**CORRECTS:** none (no disposition/mechanism is wrong — the plan's own in-flight corrections, e.g. eth/70 7706→7975, are source-confirmed).

**SHARPENS (5):**
- **RX-L6-04** — §3: the "core-geth 16MiB decompressed bound" is not byte-located; cite the located bounds (`baseProtocolMaxMsgSize` + 24-bit `maxUint24` frame ceiling) and state the decompressed cap as an L6-owned Snappy-stage R11 guard.
- **RX-L6-08** — §3: replace the absent `SetDNSDiscoveryDefaults2` with `KnownDNSNetwork` / `ClassicDNSNetwork1` (`enrtree://…@all.classic.blockd.info`) / `dnsPrefixETC` (`core-geth/params/bootnodes_classic.go:28-31`, `bootnodes.go:77`).
- **RX-L6-12** — §1/§8: "3-rule" → **"4-rule (3 accept + reject-otherwise)"** (geth `forkid.go:155-222`); §7/§8: enumerate the byte-guard DoD vectors (block-0 skip, time-fork-≤-genesis skip, consecutive-equal dedup, `MaxUint64` sentry + block-only sentry, `timestampThreshold=1438269973` block-vs-time split, ETC blocks < threshold).
- **RX-L6-18** — §6.3/§4/§5/§9: reframe the `best()` improvement as "generic **N-subprotocol** computed-offset RangeMap keeping eth+snap simultaneously" (besu still selects best-per-subprotocol); state that per-version `messageSpace` is the **wire code-span** (69/70=18, 71=20, incl. the `0x0b–0x0e` gap), offsets start at `WIRE_PROTOCOL_MESSAGE_SPACE=16`.
- **RX-L6-23** — §7/§4: fold the **full** besu direction rule (`INCOMPATIBLE_P2P_PROTOCOL_VERSION` denies **both** directions, `BREACH_OF_PROTOCOL` self-only; `PeerDenylistManager.java:31-36,68-71`); mark "LRU-bounded 500" as a fukuii-chosen `maxSize` over besu's `DROP_LEAST_RECENTLY_ACCESSED` mechanism.

**Fold-in note (RX-L6-15):** the wrong "ETH70=EIP-7706" attribution is **live in the herald agent charter/description** — §10's charter repoint is load-bearing, not cosmetic (RX corroborates the propagation).

**forge/beacon co-sign at build:** RX-L6-12 (ForkId golden vectors per network — the gate that must not be waved through), RX-L6-16 (eth/71 BAL wire byte-exactness), RX-L6-28 (networkId=1 + forkId-as-distinguisher). RX-L6-05 is the named L0→L6 R11 constant-time link.

**Blast-radius spine confirmed:** ForkId → L5 fork-schedule (wrong ForkId partitions the network); the `Blacklist` type (L6-owned) → L7 (sync depends down); the advertised eth-version set → L5 (`NetworkFamily` contributes) + L7 (sync negotiates); the ECIES auth-tag const-time → L0 `constantTimeEquals`.
