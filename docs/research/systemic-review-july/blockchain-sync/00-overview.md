# blockchain-sync — Overview (placeholder)

Populated when `SR-01` (`.claude/sprints/QUEUE.md`, "Systemic Review" section) is dispatched.
Tier A — full 6-doc split per sub-module (`orchestration/`, `fast-sync/`, `regular-sync/`,
`snap-sync/`, `codec/`), per `.agents/protocols/process/systemic-review-protocol.md`'s
template. Gated `BLOCKED-ON-BATCH-4`.
