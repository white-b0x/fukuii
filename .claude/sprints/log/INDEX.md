# Sprint Log — Master Index

Permanent, sprint-agnostic record of what changed in this codebase, why, and which sprint
batch did it. Every sprint from now on — modernization, performance, security, features,
audits — logs here, not into a sprint-specific directory name.

A batch is not eligible for `sprint-archive.sh` until it has an entry here (see
`.claude/agent-protocols/sprint-lifecycle.md`).

**Detail files in this directory:**
- [`legacy-modernization-log/`](legacy-modernization-log/INDEX.md) — the June 2026
  Pekko/Scala3 modernization effort's full historical record (31 files), migrated verbatim
  from its original home at `.claude/agent-protocols/modernization-log/` (Phase B of the
  progress-tracking/modernization-log retirement will remove the original once this copy is
  confirmed). Left as archived detail, not actively added to going forward — new entries land
  in this `INDEX.md` and its Cross-Cutting Entries table instead.
- [`july-2026-h-series-detail.md`](july-2026-h-series-detail.md) — condensed context + full
  verbatim forge/beacon verdict text for `IP-12`, `IP-13` (+`IP-13-FG`), `IP-09b` (+`IP-09b-FG`),
  and `IP-SA` — detail that only existed in `JULY_SPRINT_PROMPTS.md`/`july-follow-ups.md`
  before this migration.

---

## Area Status

| Area | Last touched | Status | Notes |
|------|--------------|--------|-------|
| core | 2026-07-01 | 19 domain opaque types, H-series (Nonce/Wei/GasAmount/GasPrice/EIP-1559/ChainId/BlockNumber/Timestamp) fully propagated across src/main/ | See Cross-Cutting Entries below |
| consensus | 2026-07-03 | Reward calc (ECIP-1017), Ethash, Engine API, EVM pipeline all opaque-typed for the H-series; IP-CL-G forge-gated PoW/Ethash family sweep (BlockRewardCalculator, MESSConfig, difficulty calculators, ommer validators) closed; IP-CL-H beacon-gated Engine API/PoS family sweep (ExecutionPayload, PayloadAttributes) closed | See Cross-Cutting Entries below |
| sync | 2026-07-03 | SNAP layer (SNAPSyncController + coordinators) BlockNumber-propagated in one commit (PR #1384 regression-pattern avoided); IP-CL-J closed the remaining sync-orchestration/checkpoint/TrieRoot family (16 files) | See Cross-Cutting Entries below |
| api | 2026-07-02 | jsonrpc/testmode/faucet/CLI DTO layer opaque-type sweep — **CLOSED**, all 4 sub-batches landed | See Cross-Cutting Entries below |
| network | 2026-07-03 | P2P peer/handshake sweep (IP-CL-K) + ETHPackets wire-ADT sweep (IP-CL-K2, forge+beacon gated) both closed | See Cross-Cutting Entries below |

Add a row (or a new `<area>.md` for detail, linked from this table) the first time a batch
touches that area. Keep the same area names `modernization-log/` already uses where they
overlap (`api`, `consensus`, `core`, `ext`, `network`, `node`, `storage`, `sync`) so the two
logs stay easy to cross-reference.

---

## Cross-Cutting Entries (3+ areas, or notable for any other reason)

| Batch | What | Areas | Commits |
|-------|------|-------|---------|
| July sprint, Batch 1 (H-series) | **Nonce** opaque type — created + propagated to 52 sites | core, api, consensus | `f30bcde3f` |
| July sprint, Batch 1 (H-series) | **Wei** opaque type — created + propagated to 47 sites, `BlockRewardCalculator` return boundaries wrapped | core, consensus, ledger | `e18ddd188`, `418106877` |
| July sprint, Batch 1 (H-series) | **GasAmount** opaque type — propagated to 31+ call sites (IP-09), then the full EVM internal pipeline (`ProgramContext.startGas` root register → `ProgramResult.gasUsed` → tracer hook signatures) (IP-09b) | core, consensus, vm, ledger | `75b290bbf`, `8bbdc84b0` |
| July sprint, Batch 1 (H-series) | **GasPrice** — 5 jsonrpc leakage sites fixed, absorbed into the EIP-1559 cascade commit rather than a standalone commit | api | `8418c42e1` |
| July sprint, Batch 1 (H-series) | **BaseFeePerGas / MaxFeePerGas / PriorityFeePerGas** (EIP-1559) — 3 new opaque types created, propagated through `BlockHeader`/`Transaction` (domain), then consensus/network/ledger, then jsonrpc, then a 65-error/16-file cascade fix, then Pickler instances | core, consensus, network, ledger, api, storage | `0f5ccd251`, `0e478c301`, `5a6c09ecd`, `8418c42e1`, `837a1ac36` |
| July sprint, Batch 1 (H-series) | **Timestamp** — audited, 0 genuine leakage sites found (existing raw uses are wall-clock `Ms:` fields or Engine API wire boundaries, correctly left as-is) | core | _(no commit — audit-only closure)_ |
| July sprint, Batch 1 (H-series) | **ChainId** — propagated through EIP-155 signing path + cascade (`Transaction`, `SignedTransaction`, `BlockchainConfig`) | core, api, network | `8c382316d` |
| July sprint, Batch 1 (H-series) | **BlockNumber** — propagated through consensus/ledger/vm, then the SNAP sync layer as a single commit (all-SNAP-files-together discipline, per the PR #1384 regression precedent) | core, consensus, ledger, vm, sync | `a007ef951`, `e14231b07` |
| July sprint, Batch 1 (IP-CL-A) | **JSON-RPC/testmode/faucet/CLI DTO sweep** — the straggler audit found this layer never got fully swept during H5–H9: DTOs had some fields already typed (e.g. `TransactionResponse.gasPrice: GasPrice`) but siblings (`nonce`, `value`, `gas`) stayed raw `BigInt` (Rule 2 half-typed pattern). 4 batches, 28 files, ~70 sites, no forge/beacon gate (pure response/request DTOs + dev tooling, no consensus computation). Also corrected round 1's wrong "GasPrice — CLEAN (main)" verdict (`TransactionRequest.scala`, `EthSimulateService.scala`) and fixed a missed IP-12 site (`EthInfoService.ChainIdResponse`) found mid-sweep. `sbt compile-all`: 0 main-source errors throughout all 4 batches; 453 pre-existing test-source errors unchanged, explicitly deferred to IP-14 (which runs after every IP-CL-* prompt). Confirmed correctly OUT of scope during the sweep (different type space, not leakage): `EthUserService.GetTransactionCountResponse` (Account.nonce is UInt256, not the tx-pool Nonce type), `EthProofService.StorageProof.value` ×4 (storage proof values have no domain opaque type), `JsonMethodsImplicits.scala:69 getDuration` (generic JSON utility, unrelated to domain types). | api, core | `34a933ccc` (batch 1: TransactionResponse/Request, TransactionReceiptResponse, BlockResponse), `8f474d800` (batch 2: Eth* services, GraphQL, testmode wrapper + ChainIdResponse fix), `1a984d40f` (batch 3: TestService, testmode consensus/mining chain), `cf1cbed07` (batch 4: faucet, CLI, transaction history, DebugTrace) |
| July sprint, Batch 1 (IP-CL-J) | **Sync orchestration + checkpoint + TrieRoot family sweep** — supersedes the old `IP-13-PL` prompt, whose 5-file scope this expands to the full 16-file set round 2's straggler audit found gate=NONE (the 4 actors that originally gated it — `BytecodeRecoveryActor`, `StorageRecoveryActor`, `BlockImporter`, `SyncStateScheduler` — are now confirmed fully Pekko Typed, clearing the LOOM cascade risk). 2 batches: Batch A (BlockNumber/ChainId — `SyncProtocol`, `CheckpointExporter`/`Importer`/`Cli`/`Archive`, `ImportMessages` + cascade into `RegularSync`/`BlockFetcher`/`BlockImporter`/`SyncController`), Batch B (TrieRoot/CodeHash — all 6 SNAP recovery/healing actors + `StateValidator`/`StateNodeFetcher`, landed as one commit per the PR #1384 all-SNAP-files-together discipline). 42 files total, ~45 sites, no forge/beacon gate. `CheckpointArchive.chainId`: verified the archive wire format uses fixed-width `wLong`/`rLong`, never RLP — the `Long → ChainId` retype is compile-time only, wire bytes unchanged, no serializer change needed. `sbt compile-all`: 0 main-source errors throughout both batches. **Discovered and logged, not caused by this batch:** ~100 pre-existing test-source compile errors (GasAmount/BaseFeePerGas mismatches) that predate IP-CL-J, from main-source signature changes that landed outside this queue's tracked sequence without their test-source updates — confirmed byte-identical (same files/lines) before and after both IP-CL-J batches via a git-stash baseline check; this blocks `testOnly`/`testEssential` queue-wide until IP-CL-G/IP-CL-H absorb the fix (see QUEUE.md Findings Resolution Log). `herald` spot-check recommended on `SNAPSyncController.scala`/`StateNodeFetcher.scala`/`StateValidator.scala`, not yet run. | sync, core | `4f42f4e31` (Batch A: SyncProtocol/Checkpoint family + ChainId), `5930054cf` (Batch B: SNAP recovery/healing actors + TrieRoot/CodeHash family) |
| July sprint, Batch 1 (IP-CL-K + IP-CL-K2) | **Network/P2P peer + handshake sweep, then ETHPackets wire-ADT sweep.** IP-CL-K (no gate, 3 files): `NetworkPeerManagerActor.UpdateClHeadCmd`/`UpdateClHead` (Command payload + internal msg) and `EthNodeStatus68ExchangeState.handleStatus68Fields`'s `totalDifficulty` param retyped BigInt→BlockNumber/TotalDifficulty; one cascade fix in `SNAPSyncController.scala`. `forkid/ForkId.scala:62` `maxBlockSentinel` assessed and left as raw `BigInt` — genuinely an internal EIP-2124 CRC32 comparison bound, never crosses a BlockNumber-typed boundary; documented as BORDERLINE-not-leakage rather than silently dropped (closes the Findings Resolution Log row). IP-CL-K2 (forge+beacon gated, `network/p2p/messages/` joint bucket): `NewBlockHashes.BlockHash.number` and `NewBlock.totalDifficulty` retyped in the RLP wire ADT, with cascades into `BlockBroadcast.scala`, `BlockFetcher.scala`, `NetworkPeerManagerActor.scala` (main) + 10 test files. **Gate caught a real bug on first pass:** the initial hand-rolled encoder (`RLPValue(ByteUtils.bigIntToUnsignedByteArray(...))`) for `BlockHash.number` diverged from the pre-refactor path at `n=0` — `0x80` (empty-string form) instead of the canonical `0x00`, because it bypassed `bigIntCodec.encode`'s zero special-case. Forge blocked, mithril fixed by routing through `BlockNumber.rlpCodec.encode(...)` instead (which delegates to the same `bigIntCodec.encode`, restoring byte-identical output for all n including 0), added a regression test pinning the zero case in `ETHPacketsRoundTripSpec.scala`. Forge and beacon both independently re-traced the codec chain and approved on re-review; beacon additionally confirmed the deliberately-different `TotalDifficulty` encode path (kept as raw `bigIntToUnsignedByteArray`, not routed through its own codec) is correct as-is — routing it through `rlpCodec.encode` would itself have changed TD's zero-byte encoding. `sbt compile-all`: 0 main-source errors in both items; `testOnly`/`testEssential` still blocked queue-wide by the pre-existing GasAmount/BaseFeePerGas baseline (same blocker as IP-CL-J, unaffected by this sweep). See the pattern captured in `sprints/patterns/PATTERNS.md` — "Opaque-type RLP encode: delegate to the type's own codec, never hand-roll the byte conversion." | network, core | `0b14b46c7` (IP-CL-K), `a9ace9f42` (IP-CL-K2, post-gate) |
| July sprint, Batch 1 (IP-CL-G) | **PoW/Ethash consensus family sweep (forge-only gate) — 9 known-site files + 30 cascade files.** `BlockRewardCalculator.scala` (both BlockNumber era-boundary ctor params and Wei reward-amount fields, same file/reviewer): `byzantiumBlockNumber`/`constantinopleBlockNumber`→`BlockNumber`; `firstEraBlockReward`/`newRewardAfterByzantium`/`newRewardAfterConstantinople` + `newBlockReward`/`calculateMiningRewardPerOmmer` return type→`Wei`; `eraDuration` + ratio numer/denom fields deliberately left raw `BigInt` (dimensionless constants). `Config.scala`'s `MonetaryPolicyConfig` Wei fields (fork-dispatch `blockNumber` fields left for IP-CL-I). `MESSConfig.scala` (ECIP-1100 window fields→`BlockNumber`). `WorkNotifier.scala` (mining work package→`BlockNumber`). `EthashDifficultyCalculator.scala`/`TargetTimeDifficultyCalculator.scala` (half-typed `parentDiff` local→`Difficulty`, redundant wrap/unwrap round-trip collapsed to one final wrap). `StdOmmersValidator.scala`(×5)/`OmmersValidator.scala`(×2)/`OmmersPool.scala` (`parentHash`/`parentBlockHash`→`BlockHash`). Forge independently traced the live diff (not the prose): ECIP-1017 emission arithmetic, ommer depth-clamp (`blockNumber.value.min(OmmerGenerationLimit).toInt`), MESS window comparisons, and the difficulty round-trip collapse all confirmed byte-identical; also verified mithril's two claimed edge cases (the deliberate `.value` unwrap in `calculateMiningRewardForOmmers`/`calculateMiningRewardPerOmmer` to avoid a `Wei.*(BigInt)` vs `Wei.*(Long)` overload ambiguity, and the difficulty calculators' round-trip collapse) directly against the code rather than taking the report at face value. `sbt compile-all`: 0 main-source errors across 3 runs (`set every maxErrors := 100000` used to see the true error count, not sbt's 100-message display cap); confirmed the 411 remaining test-source errors are a pre-existing baseline substantially larger than IP-CL-J's original ~100-error estimate (also touching unrelated `BlockHash`/`BlockNumber`/`Wei` DTOs in `jsonrpc`/`network`/`vm` test sources, not just `GasAmount`/`BaseFeePerGas`) and are NOT owned by this batch's 9-file scope — left unabsorbed for a future batch. `sbt scalafmtAll` clean. | consensus, core, ledger | `c9b34213a` |
| July sprint, Batch 1 (IP-CL-H) | **Engine API / PoS family sweep (beacon-only gate) — 2 known-site files + 4 cascade files.** `EngineApiDomain.scala`: `ExecutionPayload.stateRoot`→`TrieRoot`, `.logsBloom`→`BloomFilter`, `.blockNumber`→`BlockNumber`, `.timestamp`→`Timestamp`, `.baseFeePerGas`→`BaseFeePerGas`; `PayloadAttributes.timestamp`→`Timestamp`. `EngineApiService.scala`: `getLatestBlockNumber` return type→`BlockNumber`; redundant wraps removed at `payloadToBlock` field-construction sites; `attrs.timestamp` comparison/hash-input/fork-dispatch sites now pass the typed field directly. PARITY-01's inline EIP-1559 base-fee calc explicitly left untouched (confirmed via `git diff -U0` hunk gap by both mithril and, independently, beacon). `EngineApiController.scala` cascade: JSON-RPC wire-boundary decoders (`decodeExecutionPayload`/`decodePayloadAttributes`) now wrap at the S11 boundary — JSON wire format itself unchanged, the encoder builds its `JObject` from `block.header` fields and never serializes an `ExecutionPayload` instance. Beacon independently traced the live diff (not the prose): (1) JSON round-trip byte-identical, (2) `forTimestamp` fork dispatch unaffected, (3) PARITY-01 untouched, (4) `stateRoot`/`logsBloom` flow into `BlockHeader` unchanged. `sbt compile-all`: 0 errors in any `consensus/engine/*` file (2 full runs); confirmed the 399 remaining test-source errors are the same pre-existing GasAmount/BaseFeePerGas/BlockHash/BlockNumber/Wei baseline documented in IP-CL-G's row, not owned by this batch's 2-file scope (12 pre-existing engine-package rows absorbed as part of this sweep's own cascade, dropping the baseline count 411→399). `sbt scalafmtAll` clean. | consensus, core | `477cb7f9b` |

**Note on PR #1384:** the SNAP-layer BlockNumber commit (`e14231b07`) and every subsequent
SNAP-touching prompt in this sprint (`IP-CL-J` Batch B, `IP-CL-DE`) explicitly carries forward
the "all SNAP files land in one commit" discipline established here, because a prior partial
SNAP sweep caused a stale-base regression (PR #1384). Any future SNAP-layer opaque-type or
type-safety work should follow the same rule.
