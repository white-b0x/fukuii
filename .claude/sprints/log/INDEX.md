# Sprint Log — Master Index

Permanent, sprint-agnostic record of what changed in this codebase, why, and which sprint
batch did it. Every sprint from now on — modernization, performance, security, features,
audits — logs here, not into a sprint-specific directory name.

A batch is not eligible for `sprint-archive.sh` until it has an entry here (see
`.claude/agent-protocols/sprint-lifecycle.md`).

**Detail files in this directory:**
- [`legacy-modernization-log/`](legacy-modernization-log/INDEX.md) — the June 2026
  Pekko/Scala3 modernization effort's full historical record (31 files), migrated verbatim
  from its original home at `.claude/agent-protocols/modernization-log/` (Phase B of the
  progress-tracking/modernization-log retirement will remove the original once this copy is
  confirmed). Left as archived detail, not actively added to going forward — new entries land
  in this `INDEX.md` and its Cross-Cutting Entries table instead.
- [`july-2026-h-series-detail.md`](july-2026-h-series-detail.md) — condensed context + full
  verbatim forge/beacon verdict text for `IP-12`, `IP-13` (+`IP-13-FG`), `IP-09b` (+`IP-09b-FG`),
  and `IP-SA` — detail that only existed in `JULY_SPRINT_PROMPTS.md`/`july-follow-ups.md`
  before this migration.

---

## Area Status

| Area | Last touched | Status | Notes |
|------|--------------|--------|-------|
| tooling/ci | 2026-07-06 | REPO persistent section, Tiers 1-10 (REPO-01 through REPO-08b): repo hygiene, docs, CI review bot, dev-workflow skills, ethtest-triage, RLP-roundtrip pilot, EF-test sharding, Hive integration fixes, comment-policy doc+hook. First full execution pass of the `scout`/`batch-research-protocol.md` tooling built for this sprint. See Cross-Cutting Entries below | See Cross-Cutting Entries below |
| core | 2026-07-05 | 19 domain opaque types, H-series (Nonce/Wei/GasAmount/GasPrice/EIP-1559/ChainId/BlockNumber/Timestamp) fully propagated across src/main/; Batch 1.5 fixed a self-referential `given Ordering[X]` deadlock in `ChainId`/`Timestamp` (10 sibling types already had the pinned fix) | See Cross-Cutting Entries below |
| consensus | 2026-07-03 | Reward calc (ECIP-1017), Ethash, Engine API, EVM pipeline all opaque-typed for the H-series; IP-CL-G forge-gated PoW/Ethash family sweep (BlockRewardCalculator, MESSConfig, difficulty calculators, ommer validators) closed; IP-CL-H beacon-gated Engine API/PoS family sweep (ExecutionPayload, PayloadAttributes) closed; IP-CL-F forge+beacon-gated EVM/VM execution + tracer family sweep (Storage[S] SSTORE/SLOAD root, tracer family, TxResult/BlockResult, getBlockHashByNumber callback) closed | See Cross-Cutting Entries below |
| vm/ledger | 2026-07-03 | EVM execution core (`vm/Storage.scala`, `vm/OpCode.scala` SLOAD/SSTORE) and tracer family (`ExecutionTracer`/`CallTracer`/`PrestateTracer`/`VmTracer`/`StructLogTracer`) opaque-typed (IP-CL-F); `Program.scala:codeHash` assessed, zero call sites, left unconverted | See Cross-Cutting Entries below |
| sync | 2026-07-03 | SNAP layer (SNAPSyncController + coordinators) BlockNumber-propagated in one commit (PR #1384 regression-pattern avoided); IP-CL-J closed the remaining sync-orchestration/checkpoint/TrieRoot family (16 files) | See Cross-Cutting Entries below |
| api | 2026-07-05 | jsonrpc/testmode/faucet/CLI DTO layer opaque-type sweep — **CLOSED**, all 4 sub-batches landed; Batch 1.5 fixed a DI anti-pattern (3 files) and a positional-tuple fragility (`TransactionResponse`) | See Cross-Cutting Entries below |
| network | 2026-07-03 | P2P peer/handshake sweep (IP-CL-K) + ETHPackets wire-ADT sweep (IP-CL-K2, forge+beacon gated) both closed | See Cross-Cutting Entries below |

Add a row (or a new `<area>.md` for detail, linked from this table) the first time a batch
touches that area. Keep the same area names `modernization-log/` already uses where they
overlap (`api`, `consensus`, `core`, `ext`, `network`, `node`, `storage`, `sync`) so the two
logs stay easy to cross-reference.

---

## Cross-Cutting Entries (3+ areas, or notable for any other reason)

| Batch | What | Areas | Commits |
|-------|------|-------|---------|
| July sprint, REPO persistent section (Tiers 1-10, first full execution pass) | **Repo hygiene, docs, CI tooling, dev-workflow skills, EF-test/Hive CI fixes, comment policy — 11 items (REPO-01 through REPO-08b), all `scout`-drafted and executed sequentially via `warden`/`mithril`/`eye`.** REPO-01: SECURITY.md, CODEOWNERS, `scalacenter/sbt-dependency-submission` + `dependency-review.yml` + `nMoncho/sbt-dependency-check` (NVD CVE scan, burn-in report-only), Semgrep + Trivy, corrected false "SLSA Level 3"/"CodeQL on every push" claims across 8 docs (neither is true — SLSA generation was removed 2026-04-27, CodeQL has never run against this Scala codebase). REPO-02: `docs/architecture/FORK_DIVERGENCE.md` + incident postmortem template. REPO-03: `.github/workflows/claude.yml` mention-triggered review bot, `author_association` gate ported from Nethermind (not Erigon's unguarded shape), live pilot correctly deferred pending `CLAUDE_CODE_OAUTH_TOKEN` provisioning. REPO-04: subsystem `AGENTS.md` breadcrumbs (`blockchain/sync/`, `consensus/`, `db/`) + corrected `vault.md`'s stale illustrative package snippet. REPO-05a-d: 7 dev-workflow skills (`fukuii-implement-eip`/`-network-ports`/`-ephemeral`/`-build`/`-test-unit`/`-test-all`/`-test-hive` + `hive-run.sh`, with a `trap cleanup EXIT INT TERM` Docker/workdir teardown), PR-title-format validator (redesigned — dropped Erigon's spam-auto-close step, which would have false-positived on fukuii's own `type(scope): description` convention), Akka-BSL CI guard, `hardfork-implementation-checklist.md`, `fukuii-benchmark-diff` skill, `fukuii-ethtest-triage` skill + `StructLogTracer` re-execution helper (`EthereumTestHelper.reExecuteWithTrace`, zero `src/main` touch, `eye`-validated), and an RLP round-trip property test that found a real decoder bug rather than landing green (see Findings below — left uncommitted per protocol). REPO-06: fixed a live `build.sbt` bug where 8 of 9 EF-test spec classes silently executed zero tests every night (and in `sbt pp`/`testComprehensive`/`testCoverage`) due to a `testOptions` scope-delegation leak; added an Erigon-style manifest-driven shard split with a Besu-style file-count safety check. REPO-07: deleted a false-green dead `hive-pyspec.yml`, consolidated `hive-osaka.yml`/`hive-prague.yml` into `_hive-sim.yml` thin callers, added `eth1_snap`/`HIVE_NODETYPE`/`HIVE_LOGLEVEL` handling, rewrote `enode.sh`'s placeholder anti-pattern (ported 2 files from an orphaned unmerged branch, `upstream/fix/hive-sync-enode`, left otherwise untouched). REPO-07-ORPHAN-BRANCH: characterized that branch (297 files, not the ~30 originally estimated) as almost entirely stale/superseded — recommended discard, extracted one genuine finding (see NETWORK-01 below). REPO-08a/b: originated fukuii's first project-level comment-content policy (`comments.md`, diverging from Erigon by ratifying 113 existing PR/issue-citation sites as sanctioned house style) + an advisory `PostToolUse` hook enforcing it going forward, both independently smoke-tested. **Findings surfaced but deliberately not fixed inline (each logged with its own gate in `QUEUE.md`'s Chase & Deferred Items, not silently absorbed):** `PARITY-02` (ETC/ETH opcode-list naming is inverted from what the names suggest — `EtcOlympiaOpCodes` is ETC's real list, the unprefixed `OlympiaOpCodes` is what ETH's Cancun/Osaka path actually uses; `AGENTS.md`'s own "PoW vs PoS" section corrected in place), `RLP-DECODE-01` (`RLP.rawDecode`/`decode[T]` never verify full-buffer consumption, reachable from devp2p discovery/RLPx/MPT-validation call sites, gated on herald+forge+beacon), `REPO-06-ITSUITE` (fixing REPO-06's bug also unmasked 76 previously-never-executed failures in 6 E2E/sync spec classes, gated on flow/herald), `NETWORK-01` (`PeerActor` no longer sends `PeerManagerActor.PeerClosedConnectionCmd` on disconnect — a Pekko-Typed-capstone regression leaving reason-based peer blacklisting structurally dead in production, gated on herald), `MOD-05` (retroactive comment-policy audit, needs its own `scout` pass). Also fixed along the way: `batch-research-protocol.md` gained a `git branch -a` check in Rule (d) after REPO-07's research found an in-flight fix that `gh pr list` alone had missed; `background-script-execution.md` gained a resource-cleanup-must-be-verified section; `git-conventions.md` gained an explicit any-remote branch push/delete confirmation rule after an implementing agent pushed/deleted a scratch branch on the canonical remote without flagging it first. | tooling/ci, network, consensus (docs only) | `bb05fa7ba`, `d6a844184`, `a68dbec1f` (protocol/tooling prep) → `1abad0975` (REPO-01) → `a2ae7e5ac` (REPO-02) → `18a46816c` (REPO-03) → `9c1336431` (REPO-04) → `705e6ace3`, `2ea2a732b` (process fixes) → `5df8592a6` (REPO-05a) → `272b30a13`, `7c59dbd84` (REPO-05b + correction) → `74b0bb8c9`, `b44f00251` (REPO-05c + correction) → REPO-05d uncommitted, see `RLP-DECODE-01` → `6968a78d4` (REPO-06) → `545f480c4`, `94678d11d` (git-conventions fix) → `861dec3f0`, `afe352468`, `4c9c77356`, `037bb80f7` (REPO-07) → `414fbf219` (REPO-08a) → `23d9a3b72` (REPO-08b) |
| July sprint, Batch 1 (H-series) | **Nonce** opaque type — created + propagated to 52 sites | core, api, consensus | `f30bcde3f` |
| July sprint, Batch 1 (H-series) | **Wei** opaque type — created + propagated to 47 sites, `BlockRewardCalculator` return boundaries wrapped | core, consensus, ledger | `e18ddd188`, `418106877` |
| July sprint, Batch 1 (H-series) | **GasAmount** opaque type — propagated to 31+ call sites (IP-09), then the full EVM internal pipeline (`ProgramContext.startGas` root register → `ProgramResult.gasUsed` → tracer hook signatures) (IP-09b) | core, consensus, vm, ledger | `75b290bbf`, `8bbdc84b0` |
| July sprint, Batch 1 (H-series) | **GasPrice** — 5 jsonrpc leakage sites fixed, absorbed into the EIP-1559 cascade commit rather than a standalone commit | api | `8418c42e1` |
| July sprint, Batch 1 (H-series) | **BaseFeePerGas / MaxFeePerGas / PriorityFeePerGas** (EIP-1559) — 3 new opaque types created, propagated through `BlockHeader`/`Transaction` (domain), then consensus/network/ledger, then jsonrpc, then a 65-error/16-file cascade fix, then Pickler instances | core, consensus, network, ledger, api, storage | `0f5ccd251`, `0e478c301`, `5a6c09ecd`, `8418c42e1`, `837a1ac36` |
| July sprint, Batch 1 (H-series) | **Timestamp** — audited, 0 genuine leakage sites found (existing raw uses are wall-clock `Ms:` fields or Engine API wire boundaries, correctly left as-is) | core | _(no commit — audit-only closure)_ |
| July sprint, Batch 1 (H-series) | **ChainId** — propagated through EIP-155 signing path + cascade (`Transaction`, `SignedTransaction`, `BlockchainConfig`) | core, api, network | `8c382316d` |
| July sprint, Batch 1 (H-series) | **BlockNumber** — propagated through consensus/ledger/vm, then the SNAP sync layer as a single commit (all-SNAP-files-together discipline, per the PR #1384 regression precedent) | core, consensus, ledger, vm, sync | `a007ef951`, `e14231b07` |
| July sprint, Batch 1 (IP-CL-A) | **JSON-RPC/testmode/faucet/CLI DTO sweep** — the straggler audit found this layer never got fully swept during H5–H9: DTOs had some fields already typed (e.g. `TransactionResponse.gasPrice: GasPrice`) but siblings (`nonce`, `value`, `gas`) stayed raw `BigInt` (Rule 2 half-typed pattern). 4 batches, 28 files, ~70 sites, no forge/beacon gate (pure response/request DTOs + dev tooling, no consensus computation). Also corrected round 1's wrong "GasPrice — CLEAN (main)" verdict (`TransactionRequest.scala`, `EthSimulateService.scala`) and fixed a missed IP-12 site (`EthInfoService.ChainIdResponse`) found mid-sweep. `sbt compile-all`: 0 main-source errors throughout all 4 batches; 453 pre-existing test-source errors unchanged, explicitly deferred to IP-14 (which runs after every IP-CL-* prompt). Confirmed correctly OUT of scope during the sweep (different type space, not leakage): `EthUserService.GetTransactionCountResponse` (Account.nonce is UInt256, not the tx-pool Nonce type), `EthProofService.StorageProof.value` ×4 (storage proof values have no domain opaque type), `JsonMethodsImplicits.scala:69 getDuration` (generic JSON utility, unrelated to domain types). | api, core | `34a933ccc` (batch 1: TransactionResponse/Request, TransactionReceiptResponse, BlockResponse), `8f474d800` (batch 2: Eth* services, GraphQL, testmode wrapper + ChainIdResponse fix), `1a984d40f` (batch 3: TestService, testmode consensus/mining chain), `cf1cbed07` (batch 4: faucet, CLI, transaction history, DebugTrace) |
| July sprint, Batch 1 (IP-CL-J) | **Sync orchestration + checkpoint + TrieRoot family sweep** — supersedes the old `IP-13-PL` prompt, whose 5-file scope this expands to the full 16-file set round 2's straggler audit found gate=NONE (the 4 actors that originally gated it — `BytecodeRecoveryActor`, `StorageRecoveryActor`, `BlockImporter`, `SyncStateScheduler` — are now confirmed fully Pekko Typed, clearing the LOOM cascade risk). 2 batches: Batch A (BlockNumber/ChainId — `SyncProtocol`, `CheckpointExporter`/`Importer`/`Cli`/`Archive`, `ImportMessages` + cascade into `RegularSync`/`BlockFetcher`/`BlockImporter`/`SyncController`), Batch B (TrieRoot/CodeHash — all 6 SNAP recovery/healing actors + `StateValidator`/`StateNodeFetcher`, landed as one commit per the PR #1384 all-SNAP-files-together discipline). 42 files total, ~45 sites, no forge/beacon gate. `CheckpointArchive.chainId`: verified the archive wire format uses fixed-width `wLong`/`rLong`, never RLP — the `Long → ChainId` retype is compile-time only, wire bytes unchanged, no serializer change needed. `sbt compile-all`: 0 main-source errors throughout both batches. **Discovered and logged, not caused by this batch:** ~100 pre-existing test-source compile errors (GasAmount/BaseFeePerGas mismatches) that predate IP-CL-J, from main-source signature changes that landed outside this queue's tracked sequence without their test-source updates — confirmed byte-identical (same files/lines) before and after both IP-CL-J batches via a git-stash baseline check; this blocks `testOnly`/`testEssential` queue-wide until IP-CL-G/IP-CL-H absorb the fix (see QUEUE.md Findings Resolution Log). `herald` spot-check recommended on `SNAPSyncController.scala`/`StateNodeFetcher.scala`/`StateValidator.scala`, not yet run. | sync, core | `4f42f4e31` (Batch A: SyncProtocol/Checkpoint family + ChainId), `5930054cf` (Batch B: SNAP recovery/healing actors + TrieRoot/CodeHash family) |
| July sprint, Batch 1 (IP-14 correction to IP-CL-K2) | **Correction: the "real bug" IP-CL-K2 claimed to catch at n=0 was a false positive — `0x80` (empty string) is the canonical RLP zero-scalar encoding, not `0x00`.** IP-CL-K2's `testEssential`/`testOnly` couldn't run at the time (blocked queue-wide by the pre-existing GasAmount/BaseFeePerGas compile baseline), so its regression test (`ETHPacketsRoundTripSpec.scala`, zero-block-number case) was approved by forge+beacon tracing code logic only, never executed. IP-14 (2026-07-04) cleared that baseline for the first time and the test failed on first real run. Investigation cross-checked the official `ethereum/tests/RLPTests/rlptest.json` vector (`"zero": {"in": 0, "out": "0x80"}`) and independently against go-ethereum, core-geth, besu, nethermind, and erigon's own RLP encoders/tests — all agree `0x80` is canonical. Re-deriving the actual Scala byte arithmetic: `byteToByteArray(0)` (`RLP.scala:125-127`) and `bigIntToUnsignedByteArray(BigInt(0))` (`ByteUtils.scala:57-61`) both already produced the identical `Array.empty[Byte]` for input `0` — there was never a real divergence between the hand-rolled path and the codec path at n=0; both forge and beacon independently re-reviewed this correction (2026-07-04) and confirmed it (see `QUEUE.md` Findings Resolution Log, `IP-14` Chase & Deferred item). Fixed `ETHPacketsRoundTripSpec.scala`'s assertion and test name to expect the correct `Array.empty[Byte]`/`0x80` value, and discovered a second, independent bug while fixing it: `RLPValue` is a plain `case class RLPValue(bytes: Array[Byte])` with no custom `equals`, so a `shouldBe` comparison nested inside `RLPList`/`RLPValue` silently falls back to Java's reference-identity array equality (ScalaTest's content-aware Array equality only applies to top-level Array comparisons) — the test now destructures and compares the raw `Array[Byte]` fields directly instead of nesting them in case-class equality. `PATTERNS.md`'s "Opaque-type RLP encode" entry corrected in place with a prefixed correction note — the specific worked example was wrong but the general "trace both codepaths, don't assume equivalence either direction" principle still holds. `sbt-run.sh ip-14-rlp-fix-verify2 testOnly *ETHPacketsRoundTripSpec*`: confirmed passing. | network, core | `2c1b89bd9` |
| July sprint, Batch 1 (IP-14) | **Test-source baseline clear — 105 files (89 src/test + 15 src/it + 1 src/rpcTest).** Fixed the pre-existing GasAmount/BaseFeePerGas/BlockHash/BlockNumber/Wei test-source compile baseline that predated this queue's tracked sequence (first surfaced during IP-CL-J at ~100 errors, grew to 411 by IP-CL-G's full-error-list rerun, then 399 after IP-CL-H's cascade absorbed 12 rows, then 406 after IP-CL-F — see those rows above for the running count) and had blocked `testOnly`/`testEssential` queue-wide since discovery. All fixes are mechanical `X → Type(X)` opaque-type wraps; includes the `ETHPacketsRoundTripSpec` RLP-zero-scalar correction detailed in the row above. `sbt compile-all`: 0 main-source errors, 0 test-source errors (baseline fully cleared). `testEssential`: 3770/3770 passing on first post-fix run (one test-only false positive fixed inline, see row above); re-confirmed 3881/3881 (full aliased chain, 4 sub-suites) on BATCH-1-CLOSE's independent Wave 1 re-run. | core, consensus, network, api, vm/ledger, sync, storage | `2c1b89bd9` |
| July sprint, Batch 1 (BATCH-1-CLOSE) | **Final close gate for Batch 1 (IP-CL-A → IP-14).** Two-wave gate per `QUEUE.md`: Wave 1 (`eye`) re-ran `testEssential` fresh as an independent check of IP-14's own recorded result — first attempt died silently ~30min in (no process, no OOM, no finish trailer; infra flake, not a code issue, host uptime/load showed no freeze/reboot), retried cleanly in ~12min: 3881/3881 passing across the full 4-suite aliased chain (3770+26+11+74), 0 failures, GREEN. Wave 2 ran `forge`, `beacon`, `prism` in parallel, all APPROVED: forge confirmed zero `forTimestamp()` leakage into `consensus/`/`ledger/` and no regression vs IP-12-FG/IP-10d-CS-FG scope; beacon confirmed zero `forBlock()` leakage into ETH/PoS paths and re-confirmed PARITY-01 (base-fee decrease-delta floor divergence) remains untouched/out-of-scope; prism's 8-lens sweep across all of Batch 1's touched non-consensus files found 0 P0 (no close blockers), 6 P1 + 9 P2 — all pre-existing or minor, none introduced by this batch, logged to `QUEUE.md`'s Chase & Deferred Items as `PRISM-B1-01` through `PRISM-B1-05`. No fix window needed. **Batch 1 full commit list (chronological):** `f30bcde3f`, `e18ddd188`, `418106877`, `75b290bbf`, `8bbdc84b0`, `8418c42e1`, `0f5ccd251`, `0e478c301`, `5a6c09ecd`, `837a1ac36`, `8c382316d`, `a007ef951`, `e14231b07` (H-series, see Cross-Cutting rows above and `july-2026-h-series-detail.md`) → `34a933ccc`, `8f474d800`, `1a984d40f`, `cf1cbed07` (IP-CL-A) → `4f42f4e31`, `5930054cf` (IP-CL-J) → `0b14b46c7` (IP-CL-K) → `a9ace9f42` (IP-CL-K2) → `c9b34213a` (IP-CL-G) → `477cb7f9b` (IP-CL-H) → `3bab1108f` (IP-CL-F) → `14a0b20d8` (IP-CL-DE) → `6e1a143c0` (IP-CL-I) → `2c1b89bd9` (IP-14 + its IP-CL-K2 correction). Batch 1 CLOSED 2026-07-04. Batch 1's full prompt/context content (all IP-CL-* kickoff text, H-series detail, and this BATCH-1-CLOSE section) was moved out of `QUEUE.md` via `sprint-clear.sh --apply` into `.claude/sprints/completed/july-fourth-CLEARED.md`, then retired to `.claude/sprints/archive/july-fourth-CLEARED.md` via `sprint-archive.sh --apply` (this row is the required log reference gating that archive move). `sprint-clear.sh` itself had a boundary-detection bug found during this close (it ended a CLOSED batch's section at the first level-2 `## ` header, which falsely truncated Batch 1 to its intro paragraph since IP-CL-* prompt bodies use `## CONTEXT`/`## INSTRUCTIONS` as internal sub-headers) — fixed to bound only on the next `### Batch` header. | core, consensus, network, api, vm/ledger, sync, storage | _(this docs commit)_ |
| July sprint, Batch 1 (IP-CL-K + IP-CL-K2) | **Network/P2P peer + handshake sweep, then ETHPackets wire-ADT sweep.** IP-CL-K (no gate, 3 files): `NetworkPeerManagerActor.UpdateClHeadCmd`/`UpdateClHead` (Command payload + internal msg) and `EthNodeStatus68ExchangeState.handleStatus68Fields`'s `totalDifficulty` param retyped BigInt→BlockNumber/TotalDifficulty; one cascade fix in `SNAPSyncController.scala`. `forkid/ForkId.scala:62` `maxBlockSentinel` assessed and left as raw `BigInt` — genuinely an internal EIP-2124 CRC32 comparison bound, never crosses a BlockNumber-typed boundary; documented as BORDERLINE-not-leakage rather than silently dropped (closes the Findings Resolution Log row). IP-CL-K2 (forge+beacon gated, `network/p2p/messages/` joint bucket): `NewBlockHashes.BlockHash.number` and `NewBlock.totalDifficulty` retyped in the RLP wire ADT, with cascades into `BlockBroadcast.scala`, `BlockFetcher.scala`, `NetworkPeerManagerActor.scala` (main) + 10 test files. **Gate caught a real bug on first pass:** the initial hand-rolled encoder (`RLPValue(ByteUtils.bigIntToUnsignedByteArray(...))`) for `BlockHash.number` diverged from the pre-refactor path at `n=0` — `0x80` (empty-string form) instead of the canonical `0x00`, because it bypassed `bigIntCodec.encode`'s zero special-case. Forge blocked, mithril fixed by routing through `BlockNumber.rlpCodec.encode(...)` instead (which delegates to the same `bigIntCodec.encode`, restoring byte-identical output for all n including 0), added a regression test pinning the zero case in `ETHPacketsRoundTripSpec.scala`. Forge and beacon both independently re-traced the codec chain and approved on re-review; beacon additionally confirmed the deliberately-different `TotalDifficulty` encode path (kept as raw `bigIntToUnsignedByteArray`, not routed through its own codec) is correct as-is — routing it through `rlpCodec.encode` would itself have changed TD's zero-byte encoding. `sbt compile-all`: 0 main-source errors in both items; `testOnly`/`testEssential` still blocked queue-wide by the pre-existing GasAmount/BaseFeePerGas baseline (same blocker as IP-CL-J, unaffected by this sweep). See the pattern captured in `sprints/patterns/PATTERNS.md` — "Opaque-type RLP encode: delegate to the type's own codec, never hand-roll the byte conversion." | network, core | `0b14b46c7` (IP-CL-K), `a9ace9f42` (IP-CL-K2, post-gate) |
| July sprint, Batch 1 (IP-CL-G) | **PoW/Ethash consensus family sweep (forge-only gate) — 9 known-site files + 30 cascade files.** `BlockRewardCalculator.scala` (both BlockNumber era-boundary ctor params and Wei reward-amount fields, same file/reviewer): `byzantiumBlockNumber`/`constantinopleBlockNumber`→`BlockNumber`; `firstEraBlockReward`/`newRewardAfterByzantium`/`newRewardAfterConstantinople` + `newBlockReward`/`calculateMiningRewardPerOmmer` return type→`Wei`; `eraDuration` + ratio numer/denom fields deliberately left raw `BigInt` (dimensionless constants). `Config.scala`'s `MonetaryPolicyConfig` Wei fields (fork-dispatch `blockNumber` fields left for IP-CL-I). `MESSConfig.scala` (ECIP-1100 window fields→`BlockNumber`). `WorkNotifier.scala` (mining work package→`BlockNumber`). `EthashDifficultyCalculator.scala`/`TargetTimeDifficultyCalculator.scala` (half-typed `parentDiff` local→`Difficulty`, redundant wrap/unwrap round-trip collapsed to one final wrap). `StdOmmersValidator.scala`(×5)/`OmmersValidator.scala`(×2)/`OmmersPool.scala` (`parentHash`/`parentBlockHash`→`BlockHash`). Forge independently traced the live diff (not the prose): ECIP-1017 emission arithmetic, ommer depth-clamp (`blockNumber.value.min(OmmerGenerationLimit).toInt`), MESS window comparisons, and the difficulty round-trip collapse all confirmed byte-identical; also verified mithril's two claimed edge cases (the deliberate `.value` unwrap in `calculateMiningRewardForOmmers`/`calculateMiningRewardPerOmmer` to avoid a `Wei.*(BigInt)` vs `Wei.*(Long)` overload ambiguity, and the difficulty calculators' round-trip collapse) directly against the code rather than taking the report at face value. `sbt compile-all`: 0 main-source errors across 3 runs (`set every maxErrors := 100000` used to see the true error count, not sbt's 100-message display cap); confirmed the 411 remaining test-source errors are a pre-existing baseline substantially larger than IP-CL-J's original ~100-error estimate (also touching unrelated `BlockHash`/`BlockNumber`/`Wei` DTOs in `jsonrpc`/`network`/`vm` test sources, not just `GasAmount`/`BaseFeePerGas`) and are NOT owned by this batch's 9-file scope — left unabsorbed for a future batch. `sbt scalafmtAll` clean. | consensus, core, ledger | `c9b34213a` |
| July sprint, Batch 1 (IP-CL-H) | **Engine API / PoS family sweep (beacon-only gate) — 2 known-site files + 4 cascade files.** `EngineApiDomain.scala`: `ExecutionPayload.stateRoot`→`TrieRoot`, `.logsBloom`→`BloomFilter`, `.blockNumber`→`BlockNumber`, `.timestamp`→`Timestamp`, `.baseFeePerGas`→`BaseFeePerGas`; `PayloadAttributes.timestamp`→`Timestamp`. `EngineApiService.scala`: `getLatestBlockNumber` return type→`BlockNumber`; redundant wraps removed at `payloadToBlock` field-construction sites; `attrs.timestamp` comparison/hash-input/fork-dispatch sites now pass the typed field directly. PARITY-01's inline EIP-1559 base-fee calc explicitly left untouched (confirmed via `git diff -U0` hunk gap by both mithril and, independently, beacon). `EngineApiController.scala` cascade: JSON-RPC wire-boundary decoders (`decodeExecutionPayload`/`decodePayloadAttributes`) now wrap at the S11 boundary — JSON wire format itself unchanged, the encoder builds its `JObject` from `block.header` fields and never serializes an `ExecutionPayload` instance. Beacon independently traced the live diff (not the prose): (1) JSON round-trip byte-identical, (2) `forTimestamp` fork dispatch unaffected, (3) PARITY-01 untouched, (4) `stateRoot`/`logsBloom` flow into `BlockHeader` unchanged. `sbt compile-all`: 0 errors in any `consensus/engine/*` file (2 full runs); confirmed the 399 remaining test-source errors are the same pre-existing GasAmount/BaseFeePerGas/BlockHash/BlockNumber/Wei baseline documented in IP-CL-G's row, not owned by this batch's 2-file scope (12 pre-existing engine-package rows absorbed as part of this sweep's own cascade, dropping the baseline count 411→399). `sbt scalafmtAll` clean. | consensus, core | `477cb7f9b` |
| July sprint, Batch 1 (IP-CL-F) | **EVM/VM execution + tracer family sweep (forge+beacon gate, joint `vm/` bucket) — 13 known-site files + cascade into 51 total (24 main + 27 test).** `vm/Storage.scala` trait `store`/`load` `offset`→`StorageKey` (root SSTORE/SLOAD interface), cascading into `vm/OpCode.scala` (×6 sites), `EthSimulateService.scala`/`BlockExecution.scala`'s EIP-4788/EIP-2935 system-call storage writes, and ~20 test files. 4-file tracer family (`ExecutionTracer`/`CallTracer`/`PrestateTracer`/`VmTracer`) `value`→`Wei`; `StructLogTracer.scala` `gas`→`GasAmount` (dead members only — the class itself is live, backs `debug_traceTransaction`'s default tracer). `ledger/TxResult.scala`/`BlockResult.scala` `gasUsed`→`GasAmount`, cascading into `BlockExecution.scala`'s `executeBlockNoValidation` return type, `ChainImporter.scala`, `BlockGeneratorSkeleton.scala`, `EngineApiService.scala`, `EthInfoService.scala`. Root `getBlockHashByNumber`/`getBlockByNumber` callback in `InMemoryWorldStateProxy.scala`/`TestModeWorldStateProxy.scala` retyped `BigInt => Option[ByteString]`→`BlockNumber => Option[BlockHash]`, resolving the round-1 straggler audit's single highest-leverage BlockNumber finding (~15 duplicated call-site lambdas across `StxLedger`, `BlockExecution`, `BlockPreparator`, `TestModeBlockExecution`, `EthUserService`, `EthSimulateService`, `GraphQLSchema`). `vm/Program.scala:48` `codeHash` assessed and left unconverted — zero call sites anywhere in the codebase, independently confirmed by mithril, forge, and beacon; flagged as a possible future dead-code-review candidate, not acted on in this sweep. Both forge and beacon independently traced the live diff (not the prose summary) and APPROVED all review criteria: `Storage[S]` SLOAD/SSTORE hot-path arithmetic untouched (refund arithmetic, `R_sclear`/`G_sset`/`G_sreset` branches unaffected); EIP-4788/EIP-2935 slot-index computations untouched, only the already-computed key wrapped (beacon's ETH-specific check); `getBlockHashByNumber` cascade confirmed to produce identical block-hash lookups and to remain cleanly separate from EIP-2935's persisted historical-hash storage mechanism (no double-sourcing); tracer family confirmed debug-output-only with byte-identical hex serialization; `TxResult`/`BlockResult.gasUsed` cascade confirmed to introduce no double-conversion; Engine API touch (`gasUsedTotal`/proposer-mode field) confirmed via `git diff -U0` to not reopen IP-CL-H's already-approved `ExecutionPayload` fields or touch PARITY-01's inline base-fee block. `sbt compile-all`: 0 main-source errors (4 iterative runs during implementation); 406 pre-existing test-source baseline errors unchanged (same GasAmount/BaseFeePerGas/BlockHash/BlockNumber/Wei DTO family documented in IP-CL-G's/IP-CL-H's rows), confirmed none reference this sweep's files. `sbt scalafmtAll` clean. Neither forge nor beacon re-ran `sbt compile-all` in-agent (operator-terminal-only per resource protocol) — both rested their verdicts on identity-tracing of the 5 opaque types involved, all confirmed pure `apply`/`value`-identity wrappers with no arithmetic in the wrap/unwrap path. | consensus, vm/ledger, core | `3bab1108f` |
| July sprint, Batch 1.5 | **BATCH-1-CLOSE prism follow-ups (`Gate: none`) — 7 findings + 2 emergent fixes.** First batch drafted and executed via the new `scout`/`batch-research-protocol.md` tooling (built the same day, in direct response to Batch 1's growth). `scout`'s multi-pass sweep expanded prism's original 5 findings to 7: `CheckpointDownloader.scala` had the identical I/O-swallow pattern flagged in sibling `CheckpointExporter.scala` (4 sites); `PersonalService.ImportRawKeyRequest` had the identical cleartext-key-leak shape flagged in `Wallet.scala`. Fixes: log instead of swallow on stream-close errors (both checkpoint files); `toString`-only key redaction on `Wallet`/`ImportRawKeyRequest` (equals/hashCode untouched — `KeyStoreImplSpec.scala:128`'s structural-equality assertion depends on it); deleted `Timestamp`'s unreachable `==`/`!=`; `EthBlocksService`/`FukuiiService`/`TransactionResponse`'s module-level `given blockchainConfig` → constructor injection, with `nodebuilder/NodeBuilder.scala` construction-site fixes; `TransactionResponse`'s 8-element positional tuple → named `TxTypeFields`; `EthInfoService.createAccessList`'s partial/always-empty impl documented (real VM access-tracking is a separate feature, scheduled into the Parity Batch's `PP-00` PHASE 5); dead `TxHash.fromBytes` deleted; `CheckpointExporterSpec` gained 3 new tests. **Two things found beyond the drafted scope, both disclosed and reviewed, not silently absorbed:** (1) `TransactionResponse.scala`'s `given blockchainConfig` was believed dead code (prism's grep-based "zero references" verdict) but `SignedTransaction.getSender` consumes it via an invisible `using BlockchainConfig` clause — restored with an explanatory comment; (2) writing the requested regression test (`OpaqueOrderingResolutionSpec.scala`, all 12 domain opaque-type `given Ordering[X]` instances) hung the JVM on `ChainId` — a self-referential lazy-val deadlock `ChainId.scala`/`Timestamp.scala` still had while 10 sibling types had already been fixed during Batch 1; pinned both to the established pattern. Since both fixes touch `domain/` (FORGE+BEACON joint scope per `consensus-change-protocol.md`) and neither was in the batch's original `Gate: none` scope, both were reviewed **retroactively** before commit: forge PASS (ETC/EIP-155 path unaffected, zero consensus-relevant callers of either `Ordering` instance), beacon APPROVED (ETH/timestamp-fork-dispatch path uses raw `Long` comparison, never `Ordering[Timestamp]`; zero consensus-relevant callers). prism re-verified the full diff: 0 critical, 0 warning, 2 info (both applied — `EthInfoService`'s permanent-limitation log downgraded `warn`→`debug`; a tuple-refactor suggestion logged to Chase & Deferred). Durable lessons captured: `scala3-style.md` S12 (grep-verifiable ratchet for the self-referential-given footgun, confirmed zero remaining bare instances repo-wide), `dead-code-review.md` + `batch-research-protocol.md` (a grep-based "zero references" claim is preliminary, not confirmed, without a removal+compile check), `consensus-change-protocol.md` (a batch's own `Gate: none` status doesn't extend to a file discovered mid-implementation outside its original scope — the actual gap that let this land without a live gate), `sprints/patterns/PATTERNS.md` (both lessons as a combined entry). `sbt compile-all`/`scalafmtAll`: clean. Targeted `testOnly` across all touched specs: 97 tests, 0 failures. | core, api, storage | `2b80a2cc5` (implementation), `bdbadf9b8` (protocol/doc updates) |
| July sprint, Batch 2 (Class A TestProbe migration) | **Migrate Class A test-collaborator `TestProbe`s from Classic Pekko to Typed across the test suite — 3 sub-batches, CLOSED 2026-07-06.** Sub-batch 2a (26 files, 49 probes, `97dcc659c`), 2b (27 files, 164 probes, `623ac6700`, `eye`-validated APPROVED at 3810/3810 `testStandard`), 2c (6 files, 65 probes, `6762fbe3c`, `eye`-validated APPROVED — compile-all exit 0, `testStandard` 3810/0, targeted specs green). **Probe total: 49 + 164 + 65 = 278 of the RS-09 301-probe baseline.** The remaining 23 (all in 2c's `SyncControllerSpec`/`PeerManagerSpec`) were deferred, not converted, each for a structural reason `eye` independently verified against the code — NOT a mechanical swap: 7 TCP-bridge stubs feed `HandlePeerConnectionCmd(connection: ActorRef)` (Classic by contract, same category as the documented `ServerActor`/`RLPxConnectionHandler` exception); `setAutoPilot`/`probe.reply` sites depend on Classic-only APIs; `peerDiscoveryManager`/`peerEventBus` use Classic boolean-predicate `fishForMessage` (incompatible `FishingOutcome` typed signature — a call-site rewrite, not a probe swap); 9 `SyncControllerSpec` `watcher.watch` sites are dead no-op (never assert `Terminated`); `syncController` itself stays `TestActorRef` for `.children` inspection per its own file header. All 23 routed to Batch 4 (collaborator-type/infra work), same category and same disposition as Batch 2b's 5 "zero-change" files (`PEERTESTHELPERS-01`/`SNAPFAKEPEER-AUTOPILOT-01`). **Pre-existing finding surfaced by 2c's validation (NOT introduced — `eye` git-stashed the 2c diff and reproduced identical failures at HEAD):** `SyncControllerSpec` has **11 SyncTest-tagged tests failing** on `eventually`/`ExplicitlyTriggeredScheduler` timeouts (`None.get`, `isFastSyncDone() was false`), the same symptom signature as `IP-STATESYNC-01`'s unconfirmed layers 2-3 — folded into that item's `forge`-gated resolution as a cluster (see Findings Resolution Log + Batch 4). Both `SyncControllerSpec` and `StateSyncSpec` are `SyncTest`-tagged, which `testEssential`/`testStandard` both exclude (build.sbt:534), so the push gate is unaffected by these reds. Two probe-count corrections vs. the sub-batch reports (`eye`'s recount from the diffs): 2c is 65 converted / 23 deferred (not 67/21), and the `SyncControllerSpec` failure is 11 tests (not 1). | sync, network | `97dcc659c` (2a), `623ac6700` (2b), `6762fbe3c` (2c) |
| July sprint, IP-STATESYNC-01 cluster (forge-gated, TEST-ONLY) | **Resolved `StateSyncSpec`'s long-parked SyncTest failures; precisely root-caused `SyncControllerSpec`'s 11 and folded them into Batch 4.** Pulled forward ahead of Batch 3 per operator decision. `scout` blast-radius pass bounded the cluster to exactly 2 files (every other SyncTest spec clean or belonging to the separate `REPO-06-ITSUITE`). `forge` impact analysis: **TEST-ONLY** — `SyncStateSchedulerActor.scala` production code is correct and untouched (re-sync rebuilds fresh state each `StartSyncingTo`; stale cross-cycle responses tolerated as `UnrequestedResponse`, no consensus impact). **StateSyncSpec (FIXED):** the dominant root cause was deeper than scout's lazy-val theory — the test AutoPilot replied via `sender ! MessageFromPeer`, but the Typed `PeerRequestHandler` only receives via a `PeerEventBus` subscription, so replies hit `deadLetters`. Fix: real `PeerEventBusActor` + `PublishCmd` delivery, `scheduleOnce(20ms)` to order subscribe-before-publish, plus per-`forAll`-iteration actor isolation (fresh `SchedulerFixture`, torn down in `finally`). Layer 1 (`SendMessage`→`SendMessageCmd` `MatchError`) committed together with layers 2-3. `eye` validated: 5/5 scenarios pass across 2 independent runs incl. the prior exit-255 crash scenario, no flakiness, production untouched, compile-all green. **SyncControllerSpec (11 deferred to Batch 4):** same event-bus reply-routing breakage, but this spec runs on `ExplicitlyTriggeredScheduler`, so it needs a virtual-time-aware real-PEB rewrite — folded into the existing Batch 4 `.children`-rewrite row (land the stability fix there before/with that rewrite). `OptionValues` mixin added (`.get`→`.value`, 13 sites) as a kept ergonomics improvement (proper `eventually` retry + file:line message) though it clears 0 of the 11 (baseline-diffed identical). **Watch item** (`STATESYNC-FLAKE-WATCH-01`, Chase): the `scheduleOnce(20ms)` is a timing assumption weaker than an `expectMessageType` happens-before — small residual flake surface under heavy CI contention. Both specs are `SyncTest`-tagged (excluded from `testEssential`/`testStandard`), so none of this was ever inside the push gate. | sync | `fa16aeaf9` (StateSyncSpec, all 3 layers), `bc95ad72c` (SyncControllerSpec OptionValues) |

**Note on PR #1384:** the SNAP-layer BlockNumber commit (`e14231b07`) and every subsequent
SNAP-touching prompt in this sprint (`IP-CL-J` Batch B, `IP-CL-DE`) explicitly carries forward
the "all SNAP files land in one commit" discipline established here, because a prior partial
SNAP sweep caused a stale-base regression (PR #1384). Any future SNAP-layer opaque-type or
type-safety work should follow the same rule.
